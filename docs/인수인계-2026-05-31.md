# 🤝 인수인계 — Insight Out (2026-05-31)

> 새 Cowork(Opus) 세션이 이어받기 위한 현재 상태·다음 액션 정리.
> 이 문서 하나만 읽으면 맥락이 복원되도록 작성. 상세는 링크된 문서 참조.

---

## 0. 30초 요약

- **프로젝트**: Insight Out — LGU+ B2B 담당자용 Market Intelligence 플랫폼 (Next.js 16 + Supabase + Vercel).
- **협업 구조**: **Opus(Cowork=나)** 가 설계·지시서·검증 / **Sonnet(Claude Code)** 가 구현 / **David(용주)** 가 위임·머지 / **수희** 가 Supabase·Vercel 실행. 루프 상세: [`작업-루프.md`](./작업-루프.md).
- **지금 위치**: Phase 1-B(DB 설계) 완료·전부 main 커밋됨. 다음은 **수희 SQL 실행**, **Notion 동기화**, **#4 크롤링 아키텍처 결정**.
- **권한 주의**: David 는 Supabase 권한 없음 → 모든 SQL 은 수희가 SQL Editor 에서 실행해야 반영됨.

---

## 1. 완료됨 (main 에 커밋, working tree 깨끗)

| 항목 | 산출물 | 상태 |
|---|---|---|
| 콘텐츠+북마크 DB 스키마 설계 (#1·#2) | `supabase/schema.sql` (단일 진실) | ✅ libpg_query 파싱 검증 |
| 설계 검토 문서 / ER | [`콘텐츠-북마크-DB설계-검토.md`](./콘텐츠-북마크-DB설계-검토.md) · [`콘텐츠-북마크-ER다이어그램.mermaid`](./콘텐츠-북마크-ER다이어그램.mermaid) | ✅ |
| TS 타입 동기화 (지시서01) | `src/lib/types.ts` (enum4+interface11+라벨맵) | ✅ Opus 검증(11테이블 1:1, tsc·lint 통과) |
| 크롤링 파이프라인 설계 (#3) | [`크롤링-파이프라인-설계.md`](./크롤링-파이프라인-설계.md) · [`크롤링-시퀀스.mermaid`](./크롤링-시퀀스.mermaid) | ✅ PRD 4장 반영까지 |
| 파비콘 미갱신 수정 | `src/app/favicon.ico`·`public/favicon.ico` 16/32/48 멀티해상도 복구 | ✅ 시각검증 |
| 수희용 SQL 분할 | `supabase/2026-05-30-콘텐츠-북마크-0{1..4}.sql` | ✅ 4파일 합계 86구문=합본과 동일 |

**핵심 설계 결정**(전부 권장안 채택): 단일 `contents` + `youtube_videos`·`ai_reports` 별도 / 카테고리 `content_category` enum 8개 / 매핑 `content_services` / 다형 참조는 nullable FK + CHECK(정확히 하나).

---

## 2. 다음 액션 (우선순위 순)

### 🔴 A. 수희 — Supabase SQL 실행 (lag 게이트)
`supabase/` 의 분할 파일을 **순서대로** SQL Editor 에 실행:
```
2026-05-30-콘텐츠-북마크-01-types.sql      (enum 4)
2026-05-30-콘텐츠-북마크-02-tables.sql     (table 11 + index 20)
2026-05-30-콘텐츠-북마크-03-triggers.sql   (trigger 7 + function 1)
2026-05-30-콘텐츠-북마크-04-rls.sql        (RLS enable 11 + policy 32)
```
선행조건: 기본 스키마(users·services·`set_updated_at()`·`is_admin()`)가 이미 적용돼 있어야 함. 문제 시 `supabase/repair.sql`(멱등 복구).
> 합본 `2026-05-30-콘텐츠-북마크.sql` 도 있으나, 수희가 하나씩 올리길 원해 4분할이 기본.

### ✅ B. Notion 작업계획서 신호등 동기화 — **완료 (2026-05-31)**
git [`docs/작업계획서.md`](./작업계획서.md) ↔ Notion "04. 작업계획서"(id `3702f2c9-f358-80bf-8bf5-fb7aac0a4d22`) 신호등 일치시킴.
- 용주 트랙 7개 행 Notion 갱신: 콘텐츠/북마크 스키마 🔵, 뉴스레터구독 🟢, Supabase SQL 🔵, 크롤링설계 🔵, 크롤링구현 🔴, 뱃지→텍스트 🟡.
- 수희 Phase 1-A: **온보딩 저장 오류·팀 이름 오류 → 둘 다 🟢** (git이 stale였음 → git도 🟢로 올림. Notion은 이미 🟢).
- **🔑 동기화 규칙**: 수희는 Notion 무료플랜 **게스트라 Notion 직접 수정 불가** → 수희 상태는 **Opus 가 git↔Notion 양쪽을 같은 신호등으로 맞춤**. 앞으로도 수희 진행분은 이 방식으로 미러링.
- Notion 도구: `notion-fetch`(읽기)·`notion-update-page`(command=update_content, 표 셀 `<td>⚪</td>\n<td>작업명</td>` 단위로 old_str 잡으면 고유).

### ✅ C. #4 크롤링 아키텍처 — **결정: A (TS) (2026-05-31)**
**A. TS + Next.js API Route(`/api/cron/crawl`) + Vercel Cron** 확정. (B=Python 별도서버는 소스폭증·JS렌더링 대량 시 재검토.)
- 블로커가 바뀜: ~~아키텍처 결정~~ → 이제 **수희 1-B 스키마 적용(§2-A의 P1)** 이 #4 의 유일한 블로커.
- 착수 선행조건(병렬 준비 가능): P3 `CRON_SECRET` Vercel 추가, P4 `SUPABASE_SERVICE_ROLE_KEY` 확인(수희) / P5 `vercel.json` 크론 `0 20 * * *`, P6 RSS·본문추출 의존성(코드, #4 PR). 상세 [크롤링 설계 §10](./크롤링-파이프라인-설계.md).
- **#4 코드 착수 = P1(스키마 적용) 후** (실제 테이블 대상 테스트 가능해야 함). 소스는 하드코딩 아님(`sources` admin 관리), 초기 seed 만 준비.

### 🟢 D. Sonnet 지시서 02 (뱃지→텍스트) — 위임 대기
[`docs/sonnet-지시서/02-카테고리-뱃지-텍스트교체.md`](./sonnet-지시서/02-카테고리-뱃지-텍스트교체.md) 를 Claude Code 에 던지면 됨. 완료 보고 오면 Opus 가 검증(tsc·lint·뱃지 제거 확인).
- David 위임 복붙: `docs/sonnet-지시서/02-카테고리-뱃지-텍스트교체.md 의 지시서대로 작업해줘. 완료되면 보고 양식대로 보고해줘.`

---

## 3. 스키마 백로그 (Opus 추적 — 미해결)

| # | 이슈 | 처리 시점 |
|---|---|---|
| BL-1 | **서비스 분류 불일치**: schema 시드(STAGE/BizWork/GovLink…)는 placeholder, PRD/UI 실제는 Connectivity·보안클라우드·M2M·AICC·AIDC·모빌리티·기업솔루션. `services` 시드 교체 필요 | Phase 2 진입 전, David 확정 |
| BL-2 | 카테고리 enum값↔UI 라벨 매핑 | ✅ 지시서01 `CONTENT_CATEGORY_LABEL` 로 처리됨 |
| BL-3 | **관련 기사 N건 그룹핑**: PRD 4.4 유사중복 = 대표1건+"관련 N건". 현 `contents` 에 그룹 개념 없음 → `cluster_id`/`duplicate_of` 컬럼 필요 | #4/#12 구현 시 |
| BL-4 | **품질 보류 상태**: PRD 4.5 관련도<0.3 "보류(관리자 검토)", 어드민 "승인 대기 목록". 현 `is_published` boolean → `pending/published/rejected` enum 권장 | #13 구현 시 |
| — | `crawl_logs` 신규 테이블(크롤링 현황 #23용) 제안됨 — #4 구현 시 수희 SQL 추가 | #4 |

---

## 4. 파일 지도

```
supabase/
  schema.sql                         단일 진실 (기본+Phase1-B 전체, 602줄)
  2026-05-30-콘텐츠-북마크-0{1..4}.sql  수희 실행용 분할 (순서대로)
  2026-05-30-콘텐츠-북마크.sql          위 4개 합본
  repair.sql                         멱등 복구 스크립트
docs/
  작업계획서.md                       마스터 계획(git 미러), 신호등
  용주-작업계획.md                     용주 트랙 실행계획(Opus/Sonnet 분업)
  작업-루프.md                        협업 루프 정의 + 지시서 큐 + 백로그
  콘텐츠-북마크-DB설계-검토.md          수희 검토용 설계 근거
  콘텐츠-북마크-ER다이어그램.mermaid
  크롤링-파이프라인-설계.md             #3 설계 + PRD 4장 반영 + 아키텍처 분기
  크롤링-시퀀스.mermaid
  sonnet-지시서/01-타입-동기화.md       ✅ 완료·검증됨
  sonnet-지시서/02-카테고리-뱃지-텍스트교체.md  위임 대기
  인수인계-2026-05-31.md               (이 문서)
src/lib/types.ts                     DB row 타입 (스키마와 1:1)
```

---

## 5. 새 세션이 알아야 할 컨벤션·함정

- **schema.sql 단일 진실** (AGENTS §6). 새 테이블 = RLS 정책 함께 작성 필수 (§7).
- **David ≠ Supabase 권한**. SQL 은 항상 수희가 실행. 코드 머지(PR)와 Supabase 반영은 **별개 경로**.
- **검증은 보고를 믿지 말고 재현**: SQL 은 `pip install --only-binary :all: pglast` 후 `pglast.parse_sql`. Mermaid 는 jsdom+mermaid 로 `mermaid.parse`. (둘 다 이번 세션에서 사용함)
- **샌드박스 한계**: 마운트가 파일 **삭제(unlink) 차단** → `git`/`rm` 이 샌드박스에서 실패할 수 있음(`.git/index.lock` 제거 불가). **커밋·푸시·삭제는 David 가 본인 터미널에서**.
- **파비콘 "안 바뀜"**: 거의 항상 (1) stale `.next`(→ `rm -rf .next && npm run build`) (2) 브라우저 파비콘 캐시(→ 시크릿 창). 파일은 멀쩡한 경우가 많음.
- **AskUserQuestion 응답이 비어 오는 경우** 잦음 → David 의 "그냥 가" 기조에 맞춰 합리적 기본값으로 진행하되, 진짜 분기(아키텍처 등)는 명시적으로 한 번 더 물어볼 것.
- **Next.js 16 (Turbopack)**: 훈련데이터와 다를 수 있음 — `node_modules/next/dist/docs/` 우선 참고 (AGENTS).

---

## 6. 새 세션의 다음 한 수

**크리티컬 패스 = 수희 P1(1-B 스키마 Supabase 적용).** 이게 끝나야 #4 코드 착수 가능.
- 수희에게 P1~P4 요청(분할 SQL 실행 + Vercel 환경변수). David 가 전달.
- 그 동안 막힘없이 할 수 있는 것: Sonnet 지시서02 위임(§2-D)·검증 / BL-1 서비스 분류 확정 / 초기 seed 소스 목록 정리.
- P1 완료되면 → #4 구현(TS, `/api/cron/crawl` + RSS 어댑터 + 중복필터 + 멱등 upsert + crawl_logs + vercel.json 크론).

## 7. 미커밋 상태 (세션 전환 시점)
- 커밋됨(main): Phase 1-B 스키마·타입·설계, 파비콘, SQL 분할, 수희 1-A 신호등(6df8f85).
- **미커밋 문서**(David 커밋 필요): `인수인계-2026-05-31.md`, `phase2a-자동크롤링-상세설계.md`, `크롤링-파이프라인-설계.md`(아키텍처ⓐ 반영), `작업계획서.md`(#4 행 갱신).
- ⚠️ `.git/index.lock` 이 반복 생성됨 → 커밋 전 `rm -f .git/index.lock`.

*작성 2026-05-31 · 갱신: 아키텍처ⓐ 확정·Notion 동기화 완료 반영*
