# 인사이트아웃 — 개선 검토 (2026-07-26) · David 9영역 리뷰

> David 스크린샷 리뷰 9영역에 대한 코드 근거 검토(origin/main adc250e 기준) + 개선안 + 지시서 후보.
> ⚠️ 병렬 조사 일부는 구 스냅샷(5976992)을 읽어 438/439를 "없음"으로 봤으나, **438 발송카드·439 관계지도는 main에 실재**(재현검증 확인). 아래는 정정 반영본.

---

## 즉답 (David가 물은 것)

- **[2] Gemini 100% 초과인데 되나 안 되나?** → **`GEMINI_API_KEYS` 키 개수에 달림.** 라우팅 실제 한도 = `monthly_token_limit × 키개수`(`llm/index.ts:179`). 키 2개�키↑면 유효 한도 2M+ → 1,001,303는 아직 여유 → **Gemini 계속 사용**. 키 1개면 1M 초과 → **Gemini 건너뛰고 groq→cerebras…로 자동 폴백**(서비스는 계속 됨). 전면 중단은 모든 provider 소진 시만. **문제는 UI**: `/admin/llm`은 키개수 곱을 빼고 1M 기준으로 %를 보여줘 "100% = 죽음"처럼 오해를 줌(운영 대시보드 `AdminOpsSignals`는 키 곱 반영해 서로 불일치).
- **[4] 네이버·GDELT 오늘 수집 안 됨** → 가장 유력 원인: **`searchSeeds`(=활성 `keyword_groups.search_seeds` 합집합)가 비어 있으면 `crawlKeywordSearch` 자체가 skip**되어 네이버·GDELT 둘 다 안 돎(`orchestrator.ts:1540`). 그다음 후보: 네이버 env 키(`NAVER_CLIENT_ID/SECRET`) 미설정 → 조용히 [], `GDELT_ENABLED=false`. **확인법**: ①`select name, search_seeds from keyword_groups where is_active`(비면 원인 확정) ②`job_runs` `cron:crawl` 최신 details에 `'Google News 키워드'` 행 유무 ③Vercel 로그 `키워드 검색 수집 시작: N개 시드` / `[네이버 뉴스] 검색 실패`.
- **[5] 어드민 인사이트 카드·이슈가 홈 어디에?** → **이슈는 홈에 노출됨**(IssueRankTicker, `IssueSignals.tsx`). **인사이트 카드(industry)는 사용자에게 안 보임** — `headline` 뷰가 **관리자 전용 LAB 탭**이라 비관리자는 `brief`로 강제 폴백(`AiInsightBoard.tsx:234`). 홈의 "핵심 인사이트"는 insight_cards가 아니라 별도 `daily_insights` 테이블. **company-scope 카드(주요기업)는 렌더 표면 자체가 없음.** → 안 보이는 게 정상(노출 경로 미구현).

---

## 항목별 검토

### [1] 메일 발송 이력에 뉴스레터 포함
- 현황: 438 카드(`AdminMailDispatchCard`)가 job_runs 3키(ops-brief/weekly/alert)만. 뉴스레터는 `cron:newsletter`(job_runs) + `newsletter_issues`(sent_on·recipient_cnt·status)에 기록됨.
- 개선: 카드에 뉴스레터 행 추가 — 수신자수·제목까지면 `newsletter_issues` 조회 권장.
- 규모 **소** · SQL 없음.

### [1b] 오늘 할일 재구성 (LLM/TTS 실패 + 메일이력 하위 이동)
- 현황: `AdminTodoBlock`(검토대기·수집실패·소스오류·승인대기). LLM 실패 신호 = `job_runs status=failed`(cron:briefing/ai-refresh) 또는 `briefings.status='failed'`. **TTS 실패는 계측 자체가 없음**(console.error만) — 신호 만들려면 코드+컬럼 필요.
- 개선: LLM 실패 타일 추가(SQL 0). 메일이력 카드를 오늘할일 하위로 이동 = JSX 재배치(쉬움). TTS 실패 타일은 계측 선행(중).
- 규모 **소(LLM/재배치)~중(TTS)** · SQL: TTS만 방식따라.

### [2] LLM 사용량 초과 상태 표시
- 위 즉답. 개선: `/admin/llm` %를 **키개수 곱 반영**으로 통일 + 초과 시 "차단(폴백 전환)"/비활성/쿨다운 상태 배지.
- 규모 **소** · SQL 없음.

### [3] 뉴스탭 액션 버튼 톤앤매너
- 현황: `AdminContentManager.tsx` RowActions가 하드코딩 `h-8 text-xs` + 임의 색 클래스 → 표준 `Button size="sm"`(h-7)·`variant="destructive"`와 불일치. 앱 내 삭제 버튼 관례도 제각각.
- 개선: 표준 variant/size로 통일(삭제=destructive, 하드코딩 제거).
- 규모 **소** · SQL 없음.

### [4] 검토대기 사유 한눈에
- 현황: `contents.review_reason`(body_missing/body_short/extract_failed/low_relevance/llm_irrelevant/excluded_rule)이 **이미 배지로 표시**됨(`status-style.ts`, AdminContentManager). 단 `review_reason=null`(레거시/미적용)이면 칩 안 뜸.
- 개선: null 행에 폴백 추론(body_fetched_at null→"본문 없음", matched_groups null→"매칭 없음"; select에 matched_groups 추가). SQL 178(review_reason) 적용 여부 확인.
- 규모 **소** · SQL 없음(178 적용 전제).

### [4b] 네이버·GDELT 수집 원인 — 위 즉답. 진단 후 조치(seed/키/GDELT_ENABLED). 코드측 개선: 네이버/GDELT/RSS **소스별 카운터 분리**(현재 'Google News 키워드' 한 줄로 합산돼 진단 불가). 규모 소.

### [5] 인사이트 카드 사용자 노출 — 위 즉답.
- 개선 옵션: ①`headline` 뷰 LAB→PRIMARY 승격(가장 저렴) ②홈에 "기업 인사이트" 섹션 신설 ③company-scope 카드는 `/dashboard/entities`(주요기업)에서 조회·렌더 추가.
- 규모 **소~중** · SQL 없음.

### [6] 관계지도 재생성 주기
- 판단: **"리셋/재생성 주기" 개념 부적합.** 439 관계지도는 content_entities를 실시간 집계 → 크롤이 돌며 content_entities가 쌓이면 자동 최신화(별도 배치 없음). content_entities는 크롤 파이프라인(룰 기반 alias 매칭, LLM 0)에서 upsert.
- 유일한 실질 갭: **엔티티/별칭 추가 후 과거 콘텐츠에 소급 링크 안 됨**. 필요하면 "재링킹 백필" 관리자 트리거(그래프 재생성이 아니라).
- 규모: 재생성 버튼 불필요 / 재링킹 백필 **중** · SQL 없음.

### [7] AI 리포트 품질 · Claude/GPT MCP 고도화
- 현황: 전략보고서 = `generate-strategy.ts`가 **무료 LLM 폴백 풀**(Gemini/Groq/Cerebras/SambaNova/Mistral)로 body 생성. 프롬프트 `llm_prompts(strategy_report)`. **MCP는 이미 반대방향(외부 Claude→데이터 pull→reports write) 구현됨.**
- 옵션 A(품질 최우선): `LLM_PROVIDERS`에 **Anthropic/OpenAI 어댑터 추가** + `report` 태스크만 Claude/GPT로 라우팅. → 유료 API 키·env 필요, 중.
- 옵션 B(비용 0): 기존 MCP 반자동(competitor-weekly 분석 패턴)을 전략보고서에 일반화(근거 자동첨부·프롬프트 리소스화). → 사람 개입, 소~중.
- 옵션 C(하이브리드): 기본 무료 풀 + 실패/품질미달 시 Claude API 승격.
- **결정 필요**: 유료 API 도입 여부(A/C) vs MCP 반자동(B). 규모 중 · 외부연동(A/C).

### [8] 주간 브리핑 생성 오류 ⚠️ 실제 깨짐
- 원인 확정: `AdminCompetitorWeeklyAnalyze.tsx:52` "프롬프트+데이터 복사"가 **`await fetch()` 후 `navigator.clipboard.writeText()`** 호출 → 사용자 제스처 활성화 소실로 `NotAllowedError`(스크린샷 문구와 정확 일치). 폴백 없어 즉시 실패 노출.
- 수정: fetch로 데이터 먼저 상태 저장 → 별도 복사 버튼(즉시 write) / 또는 `<textarea>`에 렌더 + writeText 실패 시 수동 복사 안내. 
- 규모 **소** · SQL 없음. **우선순위 높음(기능 차단 상태).**

### [9a] 주요기업 뉴스 주간 예약수집 + 주별 아카이빙/조회
- 현황: 회사 카드(`generateCompanyInsightCards`)는 cron `ai-refresh`(매일)에서 **90일 롤링 윈도우**로 생성 → period_start가 매일 밀려 주 경계와 무관, "회사별 최신 1건"만 표시(주 선택 없음).
- 핵심: `insight_cards.period_start/end` + 유니크 인덱스 `(period_start,scope,topic)` **이미 존재** → **competitor_weekly 패턴**(getLastCompletedWeekKst 주경계·cron 게이트·수동 트리거·주 선택 타임라인) 그대로 이식 가능.
- 규모 **중** · **SQL 없음**(재사용).

### [9b] 주요기업 상세 가독성
- 현황: `insights/[id]/page.tsx:171`이 implication을 **단일 `<p>`**로 렌더(①②③가 한 문단에 뭉침). 렌더 순서 시사점→근거→키워드.
- 개선: implication을 `①②③`/개행 split해 리스트·문단 렌더(즉효, 재생성 불필요) + 순서 근거→시사점 조정. 일관성 원하면 프롬프트(`llm_prompts`) 구조화 출력(재생성 필요).
- 규모 **소** · SQL 없음.

### [9c] 경쟁사 최근 뉴스를 주요기업 하위로 통합
- 현황: 경쟁사 뉴스(`competitor-news.ts`, `entities.is_competitor`+raw contents)와 주요기업(`curated_companies`+insight_cards)은 **회사 식별 원천이 다름**. 주요기업 "상세"는 회사 허브가 아니라 카드 상세.
- 개선: 최소안=카드 상세에 "이 회사 최근 뉴스" 섹션 추가(중) / 완전안=회사 허브 신설+IA 재편+경쟁사탭 흡수(대, curated↔entity 매핑 정합 필요).
- 규모 **중~대** · SQL 없음.

---

## 지시서 후보 (제안 · 다음 440부터)
| 후보 | 항목 | 규모 | SQL | 우선 |
|---|---|---|---|---|
| **440 주간브리핑 생성 clipboard 오류 수정** | 8 | 소 | 0 | ★★★ 기능 차단 |
| **441 대시보드 개선**(메일이력+뉴스레터, LLM실패 타일, 메일이력 오늘할일 하위 이동) | 1·1b | 소 | 0 | ★★ |
| **442 뉴스탭 버튼 톤 통일 + 검토대기 사유 폴백** | 3·4a | 소 | 0 | ★★ |
| **443 LLM 사용량 초과상태·키개수 반영 UI** | 2 | 소 | 0 | ★★ |
| **444 인사이트 카드 사용자 노출** | 5 | 소~중 | 0 | ★★ |
| **445 주요기업 상세 가독성**(implication split) | 9b | 소 | 0 | ★★ |
| **446 주요기업 주간 아카이빙**(competitor_weekly 패턴) | 9a | 중 | 0 | ★ |
| **447 경쟁사뉴스 통합**(최소안: 카드상세 뉴스섹션) | 9c | 중 | 0 | ★ |
| 수집 소스별 카운터 분리(진단 가시성) | 4b | 소 | 0 | 보조 |
| AI 리포트 MCP/Claude 고도화 | 7 | 중 | A/C=API키 | 결정필요 |
| 관계지도 재링킹 백필(선택) | 6 | 중 | 0 | 선택 |

- **즉시(코드 아님) 확인**: [4] `keyword_groups.search_seeds`·네이버 키·GDELT_ENABLED / job_runs cron:crawl details.
- **결정 필요**: [7] 유료 API(Claude/GPT) 도입 vs MCP 반자동.
- 대부분 SQL 0(코드-only) → Sonnet 병렬 처리 가능. TTS 실패 계측·AI리포트 API만 별도.
