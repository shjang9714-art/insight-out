# 수희 대기 작업 통합 (2026-07-24)

> David 전달용. 각 항목은 독립 실행 가능. 우선순위 순.
> 실행 후 David/플래너에게 알려주면 해당 기능이 활성화됨.

---

## 🔴 1. AI 리포트 RLS (사용자에게 리포트 안 보이는 버그)
- 파일: `docs/sql-handoff/ai_reports-발행분-공개-RLS-2026-07-23.sql`
- 내용: 발행 리포트를 일반 사용자도 읽도록 SELECT 정책 2개 추가(발행분만·PII 없음).
- 효과: **다른 사용자에게 AI 리포트가 보이게 됨**(현재 David/관리자에게만 보임). 즉시 체감. 배포 불필요.

## 🔴 2. 운영 이슈 테이블 (일일 브리핑 이슈 기능 활성화)
- 파일: `docs/sql-handoff/429-ops_issues-table-2026-07-24.sql`
- 내용: `ops_issues` 테이블 + RLS(관리자).
- 효과: 배포된 일일 브리핑(429)의 **운영 이슈 fingerprint 탐지·이슈화**가 실제로 동작. 없으면 탐지기 no-op(브리핑은 raw 집계로 폴백, 안 깨짐).

## 🟡 3. C 보강 워커 (파이프라인 flip 효과 가속)
- 파일: `docs/sql-handoff/Phase1C-pg_cron-enrich-drain-2026-07-24.sql`
- 내용: pg_cron+pg_net 으로 `/api/cron/body-backfill` 10분마다 호출(`net.http_get` + Bearer). CRON_SECRET 은 Vault 에.
- 효과: 425 flip 으로 생긴 pending 이 **10분마다 게시 승격**(현재는 일1회 백필로 서서히). 소스 다양성↑ 빨라짐.
- 선행: `CRON_SECRET` 값(Vercel 환경변수와 동일)이 필요.

## 🟢 4. Supabase 세션 수명 연장 (로그인 빈도↓)
- 위치: Supabase 대시보드 → Authentication → Sessions.
- 내용: inactivity timeout 해제/연장·time-box 연장(access 짧게 유지, refresh 길게).
- 효과: 사용자가 자주 재로그인 안 해도 됨. 코드 변경 없음.

## 🟢 5. Phase1-A 스키마 (파이프라인 보완재, 여유 시)
- 파일: `docs/sql-handoff/Phase1A-discovered_at-crawl_interval-2026-07-24.sql`
- 내용: `contents.discovered_at` 추가 + `sources.crawl_interval_minutes` NULL 보정.
- 효과: 발견 시각 추적 + 소스 주기 데이터 보정(코드는 이미 기본값으로 동작 중이라 급하지 않음).
- 주의: 소스 활성 컬럼명(`is_active`/`disabled`) 실제 스키마 확인 후 (2)번 조건 맞추기.

## 🟡 6. GCP 서비스계정 키 (GDELT BigQuery 과거 백필 — 432용)
- 목적: 과거 6개월(2026-01~) 한국 기사 URL 을 GDELT BigQuery(`gdelt-bq.gdeltv2.gkg_partitioned`)에서 소급 수집.
- 할 일: **GCP 프로젝트 → BigQuery API 사용 설정 → 서비스계정 생성 → JSON 키 발급** → Vercel 환경변수(`GCP_SA_KEY` 또는 `GOOGLE_APPLICATION_CREDENTIALS_JSON`).
- 비용: **BigQuery 무료 티어(1TB/월) 내** — 월 단위 파티션·컬럼 프루닝·한국/키워드 필터로 스캔량 최소. (설계: `GDELT-통합수집-설계-2026-07-25.md`)
- 효과: 키 확보 시 지시서 432 로 6개월 과거 소급(SQL 불필요, dedup 멱등).

---

## 요약
- **즉시 체감(SQL 실행만)**: 1(리포트 노출)·2(운영 이슈)
- **가속(SQL+시크릿)**: 3(보강 워커)
- **설정(대시보드)**: 4(세션)
- **보완재(여유 시)**: 5(스키마)

각 파일은 그대로 실행 가능하도록 주석·검증쿼리 포함. 소스 컬럼명 등 확인 필요분은 파일 내 주석에 표기됨.
