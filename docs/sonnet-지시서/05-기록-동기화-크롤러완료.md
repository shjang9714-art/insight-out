# Sonnet 지시서 05 — 기록 동기화 (#4 크롤러 완료)

> 작성: Opus(Cowork) · 대상: Claude Code(Sonnet, Notion 커넥터 보유) · 유형: **기록 동기화**(AGENTS §20.8 — 기록 담당 Sonnet)
> 목적: 2026-06-02 #4 자동 크롤러 프로덕션 가동·검증 완료를 **Notion 3곳 + git `docs/작업계획서.md`** 에 반영.
> 원칙: 각 Notion DB 는 먼저 fetch 해 속성명을 확인한 뒤 갱신(아래 id·속성명은 2026-06-01 기준).

## 0. 배경 (무엇이 끝났나)
오늘 PR #16~#20(최종 main `5db095f`)로 #4 뉴스 크롤러를 프로덕션에 가동·검증 완료:
- `/api/cron/crawl` (Vercel Cron `0 20 * * *` = 05:00 KST), `?days=N`(최대 30) 소급 옵션, news_site RSS 어댑터.
- 적재 방식 insert+23505 멱등, URL 선확인 dedup. days=30 소급으로 **135건 적재·중복제거 실동작 확인**(ZDNet 완전 멱등).
- 배포 검증용 **`/api/version`** 도입(VERCEL_GIT_COMMIT_SHA 반환) → David 가 Vercel 대시보드 없이 배포 확인. 흐름·검증법은 git `docs/배포-검증-프로세스.md`.
- 잔여(후속): 불량/프리징 seed 피드(전자신문 Section901, 4월 프리징) 교체 시 재호출 dedup 누수 해소.

커밋 링크 기준: `https://github.com/shjang9714-art/insight-out/commit/5db095f`

## 1. Notion ① 개발 일지 DB — 항목 2건 추가
- DB(데이터소스) id: `4c3f5301-363e-4ec3-844f-5c098a790110` (페이지 "📓 개발 일지")
- 속성: `제목`(title) · `유형`(select: 커밋/PR/배포/검증/기타) · `작업자`(select: 수희/David) · `날짜`(date) · `브랜치`(text) · `링크`(url) · `내용`(text)

**항목 A**
- 제목: `#4 뉴스 크롤러 프로덕션 가동·검증 완료`
- 유형: `배포` · 작업자: `David` · 날짜: `2026-06-02` · 브랜치: `main`
- 링크: `https://github.com/shjang9714-art/insight-out/commit/5db095f`
- 내용: `/api/cron/crawl + Vercel Cron(05:00 KST) + ?days 소급(최대 30) + news_site RSS. insert+23505 멱등·URL 선확인 dedup. days=30 소급으로 135건 적재·중복제거 실동작 확인(ZDNet 완전 멱등). PR #16~#20.`

**항목 B**
- 제목: `배포 검증용 /api/version 도입`
- 유형: `커밋` · 작업자: `David` · 날짜: `2026-06-02` · 브랜치: `main`
- 링크: `https://github.com/shjang9714-art/insight-out/commit/5db095f`
- 내용: `VERCEL_GIT_COMMIT_SHA 반환 공개 엔드포인트. David가 Vercel 대시보드 없이 배포 완료 확인(shortCommit vs origin/main). proxy 가드 예외 추가. 문서 docs/배포-검증-프로세스.md.`

## 2. Notion ② 작업 DB — 해당 항목 갱신
- DB(데이터소스) id: `14c892b8-1475-4637-b80a-3446d307c06d` (페이지 "작업 DB")
- 대상 페이지: `36c2f2c9-f358-8172-9da0-c5769adb49ca` (`뉴스 & 미디어 RSS/뉴스 API 크롤링 연동`)
- 속성 갱신: `상태` → `Done` (이미 Done이면 유지), `메모` → `완료: 2026-06-02 프로덕션 가동·검증. /api/cron/crawl + days 소급 + /api/version 배포검증. 잔여: 불량 seed 피드 정리 후속.`

## 3. Notion 작업계획서 페이지 — 신호등 확인
- 페이지 id: `3702f2c9-f358-80bf-8bf5-fb7aac0a4d22` ("04. 작업계획서")
- "용주 트랙 (자동 크롤링)" 표의 **`뉴스 크롤링 구현 (RSS/API)`** 행 신호등이 **🟢** 인지 확인. (이미 🟢 면 변경 없음. 아니면 update_content 로 `<td>STATUS</td>\n<td>뉴스 크롤링 구현 (RSS/API)</td>` 의 STATUS 를 🟢 로.)

## 4. Notion ③ 이슈 & 공유 DB — 공유 글 1건 추가
- DB(데이터소스) id: `5e5f7e0b-c883-4589-b0fa-c1f993f4895c` (페이지 "💬 이슈 & 공유")
- 속성: `제목`(title) · `유형`(select: 이슈/결정사항/공유/질문/회의록) · `작성자`(select: 수희/David) · `상태`(select: Open/…) · `우선순위`(select: 높음/중간/낮음) · `작성일`(date)
- 생성:
  - 제목: `[개발공유] #4 자동 크롤러 프로덕션 가동 + 배포검증 체계 (6/2)`
  - 유형: `공유` · 작성자: `David` · 상태: `Open` · 우선순위: `중간` · 작성일: `2026-06-02`
  - 본문(요지):
    - #4 뉴스 크롤러 프로덕션 가동·검증 완료(매일 05:00 KST 자동 수집). days 소급 옵션으로 135건 적재·중복제거 확인.
    - 배포 검증 체계 도입: `/api/version` + 흐름(브랜치 push → Squash & merge → `/api/version` 으로 배포확인 → 테스트). 문서 `docs/배포-검증-프로세스.md`.
    - 백로그: 불량/프리징 seed 피드 교체(전자신문 Section901) → 재호출 dedup 누수 해소. 피드 목록 재정비 시 처리. 테스트로 쌓인 days=30 적재분은 필요 시 수희가 정리.

## 5. git `docs/작업계획서.md` 갱신
- 진행 현황 메모: "뉴스 크롤링 구현(#4) **프로덕션 가동·검증 완료(2026-06-02)**" 반영.
- Phase 2-A 용주 트랙 표의 `뉴스 크롤링 구현 (RSS/API)` 신호등 **🟢** 확인/유지.
- 백로그 한 줄 추가: `BL: 불량·프리징 seed 피드 교체(전자신문 Section901 4월 프리징) → 재호출 dedup 누수 해소. 피드 목록 재정비 시 처리.`
- (참고 링크) `docs/배포-검증-프로세스.md` 가 배포·검증 표준임을 메모에 언급.

## 6. 완료 보고 양식
```
## 완료 보고 — 지시서 05 기록 동기화(#4)
- Notion 개발일지: <추가한 항목 URL 2건>
- Notion 작업DB: <갱신 페이지 URL + 상태>
- Notion 작업계획서 신호등: <🟢 확인/변경 여부>
- Notion 이슈공유: <생성 글 URL>
- git 작업계획서.md: <변경 요약>
- 미해결·판단보류: <없으면 "없음">
```
