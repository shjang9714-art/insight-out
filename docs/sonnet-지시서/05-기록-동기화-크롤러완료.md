# Sonnet 지시서 05 — 기록·전체 동기화 (git 작업계획서 → Notion + 오늘 #4 기록)

> 작성: Opus(Cowork) · 대상: Claude Code(Sonnet, Notion 커넥터 보유) · 유형: **기록·동기화**(AGENTS §20.8 — 기록 담당 Sonnet)
> 목적: ① git `docs/작업계획서.md` 를 현재 상태로 갱신, ② 그 내용을 **기준(source)** 으로 Notion(작업계획서 페이지 신호등 + 작업 DB 상태)을 **모든 Phase·모든 담당(David/용주 + 수희)** 에 대해 일치시킴, ③ 오늘 #4 크롤러 완료 기록.
> 원칙: **git `docs/작업계획서.md` 의 신호등이 이번 동기화의 source.** Notion 을 git 에 맞춘다. 각 Notion DB·페이지는 먼저 fetch 해 현재값·속성명을 확인한 뒤 갱신. **모호하면 추측하지 말고 David 에게 질문.**

## 0. 작업 순서 (요약)
1. git `docs/작업계획서.md` 갱신(§1) → 2. Notion 작업계획서 페이지 신호등 일치(§2) → 3. Notion 작업 DB 상태 동기화(§3) → 4. 오늘 #4 개발일지·이슈공유 기록(§4).

## 1. git `docs/작업계획서.md` 갱신 (먼저)
파일을 읽고 아래 반영:
- **진행 현황 메모**: "뉴스 크롤링 구현(#4) **프로덕션 가동·검증 완료(2026-06-02)** — `/api/cron/crawl` + Vercel Cron(05:00 KST) + `?days` 소급. `contents` 163행 전부 distinct URL(중복 0) 검증." 추가.
- **Phase 2-A 용주 트랙** 표: `뉴스 크롤링 구현 (RSS/API)` 신호등 **🟢** 확인/설정.
- **백로그 한 줄**: `BL: 전자신문 Section901 피드 4월 프리징 → 콘텐츠 신선도 위해 교체(피드 목록 재정비 시). ※ dedup·중복제거 정상(163행 distinct 확인).`
- 배포·검증 표준이 `docs/배포-검증-프로세스.md` 임을 메모에 언급(`/api/version` 으로 배포 확인).
- 문서 맨 끝 `*마지막 sync: …*` → `2026-06-02` 로 갱신.

## 2. Notion 작업계획서 페이지 신호등 → git 과 일치
- 페이지 id: `3702f2c9-f358-80bf-8bf5-fb7aac0a4d22` ("04. 작업계획서")
- **갱신된 git `docs/작업계획서.md` 의 각 작업 행 신호등(🟢🟡🔵⚪🔴)을 그대로** Notion 표의 동일 작업 행에 반영. (모든 Phase·수희 트랙 포함)
- 신호등 셀 교체는 `update_content` 로 `<td>STATUS</td>\n<td>작업명</td>` 단위(작업명이 고유하므로 안전). 이미 일치하는 행은 건드리지 않음.
- 작업명이 git ↔ Notion 간 미세하게 다르면(띄어쓰기 등) Notion 실제 텍스트에 맞춰 매칭.

## 3. Notion 작업 DB 상태 동기화 → git 신호등 기준 (David + 수희 전부)
- DB(데이터소스) id: `14c892b8-1475-4637-b80a-3446d307c06d` ("작업 DB"). 속성: `작업명`(title)·`상태`(select)·`Phase`·`담당자`·`메모` 등.
- 먼저 작업 DB 전체를 조회. 각 작업 페이지의 `상태` 를 git `작업계획서.md` 의 해당 작업 신호등에 맞춰 갱신.
- **신호등 → 상태 매핑:**

  | git 신호등 | Notion 상태 |
  |---|---|
  | 🟢 완료 | `Done` |
  | 🟡 진행 중 | `In Progress` |
  | 🔵 검토 중(PR open) | `In Progress` |
  | ⚪ 대기 | `Todo` |
  | 🔴 블록 | `Blocked` |

- git 작업이 작업 DB 에 **없으면 임의 생성하지 말고** 목록으로 David 에게 보고(매칭 안 된 작업).
- 확실히 끝난 핵심 항목(참고, 최종 판단은 git 기준): `뉴스 & 미디어 RSS/뉴스 API 크롤링 연동`(id `36c2f2c9-f358-8172-9da0-c5769adb49ca`) → `상태: Done`, `메모: 완료 2026-06-02 프로덕션 가동·검증(/api/cron/crawl + days 소급 + /api/version). contents 163행 distinct 확인. 잔여: 피드 신선도 교체 후속.`

## 4. 오늘 #4 기록 — 개발일지 2건 + 이슈공유 1건
### 4.1 개발 일지 DB (id `4c3f5301-363e-4ec3-844f-5c098a790110`)
속성: `제목`(title)·`유형`(select: 커밋/PR/배포/검증/기타)·`작업자`(select: 수희/David)·`날짜`(date)·`브랜치`(text)·`링크`(url)·`내용`(text). 커밋 링크 기준 `https://github.com/shjang9714-art/insight-out/commit/5db095f`.

- **A** — 제목 `#4 뉴스 크롤러 프로덕션 가동·검증 완료` / 유형 `배포` / 작업자 `David` / 날짜 `2026-06-02` / 브랜치 `main` / 링크 위 커밋 / 내용 `/api/cron/crawl + Vercel Cron(05:00 KST) + ?days 소급(최대30) + news_site RSS. insert+23505 멱등·URL 선확인 dedup. days=30 소급 135건 적재, contents 163행 전부 distinct(중복0) 검증. PR #16~#20.`
- **B** — 제목 `배포 검증용 /api/version 도입` / 유형 `커밋` / 작업자 `David` / 날짜 `2026-06-02` / 브랜치 `main` / 링크 위 커밋 / 내용 `VERCEL_GIT_COMMIT_SHA 반환 공개 엔드포인트 → Vercel 대시보드 없이 배포 확인. proxy 가드 예외. 문서 docs/배포-검증-프로세스.md.`

### 4.2 이슈 & 공유 DB (id `5e5f7e0b-c883-4589-b0fa-c1f993f4895c`)
속성: `제목`(title)·`유형`(select: 이슈/결정사항/공유/질문/회의록)·`작성자`(select: 수희/David)·`상태`(select: Open/…)·`우선순위`(select)·`작성일`(date).
- 제목 `[개발공유] #4 자동 크롤러 프로덕션 가동 + 배포검증 체계 (6/2)` / 유형 `공유` / 작성자 `David` / 상태 `Open` / 우선순위 `중간` / 작성일 `2026-06-02`
- 본문 요지:
  - #4 뉴스 크롤러 프로덕션 가동·검증 완료(매일 05:00 KST 자동 수집). days 소급으로 135건 적재.
  - 데이터 무결성: `contents` 163행 전부 distinct URL(중복 0) — 중복제거 정상.
  - 배포 검증 체계 도입: `/api/version` + 흐름(브랜치 push → Squash & merge → `/api/version` 으로 배포확인 → 테스트). 문서 `docs/배포-검증-프로세스.md`.
  - 백로그: 전자신문 Section901 피드 4월 프리징 → 콘텐츠 신선도 위해 교체(피드 재정비 시).

## 5. 주의
- git `작업계획서.md` 와 Notion 이 충돌하면 **이번엔 git 을 기준**으로 Notion 을 맞춘다(David 지시).
- 어느 작업의 실제 상태가 git·Notion 양쪽에서 모호하면 임의 판단 말고 David 에게 질문.
- 코드/스키마 변경 없음(순수 문서·기록 작업).

## 6. 완료 보고 양식
```
## 완료 보고 — 지시서 05 기록·전체 동기화
- git 작업계획서.md: <변경 요약>
- Notion 작업계획서 신호등: <맞춘 행 수 / 변경 목록>
- Notion 작업 DB: <상태 갱신한 작업 목록 + 매칭 안 된 작업>
- Notion 개발일지: <추가 URL 2건>
- Notion 이슈공유: <생성 URL>
- David 확인 필요(모호 항목): <있으면 목록, 없으면 "없음">
```
