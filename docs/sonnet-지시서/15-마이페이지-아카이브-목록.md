# Sonnet 지시서 15 — 마이페이지 아카이브 목록 (#26)

> 작성: Opus(Cowork) · 대상: Claude Code(Sonnet) · 유형: 기존 페이지 기능 추가
> 목적: 마이페이지에 "내 아카이브" 섹션 추가 — 아카이브 조회·항목 확인·삭제·이메일 발송(#28 연동)
> ⚠️ 작업 전 `AGENTS.md` + `src/app/dashboard/mypage/page.tsx` (현재 구조 전체) + `src/lib/types.ts` (`Archive`, `ArchiveItem`) 를 먼저 읽을 것.

## 0. 목표 (한 줄)
마이페이지 하단에 "내 아카이브" 섹션 추가 — 생성한 아카이브 목록과 담긴 콘텐츠 확인, 삭제, 이메일 발송 버튼.

## 1. 데이터 (이미 존재 — SQL 작업 없음)

- `archives`: `id, user_id, name, description, created_at, updated_at`
- `archive_items`: `archive_id, content_id, youtube_video_id, note, "order", added_at`
  - `content_id` 는 `contents` 테이블 FK (`id, title, category, original_url, summary_ko`)
- RLS: 본인 데이터만 접근 가능 (브라우저 client 사용)

## 2. 할 일

### 2.1 `src/app/dashboard/mypage/page.tsx` 에 아카이브 섹션 추가

페이지는 이미 `'use client'`. 기존 뉴스레터 섹션 **아래**에 새 섹션 추가.

**State 추가**

```typescript
// 아카이브
interface ArchiveWithItems {
  id: string
  name: string
  description: string | null
  created_at: string
  items: {
    content_id: string | null
    youtube_video_id: string | null
    added_at: string
    contents: { id: string; title: string; category: string; original_url: string | null } | null
  }[]
}

const [archives, setArchives] = useState<ArchiveWithItems[]>([])
const [archivesLoading, setArchivesLoading] = useState(true)
const [expandedArchiveId, setExpandedArchiveId] = useState<string | null>(null)
const [archiveError, setArchiveError] = useState<string | null>(null)
```

**데이터 로드** — 기존 `load()` 함수의 `Promise.all` 에 추가

```typescript
const { data: archivesData } = await supabase
  .from('archives')
  .select(`
    id, name, description, created_at,
    archive_items(
      content_id, youtube_video_id, added_at,
      contents(id, title, category, original_url)
    )
  `)
  .order('created_at', { ascending: false })

if (archivesData) setArchives(archivesData as ArchiveWithItems[])
setArchivesLoading(false)
```

**아카이브 삭제 (`handleDeleteArchive`)**

```typescript
async function handleDeleteArchive(archiveId: string) {
  if (!window.confirm('아카이브를 삭제하면 담긴 항목도 모두 사라집니다. 계속할까요?')) return
  setArchiveError(null)
  const { error } = await supabase.from('archives').delete().eq('id', archiveId)
  if (error) {
    setArchiveError('삭제에 실패했습니다. 잠시 후 다시 시도해주세요.')
  } else {
    setArchives(prev => prev.filter(a => a.id !== archiveId))
    if (expandedArchiveId === archiveId) setExpandedArchiveId(null)
  }
}
```

**아카이브 아이템 제거 (`handleRemoveItem`)**

```typescript
async function handleRemoveItem(archiveId: string, contentId: string | null, youtubeId: string | null) {
  setArchiveError(null)
  let query = supabase.from('archive_items').delete().eq('archive_id', archiveId)
  if (contentId) query = query.eq('content_id', contentId)
  else if (youtubeId) query = query.eq('youtube_video_id', youtubeId)

  const { error } = await query
  if (error) {
    setArchiveError('항목 제거에 실패했습니다.')
  } else {
    setArchives(prev =>
      prev.map(a =>
        a.id === archiveId
          ? {
              ...a,
              items: a.items.filter(item =>
                contentId ? item.content_id !== contentId : item.youtube_video_id !== youtubeId
              ),
            }
          : a
      )
    )
  }
}
```

### 2.2 UI 구조 (아카이브 섹션)

```tsx
<section className="rounded-2xl border border-gray-100 bg-white p-6 shadow-sm">
  <div className="mb-5">
    <h2 className="text-base font-semibold text-gray-900">내 아카이브</h2>
    <p className="mt-1 text-xs text-gray-500">
      담아둔 콘텐츠 모음입니다. 아카이브 단위로 이메일로 받아볼 수 있습니다.
    </p>
  </div>

  {archivesLoading ? (
    <p className="text-sm text-gray-400 text-center py-4">불러오는 중...</p>
  ) : archives.length === 0 ? (
    {/* 빈 상태 */}
    <div className="rounded-lg border border-dashed p-8 text-center text-muted-foreground">
      아직 담아둔 아카이브가 없습니다. 콘텐츠 상세 페이지에서 "아카이빙 담기"를 눌러 보세요.
    </div>
  ) : (
    <div className="flex flex-col gap-3">
      {archives.map(archive => (
        <div key={archive.id} className="rounded-xl border border-gray-100 overflow-hidden">
          {/* 아카이브 헤더 */}
          <div className="flex items-center justify-between px-4 py-3 bg-gray-50">
            <button
              type="button"
              onClick={() => setExpandedArchiveId(
                expandedArchiveId === archive.id ? null : archive.id
              )}
              className="flex items-center gap-2 text-sm font-medium text-gray-800 hover:text-brand-600"
            >
              {/* lucide-react ChevronDown / ChevronRight 아이콘 */}
              {expandedArchiveId === archive.id ? <ChevronDown ... /> : <ChevronRight ... />}
              {archive.name}
              <span className="text-xs font-normal text-gray-400">
                {archive.items.length}건
              </span>
            </button>
            <div className="flex items-center gap-1.5">
              {/* 이메일로 받기 버튼 — #28 구현 전 임시 disabled */}
              <Button
                type="button"
                variant="outline"
                size="sm"
                disabled
                title="이메일 발송 기능 준비 중"
                className="h-7 text-xs"
              >
                <Mail className="mr-1 h-3.5 w-3.5" />
                이메일로 받기
              </Button>
              {/* 삭제 버튼 */}
              <button
                type="button"
                onClick={() => handleDeleteArchive(archive.id)}
                className="rounded-lg p-1.5 text-gray-400 hover:bg-red-50 hover:text-red-500 transition-colors"
                title="아카이브 삭제"
              >
                <Trash2 className="h-4 w-4" />
              </button>
            </div>
          </div>

          {/* 아카이브 항목 목록 (expandedArchiveId 일치 시) */}
          {expandedArchiveId === archive.id && (
            <div className="divide-y divide-gray-50">
              {archive.items.length === 0 ? (
                <p className="px-4 py-3 text-xs text-gray-400">담긴 콘텐츠가 없습니다.</p>
              ) : (
                archive.items.map(item => (
                  <div key={item.content_id ?? item.youtube_video_id} className="flex items-start justify-between px-4 py-3 gap-2">
                    <div className="flex-1 min-w-0">
                      {item.contents ? (
                        <a
                          href={`/dashboard/contents/${item.contents.id}`}
                          className="text-sm font-medium text-gray-800 hover:text-brand-600 line-clamp-1"
                        >
                          {item.contents.title}
                        </a>
                      ) : (
                        <span className="text-sm text-gray-400">(삭제된 콘텐츠)</span>
                      )}
                      <p className="mt-0.5 text-xs text-gray-400">
                        {item.contents?.category} · {new Date(item.added_at).toLocaleDateString('ko-KR')}
                      </p>
                    </div>
                    <button
                      type="button"
                      onClick={() => handleRemoveItem(archive.id, item.content_id, item.youtube_video_id)}
                      className="shrink-0 rounded p-1 text-gray-300 hover:text-red-400 transition-colors"
                      title="목록에서 제거"
                    >
                      <X className="h-3.5 w-3.5" />
                    </button>
                  </div>
                ))
              )}
            </div>
          )}
        </div>
      ))}
    </div>
  )}

  {archiveError && (
    <p className="mt-3 text-xs text-red-500">{archiveError}</p>
  )}
</section>
```

**추가 import**
```typescript
import { ChevronDown, ChevronRight, Mail, Trash2, X } from 'lucide-react'
```

> **주의**: "이메일로 받기" 버튼은 **지시서 16(#28) 구현 전까지 `disabled` 상태**로 남긴다. 버튼만 UI에 노출해 두고 클릭 핸들러는 빈 상태 또는 `TODO` 주석으로 처리.

## 3. 완료 조건
- [ ] 마이페이지 하단 "내 아카이브" 섹션 표시
- [ ] 아카이브 목록·항목 수 표시, 아코디언 expand/collapse
- [ ] 항목 제목 클릭 시 `/dashboard/contents/{id}` 이동
- [ ] 아카이브 삭제 (확인 후 삭제, 목록 갱신)
- [ ] 항목 개별 제거 (X 버튼, 목록 갱신)
- [ ] "이메일로 받기" 버튼 — `disabled` 표시 (후속 지시서 16에서 활성화)
- [ ] 빈 상태, 로딩 상태, 에러 메시지 한국어
- [ ] `npx tsc --noEmit` · `npm run build` · `npm run lint` 통과

## 4. 검증 명령
```bash
npx tsc --noEmit && npm run build && npm run lint
```
→ 로컬에서 마이페이지 진입 → 아카이브 목록 확인 → expand → 항목 제거 → 아카이브 삭제 동작 확인.

## 5. 완료 보고 양식
```
## 완료 보고 — 지시서 15 마이페이지 아카이브 목록
- 변경 파일: <경로 목록>
- 한 일: <3~5줄>
- 완료 조건 충족: [x] 항목별
- 검증 결과: tsc · build · lint
- 미해결·판단보류: <없으면 "없음">
```
