# 지시서 127 — LLM 모델 현행화 (Groq 폐기모델 → openai/gpt-oss-120b)

> 작성: Opus(Cowork) · 대상: 구현 에이전트(Claude Code) · 검증: Opus · 커밋: 구현 에이전트
> ⚠️ 작업 전: `src/lib/llm/providers/groq.ts`(defaultModel) · `src/lib/llm/index.ts`(라우팅→고정폴백) · `docs/sql-handoff/55-llm-routing.sql`(routing/catalog 시드) · `/api/admin/llm-test`(검증). `npm install` 먼저.
> **코드 1파일 + SQL 핸드오프 1건.** (`/admin/llm`은 라우팅 표시 전용이라 model_id 변경은 SQL 필요.)

---

## 배경 (운영 진단 2026-06-21)

`/api/admin/llm-test` 결과 작동 프로바이더 0개(`test.ok=false`). 두 축:
1. **키**: Groq 미인식·OpenRouter 호출 실패 → 별도 Vercel 작업(`docs/sql-handoff/LLM키-점검-Vercel-2026-06-21.md`, 수희).
2. **모델 현행성(본 지시서)**: Groq classify 라우팅·기본값이 **`llama-4-scout-17b-16e-instruct`** = ① 접두사 누락(정식 `meta-llama/...`) ② **2026-06-17 Groq 폐기**. → 키가 살아도 Groq 경로가 실패. 현행 권장 후속 모델 **`openai/gpt-oss-120b`**(Groq 프로덕션, 무료티어 가용)로 교체.
   - 참고: `llama-3.3-70b-versatile`도 2026-06-17 폐기 예고 → 쓰지 말 것.

## 설계 결정 (Opus)

1. Groq 모델 → **`openai/gpt-oss-120b`** 로 통일(코드 기본값 + DB 라우팅 + 카탈로그).
2. 라우팅 model_id 는 DB(`llm_task_routing`)가 실효값 → **SQL UPDATE**(멱등). 코드 기본값은 **고정 폴백** 경로에서만 쓰이므로 같이 교체.
3. OpenRouter(`meta-llama/llama-3.3-70b-instruct:free`)는 현행 유효 → 유지. Gemini/Cerebras 라우팅 model_id 도 현행(`gemini-2.5-flash`/`gpt-oss-120b`)이라 유지(미설정이라 영향 적음).
4. 55 시드 파일도 기록 일관을 위해 동일 수정(이미 적용된 DB엔 영향 없음, 신규 환경용).

## 작업

### A. 코드 — `src/lib/llm/providers/groq.ts`
- `defaultModel: 'llama-4-scout-17b-16e-instruct'` → `defaultModel: 'openai/gpt-oss-120b'`.
- (그대로) `modelEnv: 'GROQ_MODEL'` 유지(필요 시 env 오버라이드).

### B. SQL 핸드오프 — `docs/sql-handoff/127-groq-model-현행화.sql` (신규)
```sql
-- 127: Groq 폐기모델(llama-4-scout) → 현행 openai/gpt-oss-120b. 멱등.
-- 라우팅 실효값 교체
update public.llm_task_routing
set model_id = 'openai/gpt-oss-120b'
where provider = 'groq'
  and model_id = 'llama-4-scout-17b-16e-instruct';

-- 카탈로그: 폐기모델 비활성 + 현행모델 보장
update public.llm_models set is_active = false
where provider = 'groq' and model_id = 'llama-4-scout-17b-16e-instruct';

insert into public.llm_models (provider, model_id, label, strengths, context_tokens)
values ('groq', 'openai/gpt-oss-120b', 'GPT-OSS 120B (Groq)', '고속·범용·추론', 131072)
on conflict (provider, model_id) do update set is_active = true;

-- 확인
select task_type, priority, provider, model_id, is_active
from public.llm_task_routing where task_type = 'classify' order by priority;
```
- (주의) `llm_models` insert 컬럼은 55 시드의 실제 컬럼에 맞출 것(strengths/context_tokens 명칭·NOT NULL 확인 후 조정).

### C. 시드 기록 동기화 — `docs/sql-handoff/55-llm-routing.sql`
- classify 라우팅의 `('classify', 2, 'groq', 'llama-4-scout-17b-16e-instruct')` → `'openai/gpt-oss-120b'`. `llm_models` 시드의 groq 행도 동일 교체(기록용, 이미 적용 DB엔 무영향).

## 검증 (구현 에이전트)
- `npx tsc --noEmit` 0 / `npx eslint src/lib/llm/providers/groq.ts` 0.
- SQL 은 핸드오프(수희 적용). 코드 커밋·푸시.
- (배포+키+SQL 후, 수동) `/api/admin/llm-test` → `test.ok=true`, `responded_provider=groq`(또는 openrouter) → `/admin/insights` 논조 분석 `analyzed>0`.

## 운영 순서 (의존성)
1. (수희) Vercel 키 등록/교체 + 재배포 — `LLM키-점검-Vercel-2026-06-21.md`.
2. (수희) `127-groq-model-현행화.sql` 적용.
3. 코드(groq.ts 기본값) 커밋·배포.
4. `/api/admin/llm-test` 로 `test.ok=true` 확인 → 논조 재실행.

## 커밋
- `fix: 지시서 127 Groq 모델 현행화(llama-4-scout 폐기 → openai/gpt-oss-120b)`
