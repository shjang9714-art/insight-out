# 지시서 445 — 주요기업 상세(인사이트 카드) 가독성

> Opus · 2026-07-26 · 개선검토 [9b]. SQL 0·의존성 0. 브랜치 `agent/445-insight-detail-readability`.
> 대상: `src/app/dashboard/insights/[id]/page.tsx` (렌더만). 데이터·프롬프트 불변(렌더측 정규화).

## 현행 문제
- implication을 **단일 `<p>{implication}</p>`**(~L169)로 렌더 → 스크린샷처럼 ①②③가 한 문단에 뭉침.
- 순서: 시사점 → 근거자료 → 키워드. David 요청은 **제목·요약·근거 설명 후 시사점** 순.

## 수정 3
1. **implication 문단 분리** — `①②③`(또는 `¹²³`), 개행, `1. 2. 3.` 마커로 split해 **각 항목을 별도 문단/리스트**로 렌더(항목 간 여백). 마커·개행 없으면 문장(`. `) 기준 완만 분리 or 단일 문단 폴백(과분할 금지). 빈 항목 제거.
2. **순서 조정** — 블록 순서를 **제목/부제(headline) → 근거 자료 → 시사점 → 관련 키워드**로. (시사점 블록을 근거 자료 뒤로 이동.)
3. **가독성** — 시사점 항목 리스트에 `leading-relaxed`·항목 간격, 근거 인용/제목 간 여백 유지. 과한 재설계 금지, 기존 토큰 재사용.

## 검증
- 마커 있는 카드: ①②③ 각각 줄 분리. 마커 없는 카드: 단일 문단(깨짐 없음). 순서 근거→시사점 반영.
- tsc/lint/build 통과. 변경 page.tsx 1파일. SQL·데이터·프롬프트 불변.

## 후속(별건)
- implication 구조 **일관성**은 생성 프롬프트(`llm_prompts` company_insight) 구조화 출력 필요 → 재생성 동반이라 별도. 445는 렌더측 정규화로 기존 카드까지 즉시 개선.
