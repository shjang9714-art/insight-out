# 지시서 269 — 로그인 카드: 골드 언더바 제거 + 타이틀 한글화

> David 요청. 로그인 배치는 현재 좋음(268 배치조정은 보류/철회). 로그인 카드만 소소 2건 수정.

대상: `src/components/login/LoginCard.tsx` (단일). SQL·다른 파일 없음.

---

## 1. 변경
1. **골드 언더바 제거**: 카드 타이틀 밑 노란 그라디언트 막대(`.underbar` 역할의 `<span aria-hidden>` — `mt-3.5 h-[3px] w-[30px]` + gold linear-gradient/boxShadow)를 **삭제**한다(오류처럼 보임). 관련 인라인 style도 함께 제거.
2. **타이틀 한글화**: 카드 헤드의 `<h1>Insight Out</h1>` → `<h1>인사이트 아웃</h1>`. 폰트/크기/굵기(`text-[26px] font-extrabold …`)는 그대로.

> 히어로 헤드라인(`흩어진 인텔리전스를…`)·서브(`Intelligence in. Insight out.`)·페이지 metadata·앱 로고는 **변경하지 않는다**(이번 범위는 로그인 카드 타이틀만).

## 2. 회귀 가드
- 캡션(`사내 이메일로 로그인하세요`↔`메일로 받은…`) 토글, OTP 2단계, 관리자 Google, 에러 배너 등 **동작·나머지 스타일 불변**.
- 언더바 삭제 후 타이틀↔캡션 간 간격이 어색하면 캡션 상단 margin만 소폭 조정(선택, 폰트 불변).

## 3. 검증 (Sonnet)
- `npx tsc --noEmit` 0 / `npx eslint` 0 / `npm run build`.
- 로그인 카드: 타이틀 `인사이트 아웃`, 그 밑 노란 막대 없음, 나머지 동일.
- 커밋: `fix: 로그인 카드 언더바 제거 + 타이틀 한글화 (지시서 269)` — 배포까지.

SQL 없음. 이 지시서는 로그인 카드 타이틀 한글화 + 언더바 제거.
