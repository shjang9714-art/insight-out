# 지시서 158 — 채택④: 개인화 넛지 (미설정 유도)

> 작성: Opus(Cowork) · 2026-06-24 · 레인: 데이터+AI MI
> 근거: 채택 토론 P4 "개인화 미설정=일반 화면". 온보딩 스킵 가능 → 워치리스트/담당서비스 미설정 사용자는 147(관심기업)·렌즈·153 델타 개인화를 못 받음. 미설정 안내가 화면별로 흩어져 있음 → 홈에서 한 번 일관되게 유도.
> 협업 루프: 로컬(커밋X). David 위임 → 구현 → Opus 검증 → "커밋". **SQL 없음.**

---

## 0. 한 줄

개인화(담당 서비스·관심 기업)를 안 한 사용자는 일반 화면만 본다. 홈 상단에 **미설정 상태를 감지한 디스미스 가능한 넛지 배너**를 띄워 마이페이지 설정으로 유도한다. 이미 설정했거나·온보딩 신규(별도 흐름)이거나·닫았으면 안 보임. 기존 화면별 안내는 그대로 두고, 홈에 **단 하나의 명확한 유도점**을 추가.

## 1. 현행 진단 (검증된 코드 사실)

- 개인화 토대: `getFeedOnboardingStatus(supabase, userId)`('new'/'skipped'/'existing')·`getUserPrimaryServiceId`(담당 서비스)·`user_watchlist`(관심 기업) — `src/lib/preferences`·FeedSlot 에서 사용 중.
- 'new' → FeedSlot 이 온보딩 picker 렌더(별도). 'skipped'/'existing' 인데 서비스/워치리스트 미설정이면 **일반 화면 + 흩어진 안내만**(147 회사섹션·렌즈 빈상태·156 폴백 넛지).
- 홈(`src/app/dashboard/page.tsx`)은 서버. 상단 VisitDelta(153) 패턴(서버 판정+클라 자식) 재사용 가능.
- 통합 유도 배너 없음 → 이 슬라이스가 추가.

## 2. DB / SQL

**없음.** 기존 helper + `user_watchlist` head-count.

---

## 3. 구현

### 3-1. `src/components/dashboard/PersonalizationNudge.tsx` (신규, 서버)

미설정 판정 → 클라 배너 렌더. (판정은 서버, dismiss는 클라.)

```tsx
import { createClient } from '@/lib/supabase/server'
import { getFeedOnboardingStatus, getUserPrimaryServiceId } from '@/lib/preferences'
import PersonalizationNudgeBanner from './PersonalizationNudgeBanner'

export default async function PersonalizationNudge() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return null

  const status = await getFeedOnboardingStatus(supabase, user.id)
  if (status === 'new') return null // 온보딩 흐름이 따로 유도

  const [serviceId, watchRes] = await Promise.all([
    getUserPrimaryServiceId(supabase, user.id),
    supabase.from('user_watchlist').select('id', { count: 'exact', head: true }).eq('user_id', user.id),
  ])

  const noService   = !serviceId
  const noWatchlist = (watchRes.count ?? 0) === 0
  if (!noService && !noWatchlist) return null // 둘 다 설정됨 → 넛지 불필요

  return <PersonalizationNudgeBanner noService={noService} noWatchlist={noWatchlist} />
}
```

### 3-2. `src/components/dashboard/PersonalizationNudgeBanner.tsx` (신규, 클라·디스미스)

```tsx
'use client'
import { useState } from 'react'
import Link from 'next/link'
import { Sparkles, X } from 'lucide-react'

const DISMISS_KEY = 'io:personalize-nudge-dismissed'

export default function PersonalizationNudgeBanner({
  noService, noWatchlist,
}: { noService: boolean; noWatchlist: boolean }) {
  const [dismissed, setDismissed] = useState(() => {
    if (typeof window === 'undefined') return false
    try { return localStorage.getItem(DISMISS_KEY) === '1' } catch { return false }
  })
  if (dismissed) return null

  const missing =
    noService && noWatchlist ? '담당 서비스와 관심 기업'
    : noService ? '담당 서비스'
    : '관심 기업'

  const handleDismiss = () => {
    try { localStorage.setItem(DISMISS_KEY, '1') } catch { /* noop */ }
    setDismissed(true)
  }

  return (
    <div className="flex items-center gap-3 rounded-xl border border-brand-600/30 bg-brand-600/5 px-4 py-3">
      <Sparkles className="h-4 w-4 shrink-0 text-brand-600" />
      <p className="flex-1 text-sm text-foreground">
        {missing}을 설정하면 당신에게 맞는 이슈·기업·인사이트를 모아 보여드려요.{' '}
        <Link href="/dashboard/mypage" className="font-medium text-brand-600 hover:underline">
          설정하기 →
        </Link>
      </p>
      <button
        type="button"
        onClick={handleDismiss}
        aria-label="닫기"
        className="shrink-0 rounded p-1 text-muted-foreground hover:text-foreground transition-colors"
      >
        <X className="h-4 w-4" />
      </button>
    </div>
  )
}
```
- 색은 brand 토큰만(하드코딩 hex 금지).
- 마이페이지 경로는 실제 워치리스트/서비스 설정 경로로 확인(`/dashboard/mypage` 가정 — 153 넛지와 동일 경로 사용).

### 3-3. `src/app/dashboard/page.tsx` — 상단 렌더

VisitDelta 아래(또는 위)에:
```tsx
<Suspense fallback={null}>
  <PersonalizationNudge />
</Suspense>
```
(import 추가. VisitDelta·SuggestedQuestions 와 함께 상단 영역.)

---

## 4. 회귀 가드 / 비기능 요건

- 둘 다 설정·온보딩 신규·비로그인·닫음 → 미노출(과한 넛지 금지).
- 디스미스 영속(localStorage) — 한 번 닫으면 다시 안 뜸. 단 설정하면 자연 소멸(미설정 조건 해제).
- 기존 화면별 안내(147·렌즈·156) 무변경 — 홈 통합 유도점만 추가.
- 서버 판정 실패/null → null 반환(홈 안 깨짐).
- SQL·신규 의존성 0. 하드코딩 hex 0.

## 5. 검증 (Sonnet 자체)

1. `npx tsc --noEmit` 0
2. `npx eslint` 0
3. hex grep 0
4. 미설정 사용자=배너 표시 / 설정 사용자=미표시 / 닫기=localStorage 영속 확인
5. 마이페이지 설정 경로 실제값 확인

## 6. 후속 (범위 밖)

- 디스미스 영속 대신 N일 후 재노출(과소 넛지 방지).
- 배너에서 인라인 빠른 설정(모달) — 마이페이지 이동 없이.
- 설정 완료율을 어드민 건강 지표로(152 연계, P4 추적).

---

## 7. 라이브 검증 체크리스트 추가분 (§25 신설)

- [ ] 담당 서비스/관심 기업 미설정 사용자 홈 상단에 넛지 배너 + "설정하기" 링크
- [ ] 미설정 항목에 따라 문구 적응(담당 서비스 / 관심 기업 / 둘 다)
- [ ] 닫기(X) → 새로고침·재방문에도 안 뜸(localStorage 영속)
- [ ] 둘 다 설정한 사용자·온보딩 신규·비로그인 = 배너 미표시
- [ ] 마이페이지에서 설정 후 = 배너 자연 소멸
