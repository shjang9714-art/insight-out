# 지시서 456 — 소스 관리에 '검색 수집원(네이버·GDELT)' 읽기전용 상태 패널 (rev2)

선행: 448 병합(providers meta). **SQL·env·의존성 0.** 읽기전용. 브랜치 `agent/456-search-providers`.

> **rev2(07-29)**: `origin/main = d5bd3a0`(455 IA 재편 이후) 기준 재실사 — **지시서 유효**. 아래 ‘실사 확인’ 통과, 보완 2건 반영.

## 실사 확인 (d5bd3a0)
- `src/app/admin/sources/page.tsx` **경로 유지**, 현재 sync 함수(`AdminPageHeader` + `SourceManager`) → async 전환 가능. async server page 선례 다수(`crawl-logs`·`job-runs`·`contents`).
- `SourceManager`·`AdminPageHeader` 모두 `'use client'` → server page가 렌더해도 무관.
- `job_runs.meta` jsonb 존재, provider 카운트는 **`meta.providers` 하위**가 맞음.
- `keyword_groups.search_seeds text[]` + `is_active` 존재.

## 배경
네이버·GDELT는 RSS 소스가 아니라 검색 API(키워드 시드마다 호출)라 소스 목록에 없음. 소스 관리 상단에 상태 패널로 노출해 "어디서 관리하는지" 가시화.

## 변경
1. **신규 `src/components/admin/SearchProvidersPanel.tsx`**(프레젠테이션, props로 상태 받음):
   - 2행: **네이버**(키 설정됨/미설정 배지 · 최근 유입 N건), **GDELT**(활성/비활성 배지 · 최근 유입 N건).
   - 공통: 검색 시드 수, 마지막 크롤 시각, `keyword_phase_skipped`면 "키워드 검색 단계 미실행(시드 미설정)" 경고.
   - 0건·키 미설정·시드 0·phase skipped → amber. 안내문: 시드는 기준정보>키워드 그룹, 유입 상세는 로그 분석(`/admin/crawl-logs`).

2. **[보완①] `parseProviderCounts` 공용화** — 현재 `src/app/admin/crawl-logs/page.tsx`의 **로컬 함수**라 그대로는 재사용 불가.
   → `src/lib/admin/crawl-providers.ts`로 **로직 변경 없이 이동**(`ProviderCounts` 타입 포함), `crawl-logs/page.tsx`는 **import 교체만**. 복제 금지(진단 로직 이원화 방지).
   ⚠️ 검증 대상 키는 **5개 전부**(`keyword_google`·`keyword_naver`·`keyword_gdelt`·`company_google`·`keyword_phase_skipped`). 3개만 검사하면 `parse`가 항상 `null` → 패널이 영구 '데이터 없음'.

3. **`src/app/admin/sources/page.tsx`**(server, async로 변경): 상태 조회 후 `<SearchProvidersPanel .../>`를 `<SourceManager/>` 위에 렌더.
   - `keySet` = `!!(process.env.NAVER_CLIENT_ID ?? process.env.NAVER_SEARCH_CLIENT_ID) && !!(process.env.NAVER_CLIENT_SECRET ?? process.env.NAVER_SEARCH_CLIENT_SECRET)`
   - `gdeltEnabled` = `process.env.GDELT_ENABLED !== 'false'`
   - providers: `createAdminClient()` → 최신 `cron:crawl` job_runs(`status in (succeeded, failed)`, `order started_at desc limit 1`) → `meta` → `parseProviderCounts`. graceful(42P01·파싱 실패 시 패널 '데이터 없음').
   - seedCount: `admin.from('keyword_groups').select('search_seeds').eq('is_active', true)` → flat unique 길이. 실패 시 null.
   - 모든 조회 try/catch graceful(실패해도 `SourceManager` 정상 렌더).

## 완료조건
읽기전용(쓰기·SQL·env·의존성 0) · 조회 실패해도 소스 목록 정상 · **`/admin/crawl-logs` 기존 "소스별 유입" 표시 회귀 0**(보완① 이동 후 동작 동일) · tsc/lint/build.

## 재현검증
`git diff --stat origin/main...origin/agent/456-search-providers` → `SearchProvidersPanel` 신설 + `crawl-providers.ts` 추출 + `crawl-logs/page.tsx` import 교체 + `sources/page.tsx` async 상태조회. **crawl-logs는 import 외 로직 diff 0**인지 확인. graceful 확인.
