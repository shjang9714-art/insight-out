# 지시서 351-E — 키워드 분석: 버블 클러스터 맵 제거(카드만 유지)

> 대상: 구현 에이전트 · **신규 SQL 없음** · 351-D 부분 롤백
> ⚠️ 읽을 것: `src/components/analysis/AiInsightBoard.tsx`(keyword 뷰·버블/카드 토글) · `src/components/analysis/KeywordClusterMap.tsx`(351-D 신규 — 제거 대상) · `AGENTS.md`
> 배경(David): 버블맵 시각이 조악함 → **버블 제거, 카드 그리드만 유지.**

## 작업
1. **버블 클러스터 맵 제거** — keyword 뷰에서 `KeywordClusterMap` 렌더 삭제. **`KeywordClusterMap.tsx` 파일도 삭제**(dead code 방지). `react-force-graph-2d` import가 이 뷰 전용이었다면 함께 정리(관계지도 KnowledgeGraph의 사용과는 무관하니 그건 건드리지 말 것).
2. **뷰 토글(버블/카드) 제거** — 상단 "버블 · 카드" 토글 삭제. **카드 그리드가 기본이자 유일한 뷰**(351-A의 엔티티 시그널 카드 그리드로 환원).
3. **유지(삭제 금지)** — 351-D의 **오늘의 주요 변화 3카드**·**급상승 랭킹 Top 10 7일 스파크라인**·4지표 스트립·급상승 랭킹은 **그대로 둔다**. 이번엔 버블/토글만 걷어낸다.

## 회귀 / 주의
- 카드 클릭 → `keywords/[name]` 링크 유지.
- 토글 제거로 남는 빈 상태·레이아웃(상단 우측 여백) 정리.
- `KeywordClusterMap` 삭제 후 참조(import) 잔존 없게(빌드 에러 방지).
- 주요 변화·스파크라인 관련 서버 데이터 조립은 유지(버블 전용 데이터가 따로 있었다면 그것만 정리).
- 검증: `tsc`·ESLint·`check-prefetch`·`build`·육안(키워드 분석 = 카드 그리드만·주요변화 3카드·랭킹 스파크라인 정상·토글 없음). 완료보고 커밋 해시.

## 배포 게이트
⚠️ main 머지·배포하지 말 것. 브랜치 push + PR까지만 하고 브랜치명 회신 → Opus 검증(tsc·build·diff·지시서 대조) 통과 후 머지·배포.

## 쪼개기
단일 관심사 → **1커밋**.
