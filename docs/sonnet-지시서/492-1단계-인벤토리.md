# 지시서 492 — 1단계 인벤토리 산출물

> Sonnet(Claude Code) · 기준 `origin/main = 6a55bcb` · 브랜치 `agent/492-soft-delete`
> **코드 변경 0.** 인벤토리 + `get_recommended_feed` 현재 본문만 제출.

---

## 0. 총계 요약

`from('contents')` 호출은 `src/` 전체에 **224건**. 이 중 서비스롤(RLS 우회) 호출만 골라 분류했다.

| 분류 | 건수 |
|---|---|
| **A · 살아있는 것만** | 94 |
| **B · 휴지통 포함** | 2 |
| **C · 무관** | 37 |
| **합계** | **133** |

지시서가 제시한 추정치(109 = 어드민 77 + 비어드민 32)와 **다르다.** 억지로 맞추지 않고 실측대로 보고한다. 원인:

- **어드민 영역이 추정보다 훨씬 적다(약 34건).** 가장 큰 이유: `src/app/admin/page.tsx`의 `.from('contents')` **14곳 전부**가 `createAdminClient()`가 아니라 `createServerClient()`(RLS 적용 세션)로 실행된다. 파일 안에 `admin = createAdminClient()`도 있지만 그건 `llm_usage`·`job_runs` 등 다른 테이블에만 쓰인다. `AdminContentManager.tsx`(12곳)도 전부 브라우저 `createClient()`(RLS)다. 즉 어드민 화면 상당수가 서비스롤이 아니라 **관리자 본인 세션의 RLS**로 돈다 — `contents: admin 전체 조회` 정책 덕분이다. ⚠️ **이 정책은 2단계 SQL B에서 그대로 둘 예정**(어드민은 휴지통을 봐야 하므로 `deleted_at` 조건이 없다)이라, **이 RLS-어드민 경로들도 결국 서비스롤 경로와 똑같이 "삭제분이 안 걸러진다."** 아래 §3 참고 — 2단계 범위를 다시 논의할 필요가 있다.
- **비어드민 `lib`/cron 쪽이 추정보다 훨씬 많다(약 94건).** 백필 스크립트(`contents/*-backfill.ts`, `insight/*-backfill.ts` 등 15개 파일)가 파일당 "남은 건수 카운트 1곳 + 다음 배치 조회 1곳 + 결과 저장 update 1~2곳" 패턴을 반복해서 건수가 누적됐다.

---

## 1. 분류표 (파일:줄 · 분류 · 근거)

파일 경로 오름차순 · 줄 번호 오름차순.

| 파일:줄 | 분류 | 근거 |
|---|---|---|
| `src/app/admin/contents/[id]/page.tsx:90` | C | id 단건 조회, 상세페이지 |
| `src/app/admin/contents/[id]/page.tsx:92` | C | fallback id 단건 조회 |
| `src/app/admin/taxonomy/page.tsx:44` | A | 카테고리별 건수 집계 |
| `src/app/api/admin/briefings/[id]/highlights/route.ts:46` | A | 브리핑용 인사이트 생성 대상 조회 |
| `src/app/api/admin/canonical-backfill/route.ts:34` | A | canonical 미해소 대상 배치 조회 |
| `src/app/api/admin/canonical-backfill/route.ts:55` | C | canonical_url UPDATE만 |
| `src/app/api/admin/canonical-backfill/route.ts:70` | A | 잔여 건수 집계 |
| `src/app/api/admin/company-documents/review/[contentId]/route.ts:65` | C | status UPDATE만 |
| `src/app/api/admin/company-documents/review/[contentId]/route.ts:75` | C | status UPDATE만 |
| `src/app/api/admin/company-documents/review/[contentId]/route.ts:121` | C | published_at UPDATE만 |
| `src/app/api/admin/company-documents/upload/route.ts:75` | C | INSERT만 |
| `src/app/api/admin/company-documents/upload/route.ts:110` | C | 롤백용 DELETE만 |
| `src/app/api/admin/contents/[id]/extract/route.ts:31` | C | id 단건 조회 |
| `src/app/api/admin/contents/[id]/extract/route.ts:216` | C | 본문/요약 UPDATE만 |
| `src/app/api/admin/contents/[id]/file-url/route.ts:27` | C | id 단건 조회 |
| `src/app/api/admin/contents/[id]/pdf-cover/route.ts:29` | C | id 단건 조회 |
| `src/app/api/admin/contents/bulk-review/route.ts:70` | A | 일괄반려 대상 미리보기 건수 집계 |
| `src/app/api/admin/contents/bulk-review/route.ts:112` | C | 필터 매칭 UPDATE만 |
| `src/app/api/admin/contents/delete/route.ts:38` | C | id 배열 DELETE 실행문 자체 — 2단계에서 soft-delete update로 교체될 지점 |
| `src/app/api/admin/contents/purge/route.ts:19` | B | 영구삭제 대상 전수(소프트삭제분 포함) 미리보기 카운트 |
| `src/app/api/admin/contents/purge/route.ts:44` | B | purge 대상 id 목록 조회, 소프트삭제분도 포함돼야 함 |
| `src/app/api/admin/contents/purge/route.ts:58` | C | 영구 DELETE 실행문 자체 |
| `src/app/api/admin/issues/[id]/rematch/route.ts:40` | A | 이슈 재매칭 후보 검색(published) |
| `src/app/api/admin/keyword-suggestions/route.ts:61` | A | 키워드 제안용 대량 배치 조회 |
| `src/app/api/admin/knowledge-reports/[id]/route.ts:30` | C | id+category UPDATE(select는 갱신결과 반환용) |
| `src/app/api/admin/knowledge-reports/[id]/route.ts:56` | C | status UPDATE만 |
| `src/app/api/admin/knowledge-reports/route.ts:43` | C | INSERT만 |
| `src/app/api/admin/paste/route.ts:94` | C | INSERT만 |
| `src/app/api/admin/paste/route.ts:103` | C | INSERT 재시도(컬럼 미적용 graceful) |
| `src/app/api/admin/reports/import/route.ts:123` | A | 보고서 근거 연결용 제목검색(ilike) |
| `src/app/api/admin/translate-backfill/route.ts:30` | A | 번역 백필 대상 배치 조회 |
| `src/app/api/admin/translate-backfill/route.ts:43` | A | 대상 0건 시 잔여 건수 집계 |
| `src/app/api/admin/translate-backfill/route.ts:88` | C | 번역결과 UPDATE만 |
| `src/app/api/admin/translate-backfill/route.ts:107` | A | 잔여 건수 집계 |
| `src/app/api/contents/[id]/source/route.ts:75` | C | UPDATE만(원문URL 자가치유 캐시), 조회 없음 |
| `src/app/api/council/route.ts:55` | A | 외부 COUNCIL 검색 API, 목록형 조회 |
| `src/app/api/council/route.ts:92` | A | id 단건, admin 편집 아닌 외부 인용 조회 — 삭제글 노출 부적절 |
| `src/app/api/cron/link-health/route.ts:24` | A | 링크 유효성 점검 대상 조회, 살아있는 published만 대상 |
| `src/app/api/cron/link-health/route.ts:44` | C | UPDATE만(link_ok/checked_at 갱신), 조회 없음 |
| `src/lib/admin/analytics.ts:39` | A | 콘텐츠 분석 일별/카테고리별 집계, 삭제 행 포함 시 통계 왜곡 |
| `src/lib/admin/analytics.ts:44` | A | 소스별 집계, 동일 이유 |
| `src/lib/admin/analytics.ts:50` | A | 북마크 상위 랭킹 목록, 삭제된 글 노출되면 안 됨 |
| `src/lib/admin/relations.ts:159` | A | 엔티티 관계 근거 기사 목록 표시 |
| `src/lib/briefing/generate-briefing.ts:359` | A | 브리핑 생성 후보 조회, 삭제 글 포함되면 안 됨 |
| `src/lib/company-docs/discovery/types.ts:123` | A | original_url 중복 판정, 삭제행을 존재로 오판하면 재수집 막힘 |
| `src/lib/company-docs/discovery/types.ts:136` | C | insert 후 id 반환용 select |
| `src/lib/company-docs/discovery/types.ts:175` | C | 삽입 실패 롤백용 delete |
| `src/lib/company-docs/ingest.ts:111` | C | insert 후 id 반환용 select |
| `src/lib/company-docs/ingest.ts:137` | C | 실패 롤백용 delete |
| `src/lib/company-docs/ingest.ts:151` | C | 실패 롤백용 delete |
| `src/lib/competitor-weekly/generate.ts:135` | A | 주간 경쟁사 리포트용 콘텐츠 조회 |
| `src/lib/competitor-weekly/generate.ts:147` | A | 컬럼 미적용 폴백 재조회, 위와 동일 |
| `src/lib/contents/classify-signals.ts:97` | A | 신호분류 대기건수 카운트 |
| `src/lib/contents/classify-signals.ts:122` | A | 신호분류 대상 목록 조회 |
| `src/lib/contents/classify-signals.ts:166` | C | signals_classified_at 마킹 update |
| `src/lib/contents/cover-from-image.ts:161` | C | thumbnail_url update |
| `src/lib/contents/cover-from-pdf.ts:118` | C | thumbnail_url update |
| `src/lib/contents/enrich-body.ts:46` | C | 본문보강 성공 update |
| `src/lib/contents/enrich-body.ts:52` | C | 본문보강 성공 update(폴백) |
| `src/lib/contents/enrich-body.ts:70` | C | 재시도/영구마킹 update |
| `src/lib/contents/enrich-body.ts:81` | C | 영구마킹 update |
| `src/lib/contents/enrich-body.ts:256` | A | 선택 ID 강제 재추출 대상 목록(다건 조회) |
| `src/lib/contents/enrich-body.ts:290` | A | 본문보강 대기건수 카운트 |
| `src/lib/contents/enrich-body.ts:328` | A | 본문보강 대상 목록 조회 |
| `src/lib/contents/full-body.ts:64` | C | body_original/fetched_at update |
| `src/lib/contents/full-body.ts:73` | C | body_fetched_at update |
| `src/lib/contents/pdf-cover-backfill.ts:38` | A | PDF 커버 백필 대기건수 카운트 |
| `src/lib/contents/pdf-cover-backfill.ts:74` | C | thumbnail_fetched_at 마킹 update |
| `src/lib/contents/pdf-cover-backfill.ts:101` | A | PDF 커버 백필 대상 목록 조회 |
| `src/lib/contents/summarize-backfill.ts:54` | A | 요약 백필 대기건수 카운트 |
| `src/lib/contents/summarize-backfill.ts:81` | A | 요약 백필 대상 목록 조회 |
| `src/lib/contents/summarize-backfill.ts:143` | C | summary_ko/attempted_at update |
| `src/lib/contents/summarize-backfill.ts:154` | C | summary_attempted_at 마킹 update |
| `src/lib/contents/thumbnail-backfill.ts:43` | A | 썸네일 백필 대기건수 카운트 |
| `src/lib/contents/thumbnail-backfill.ts:81` | C | thumbnail_fetched_at 마킹 update |
| `src/lib/contents/thumbnail-backfill.ts:104` | A | 썸네일 백필 대상 목록 조회 |
| `src/lib/contents/youtube-transcript-backfill.ts:47` | A | 자막 백필 대기건수 카운트 |
| `src/lib/contents/youtube-transcript-backfill.ts:77` | C | video_id 없는 행 스킵 마킹 update |
| `src/lib/contents/youtube-transcript-backfill.ts:105` | A | 자막 백필 대상 목록 조회 |
| `src/lib/crawler/cluster-backfill.ts:68` | A | 재클러스터링 백필 대기건수 카운트 |
| `src/lib/crawler/cluster-backfill.ts:99` | A | 대표 재선정용 대표행 신호 조회, 삭제행이 대표로 뽑히면 안 됨 |
| `src/lib/crawler/cluster-backfill.ts:100` | A | 대표 재선정용 멤버행 신호 조회, 동일 이유 |
| `src/lib/crawler/cluster-backfill.ts:124` | C | cluster_checked_at 마킹 update |
| `src/lib/crawler/cluster-backfill.ts:147` | A | 재클러스터링 대상 목록 조회 |
| `src/lib/crawler/cluster.ts:59` | C | 대표 확정 cluster_id=null update |
| `src/lib/crawler/cluster.ts:65` | C | 멤버 cluster_id 재지정 update |
| `src/lib/crawler/dedup.ts:22` | A | URL 중복탐지, 삭제된 글을 존재로 오판하면 재수집 막힘 |
| `src/lib/crawler/dedup.ts:40` | A | 제목해시 중복탐지, 동일 이유 |
| `src/lib/crawler/dedup.ts:57` | A | 본문해시 중복탐지, 동일 이유 |
| `src/lib/crawler/dedup.ts:83` | A | near-dup 후보 조회, 삭제행 매칭 시 오탐 |
| `src/lib/crawler/dedup.ts:122` | A | near-dup(본문포함) 후보 조회, 동일 이유 |
| `src/lib/crawler/orchestrator.ts:150` | C | tagYoutubeContent 매칭태그 update, id 지정 |
| `src/lib/crawler/orchestrator.ts:471` | C | near-dup 대표 승격 cluster_id update |
| `src/lib/crawler/orchestrator.ts:567` | C | 신규 콘텐츠 insert |
| `src/lib/crawler/orchestrator.ts:575` | C | insert 재시도(review_reason 컬럼 미적용 폴백) |
| `src/lib/crawler/orchestrator.ts:607` | C | 매칭태그 update, newId 지정 |
| `src/lib/crawler/orchestrator.ts:861` | C | 유튜브 mirror insert |
| `src/lib/crawler/orchestrator.ts:1147` | A | mergeByCanonical: canonical_url로 기존행 탐색 — 중복탐지 성격, 삭제행을 기존행으로 오판하면 클러스터링 오류 |
| `src/lib/crawler/orchestrator.ts:1160` | A | mergeByCanonical 폴백: original_url로 기존행 탐색, 동일 이유 |
| `src/lib/crawler/orchestrator.ts:1173` | C | cluster_id update, id 지정 |
| `src/lib/crawler/orchestrator.ts:1175` | C | cluster_id update, id 지정 |
| `src/lib/crawler/orchestrator.ts:1195` | A · 확인 필요 | enrichRecentContents: 이번 크롤런 신규 적재분 본문보강 대상 조회. 대부분 갓 생성된 행이라 실무상 삭제분과 겹칠 일이 거의 없지만, 원칙상 필터 권장 |
| `src/lib/crawler/orchestrator.ts:1232` | C | canonical_url update, id 지정 |
| `src/lib/crawler/youtube-transcript.ts:97` | C | transcript_fetched_at 마킹 update |
| `src/lib/crawler/youtube-transcript.ts:110` | C | transcript 저장 update |
| `src/lib/crawler/youtube-transcript.ts:143` | C | transcript_ko 저장 update |
| `src/lib/daily-insights/relevance.ts:37` | A · 확인 필요 | 과거기사 재분류용 조회. 대부분 이미 선정된 id 집합이라 영향 적음 |
| `src/lib/entities/generate-events.ts:181` | A | 엔티티 타임라인용 콘텐츠 목록 조회 |
| `src/lib/insight/generate.ts:158` | A | 산업 인사이트 카드 생성용 후보 조회 |
| `src/lib/insight/generate.ts:412` | A | 기업별 인사이트 카드 생성용 후보 조회 |
| `src/lib/insight/lgu-impact-backfill.ts:48` | A | LGU 임팩트 백필 후보 조회 |
| `src/lib/insight/lgu-impact-backfill.ts:81` | C | lgu_impact update |
| `src/lib/insight/sentiment-backfill.ts:47` | A | 감성분석 백필 후보 조회 |
| `src/lib/insight/sentiment-backfill.ts:68` | C | sentiment update |
| `src/lib/insight/youtube-summary-backfill.ts:34` | A | 유튜브 요약 백필 후보 조회 |
| `src/lib/insight/youtube-summary-backfill.ts:56` | C | summary_ko update |
| `src/lib/insight/youtube-tagging-backfill.ts:64` | A | 유튜브 태깅 백필 후보 조회 |
| `src/lib/issues/generate-candidates.ts:151` | A | 이슈 후보 생성용 콘텐츠 조회 |
| `src/lib/key-insights/candidates.ts:118` | A | 핵심 인사이트 후보 풀 조회(status='published') |
| `src/lib/key-insights/candidates.ts:273` | A | 같은 이슈의 과거기사 연계 조회 |
| `src/lib/mcp/tools/archive.ts:35` | C | id 단건 조회(.eq('id',x).maybeSingle()) |
| `src/lib/mcp/tools/read.ts:75` | A | content_search 목록형 조회 |
| `src/lib/mcp/tools/read.ts:119` | A | content_get: id 단건, admin 편집 아닌 외부 보고서 인용용 |
| `src/lib/newsletter/prepare-issue.ts:94` | A | 뉴스레터 카드선정 후보 조회 |
| `src/lib/newsletter/teasers.ts:32` | A | 뉴스레터 지식보고서 티저 후보 조회 |
| `src/lib/ops/daily-brief.ts:379` | A | 소스별 수집현황 집계 |
| `src/lib/ops/daily-brief.ts:518` | A | 본문보강 대기 카운트 |
| `src/lib/ops/daily-brief.ts:519` | A | 거절 콘텐츠 카운트 |
| `src/lib/ops/daily-brief.ts:520` | A | 검토대기 카운트 |
| `src/lib/ops/daily-brief.ts:553` | A | 검토사유별 카운트 |
| `src/lib/ops/detect-issues.ts:21` | A | 본문보강 백로그 카운트(이슈 감지 신호) |
| `src/lib/ops/weekly-report.ts:35` | A | 주간 수집 총량 카운트 |
| `src/lib/ops/weekly-report.ts:36` | A | 주간 발행 카운트 |
| `src/lib/ops/weekly-report.ts:37` | A | 주간 거절 카운트 |
| `src/lib/ops/weekly-report.ts:38` | A | 전주 대비 카운트 |
| `src/lib/ops/weekly-report.ts:39` | A | 본문보강 백로그 카운트 |
| `src/lib/reports/generate-strategy.ts:74` | A · 확인 필요 | AI 전략보고서 근거 콘텐츠 조회 — 사용자가 명시 선택한 id 집합. 삭제된 근거를 포함할지는 정책 결정 필요 |
| `src/lib/search/rag-retrieve.ts:105` | A | RAG 검색 1차 조회 |
| `src/lib/search/rag-retrieve.ts:141` | A | RAG 검색 엔티티 보강 조회 |

**"확인 필요" 3곳**: `orchestrator.ts:1195` · `daily-insights/relevance.ts:37` · `reports/generate-strategy.ts:74` — 코드만으로는 판단 근거가 부족한 제품 정책 결정이 필요한 지점.

---

## 2. 특히 확인 요청받은 4개 파일 — 전수 재확인

### `src/lib/crawler/orchestrator.ts` (12곳, 지시서 예상치와 정확히 일치)

| 줄 | 분류 | 비고 |
|---|---|---|
| 150 | C | 매칭태그 update |
| 471 | C | near-dup 대표 승격 update |
| 567 | C | insert |
| 575 | C | insert 재시도 |
| 607 | C | 매칭태그 update |
| 861 | C | 유튜브 mirror insert |
| 1147 | **A** | canonical_url 기존행 탐색 — 중복판정 지점 |
| 1160 | **A** | original_url 폴백 탐색 — 중복판정 지점 |
| 1173 | C | cluster_id update |
| 1175 | C | cluster_id update |
| 1195 | A(확인 필요) | 본문보강 대상 조회 |
| 1232 | C | canonical_url update |

**결론**: 중복판정에 직결되는 곳은 `mergeByCanonical`의 1147·1160 두 곳이다. 지시서가 우려한 "삭제분이 중복으로 잡혀 재수집이 막히는" 실제 판정 로직은 `dedup.ts`(아래)에 있고, `orchestrator.ts` 안에서는 `findSimilarCandidates` 호출(459행, `dedup.ts`로 위임)과 `mergeByCanonical`(1147/1160)이 그 지점이다.

### `src/lib/crawler/dedup.ts` (12곳 밖이지만 중복판정 핵심 — 5곳, 전부 A)

| 줄 | 분류 | 비고 |
|---|---|---|
| 22 | A | URL 중복탐지 |
| 40 | A | 제목해시 중복탐지 |
| 57 | A | 본문해시 중복탐지 |
| 83 | A | near-dup 후보 조회 |
| 122 | A | near-dup(본문포함) 후보 조회 |

⚠️ **가장 중요한 지점.** 여기 5곳 전부에 `deleted_at is null`이 빠지면 삭제된 기사와 URL/제목/본문이 같은 새 기사가 "이미 있음"으로 오판되어 재수집이 영구히 막힌다. 지시서 §0이 우려한 문제의 실제 발생 지점은 `orchestrator.ts` 자체보다 이 파일이다.

### `src/lib/mcp/tools/read.ts` (2곳, 전부 A)

| 줄 | 분류 | 비고 |
|---|---|---|
| 75 | A | content_search 목록 조회 |
| 119 | A | content_get id단건 — MCP로 외부(Claude 등)에 콘텐츠 본문을 반환하는 지점이라, admin 내부 열람용 단건조회와 달리 삭제된 글이 노출되면 안 됨 |

### `src/lib/key-insights/candidates.ts` (2곳, 전부 A)

| 줄 | 분류 | 비고 |
|---|---|---|
| 118 | A | 핵심 인사이트 후보 풀 조회 |
| 273 | A | 같은 이슈의 과거기사 연계 조회 |

### `src/app/api/council/route.ts` (2곳, 전부 A)

| 줄 | 분류 | 비고 |
|---|---|---|
| 55 | A | 외부 COUNCIL 검색 API 목록 조회 |
| 92 | A | id단건 — 외부 인용용, admin 편집 아님 |

---

## 3. ⚠️ 2단계 범위에 관한 제안 — David 판단 필요

지시서 §2단계는 "RLS 정책 2개(사용자 조회·익명 조회)만 교체하고, `admin 전체 조회`·`admin 관리` 정책은 그대로 둔다"로 돼 있다. 그런데 §1 인벤토리 과정에서 확인된 바로는:

- `src/app/admin/page.tsx`(14곳)와 `AdminContentManager.tsx`(12곳)를 포함해 **다수의 어드민 화면이 서비스롤이 아니라 `admin 전체 조회` RLS 정책으로 돈다.**
- 이 정책은 2단계에서 `deleted_at` 조건 없이 그대로 유지될 예정이므로, **소프트 삭제 후에도 이 화면들에는 삭제된 콘텐츠가 계속 섞여 나온다** — 서비스롤 A분류 지점과 똑같은 문제.
- 하지만 이 화면들은 "휴지통을 보여줘야 하는 화면"이 아니라 **일반 콘텐츠 관리 목록**이므로, 실제로는 여기에도 `.is('deleted_at', null)`이 필요하다(=A 취급).

**제안**: 2단계 3번("1단계 A분류 지점에 필터 추가")의 범위를 서비스롤 A분류 + `admin/page.tsx`·`AdminContentManager.tsx` 등 RLS-어드민 A성격 지점까지 넓히는 걸 검토해달라. 원하면 이 RLS-어드민 지점들도 별도 표로 추가 인벤토리하겠다(현재 표는 서비스롤만 다룬다 — 지시서가 요청한 범위 그대로).

---

## 4. `get_recommended_feed` 현재 본문 (Supabase에서 직접 조회)

```sql
CREATE OR REPLACE FUNCTION public.get_recommended_feed(p_user_id uuid, p_slot text, p_hashtags text[] DEFAULT '{}'::text[], p_limit integer DEFAULT 6)
 RETURNS TABLE(content_id uuid, score numeric, slot text, reason_keys text[])
 LANGUAGE plpgsql
 SECURITY DEFINER
 SET search_path TO 'public'
AS $function$
begin
  if p_user_id is distinct from auth.uid() then
    raise exception 'user_id mismatch';
  end if;

  if p_slot not in ('personalized', 'trending', 'editor', 'explore') then
    raise exception 'invalid slot: %', p_slot;
  end if;

  if p_slot = 'personalized' then
    return query
    with seen_recently as (
      select distinct cv.content_id
      from public.content_views cv
      where cv.user_id = p_user_id
        and cv.viewed_at >= now() - interval '14 days'
    ),
    scored as (
      select
        c.id as content_id,
        least(ov.match_count / 3.0, 1.0)::numeric as keyword_match,
        (exp(
          -ln(2)
          * (extract(epoch from (now() - coalesce(c.published_at, c.created_at))) / 3600.0)
          / 168.0
        ))::numeric as recency_decay,
        (case c.category
          when '가트너'     then 1.0
          when 'KRG'        then 0.9
          when '리포트'     then 0.95
          when '뉴스'       then 0.7
          when '유튜브'     then 0.6
          when '웹인사이트' then 0.65
          when '오피니언'   then 0.65
          else 0.5
        end)::numeric as source_weight,
        0::numeric as behavioral_boost,
        (case when sr.content_id is not null then 0.5 else 0 end)::numeric as seen_penalty,
        ov.matched_names as reason_keys
      from public.contents c
      left join seen_recently sr on sr.content_id = c.id
      cross join lateral (
        select
          coalesce(array_agg(distinct tag), '{}'::text[]) as matched_names,
          count(distinct tag) as match_count
        from unnest(
          coalesce(c.matched_keywords, '{}'::text[]) || coalesce(c.matched_groups, '{}'::text[])
        ) as tag
        where tag = any(p_hashtags)
      ) ov
      where c.status = 'published'
        and coalesce(c.published_at, c.created_at) >= now() - interval '5 days'
        and ov.match_count > 0
    )
    select
      scored.content_id,
      (0.55 * keyword_match
       + 0.25 * recency_decay
       + 0.10 * source_weight
       + 0.10 * behavioral_boost
       - seen_penalty)::numeric as score,
      p_slot,
      scored.reason_keys
    from scored
    order by score desc
    limit p_limit;

  elsif p_slot = 'trending' then
    return query
    with trend_counts as (
      select cv.content_id, count(*) as view_count
      from public.content_views cv
      where cv.viewed_at >= now() - interval '3 days'
      group by cv.content_id
    ),
    content_keyword_names as (
      select ck.content_id, array_agg(k.name) as names
      from public.content_keywords ck
      join public.keywords k on k.id = ck.keyword_id
      group by ck.content_id
    )
    select
      c.id,
      (exp(
        -ln(2)
        * (extract(epoch from (now() - coalesce(c.published_at, c.created_at))) / 3600.0)
        / 168.0
      ))::numeric as score,
      p_slot,
      coalesce(ckn.names, '{}'::text[])
    from trend_counts tc
    join public.contents c on c.id = tc.content_id and c.status = 'published'
    left join content_keyword_names ckn on ckn.content_id = c.id
    order by tc.view_count desc, c.id
    limit p_limit;

  elsif p_slot = 'editor' then
    return query
    select
      c.id,
      0.5::numeric as score,
      p_slot,
      '{}'::text[]
    from public.contents c
    where c.status = 'published'
      and c.is_editor_pick = true
      and coalesce(c.published_at, c.created_at) >= now() - interval '14 days'
    order by coalesce(c.published_at, c.created_at) desc
    limit p_limit;

  elsif p_slot = 'explore' then
    return query
    with seen_recently as (
      select distinct cv.content_id
      from public.content_views cv
      where cv.user_id = p_user_id
        and cv.viewed_at >= now() - interval '14 days'
    ),
    scored as (
      select
        c.id as content_id,
        (exp(
          -ln(2)
          * (extract(epoch from (now() - coalesce(c.published_at, c.created_at))) / 3600.0)
          / 168.0
        ))::numeric as recency_decay,
        (case c.category
          when '가트너'     then 1.0
          when 'KRG'        then 0.9
          when '리포트'     then 0.95
          when '뉴스'       then 0.7
          when '유튜브'     then 0.6
          when '웹인사이트' then 0.65
          when '오피니언'   then 0.65
          else 0.5
        end)::numeric as source_weight,
        (case when sr.content_id is not null then 0.5 else 0 end)::numeric as seen_penalty,
        coalesce(c.matched_keywords, '{}'::text[]) as reason_keys
      from public.contents c
      left join seen_recently sr on sr.content_id = c.id
      where c.status = 'published'
        and coalesce(c.published_at, c.created_at) >= now() - interval '5 days'
        and not (
          coalesce(c.matched_keywords, '{}'::text[]) && p_hashtags
          or coalesce(c.matched_groups, '{}'::text[]) && p_hashtags
        )
    )
    select
      scored.content_id,
      (0.25 * recency_decay
       + 0.10 * source_weight
       - seen_penalty)::numeric as score,
      p_slot,
      scored.reason_keys
    from scored
    order by score desc
    limit p_limit;
  end if;
end;
$function$
```

4개 분기(`personalized`/`trending`/`editor`/`explore`) 모두 `public.contents c`를 직접 스캔하며 `c.status = 'published'`만 걸고 있다. 2단계에서 4곳 각각의 `where`절(또는 `join ... on`절, `trending`은 `join` 조건에 있음)에 `and c.deleted_at is null`을 추가해야 한다.

---

## 세 SHA

1단계는 코드 변경이 없어 커밋 1개(문서만)다.

- 브랜치 시작점: `6a55bcb` (origin/main)
- 커밋: 아래 참고 (본 문서 + 지시서 자체를 함께 커밋)
