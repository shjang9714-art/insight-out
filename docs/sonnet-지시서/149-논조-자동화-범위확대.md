# 지시서 149 — C4: 논조 자동화 + 범위 확대

> 작성: Opus(Cowork) · 2026-06-24 · 레인: 데이터+AI MI
> 근거: `docs/AI작업-잔여-리스트-2026-06-22.md` C4 · 엔티티/이슈 로드맵 메모리(논조=경쟁사 한정·수동)
> 협업 루프: 로컬(커밋X). David 위임 → 구현 → Opus 검증 → "커밋" 트리거. **SQL 없음.**

---

## 0. 한 줄

논조(sentiment)는 지금 ① 어드민 "논조 분석" 버튼 **수동 트리거**로만 돌고 ② 후보가 **경쟁사 `matched_keywords` 한정**이라 이슈/엔티티 논조 추세가 빈약하다. 후보 범위를 **전 추적 엔티티(경쟁사 + mention 상위)**로 넓히고, 분류 루프를 공유 lib로 빼서 **야간 cron(145)에 자동 드레인**을 추가한다. DB·키 없으면 graceful(기존과 동일).

## 1. 현행 진단 (검증된 코드 사실)

- `src/lib/insight/sentiment.ts` — `classifySentiment(title, snippet)` → `'긍정'|'중립'|'부정'|null`. llmComplete('classify')·looseJsonParse·키워드 폴백. **재사용.**
- `src/app/api/admin/sentiment/route.ts` — 수동 POST. 후보 쿼리: `entities.is_competitor=true` 이름 → `contents` (published·`sentiment is null`·recent·`overlaps(matched_keywords, names)`) limit 40 → 순차 classify → `contents.sentiment` update. 경쟁사 한정.
- `src/app/admin/insights/page.tsx:266~289` — "경쟁사 논조 분석" 패널(handleSentiment, 라벨 경쟁사).
- `src/app/api/cron/ai-refresh/route.ts` — 논조 단계 **없음**.
- 소비처: 이슈 상세 논조 시계열/분포 · 엔티티 사건 타임라인(134) · 이슈 activity worsening 플래그 · AiInsightsView 경쟁사 동향. → 후보가 넓어지면 전부 풍부해짐.

## 2. DB / SQL

**스키마 변경 없음. SQL 핸드오프 없음.** `contents.sentiment` 기존. 신규 의존성 없음.

---

## 3. 구현

### 3-1. `src/lib/insight/sentiment-backfill.ts` (신규, server-only)

분류 루프를 공유 lib로 추출 + 범위 확대 + deadline 인지.

```ts
import 'server-only'
import type { SupabaseClient } from '@supabase/supabase-js'
import { classifySentiment } from '@/lib/insight/sentiment'

export interface BackfillSentimentOpts {
  days?: number        // 기본 14
  max?: number         // 기본 40, 상한 100
  deadline?: number    // Date.now() 비교용(cron); 없으면 무제한
}

export interface BackfillSentimentResult {
  analyzed: number
  candidates: number
  nullCount: number
  reason?: string
}

/**
 * 추적 엔티티(경쟁사 + mention 상위) 관련 최근 기사의 sentiment 백필.
 * 경쟁사 한정 → 범위 확대(C4). 키/후보 없으면 graceful(0 반환).
 */
export async function backfillSentiment(
  admin: SupabaseClient,
  opts: BackfillSentimentOpts = {},
): Promise<BackfillSentimentResult> {
  const days = Math.max(1, opts.days ?? 14)
  const max = Math.max(1, Math.min(opts.max ?? 40, 100))
  const deadline = opts.deadline

  // 1. 추적 엔티티 이름 (경쟁사 OR mention_count>=3) — 범위 확대 핵심
  const { data: entRows } = await admin
    .from('entities')
    .select('canonical_name, is_competitor, mention_count')
    .or('is_competitor.eq.true,mention_count.gte.3')
    .limit(100)

  const names = (entRows ?? [])
    .map((e: { canonical_name: string }) => e.canonical_name)
    .filter(Boolean)
  if (names.length === 0) {
    return { analyzed: 0, candidates: 0, nullCount: 0, reason: '추적 엔티티 미등록' }
  }

  // 2. 후보 (published·sentiment null·recent·matched_keywords ∩ names)
  const since = new Date(Date.now() - days * 24 * 60 * 60 * 1000 + 9 * 60 * 60 * 1000).toISOString()
  const { data: candidates } = await admin
    .from('contents')
    .select('id, title, summary_ko, body_original')
    .eq('status', 'published')
    .is('sentiment', null)
    .gte('collected_at', since)
    .overlaps('matched_keywords', names)
    .order('collected_at', { ascending: false })
    .limit(max)

  const rows = (candidates ?? []) as {
    id: string; title: string; summary_ko: string | null; body_original: string | null
  }[]
  if (rows.length === 0) return { analyzed: 0, candidates: 0, nullCount: 0 }

  // 3. 순차 classify → update (deadline 인지)
  let analyzed = 0, nullCount = 0
  for (const row of rows) {
    if (deadline && Date.now() >= deadline) break
    const snippet = row.summary_ko ?? row.body_original?.slice(0, 300) ?? row.title
    const result = await classifySentiment(row.title, snippet)
    if (!result) { nullCount++; continue }
    const { error } = await admin.from('contents').update({ sentiment: result }).eq('id', row.id)
    if (!error) analyzed++
  }

  return { analyzed, candidates: rows.length, nullCount }
}
```

핵심 변경 = 엔티티 이름 쿼리 `is_competitor=true` → `.or('is_competitor.eq.true,mention_count.gte.3')`. 나머지는 route 로직 그대로 옮김.

### 3-2. `src/app/api/admin/sentiment/route.ts` — lib 경유로 교체

후보/루프 인라인(65~118행)을 lib 호출로 대체. verifyAdmin·body 파싱 유지:

```ts
const supabase = createAdminClient()
const result = await backfillSentiment(supabase, { days, max })
return NextResponse.json(result)
```

(반환 형태 `{analyzed, candidates, nullCount, reason?}` 동일 → 어드민 UI 무변경. `classifySentiment` direct import 제거, `backfillSentiment` import 추가.)

### 3-3. `src/app/api/cron/ai-refresh/route.ts` — 자동 드레인 단계 추가

import + result 필드 + 단계(④ 브리핑 뒤, 끝에). deadline 인지.

```ts
import { backfillSentiment } from '@/lib/insight/sentiment-backfill'
// result 에 sentiments: 0 추가

// ── ⑤ 논조 백필 (추적 엔티티 기사, deadline 인지) ──────────────────────────
try {
  const { analyzed } = await backfillSentiment(admin, { deadline })
  result.sentiments = analyzed
} catch (err) {
  result.errors.push(`sentiments: ${err instanceof Error ? err.message : String(err)}`)
}
```
(마지막 단계라 별도 deadline early-return 불필요. backfillSentiment 내부가 deadline 체크.)

### 3-4. `src/app/admin/insights/page.tsx` — 라벨 범위 반영

"경쟁사 논조 분석"(L267) → "논조 분석" 또는 "논조 분석 (이슈·기업 기사)". confirm 문구(L103) "최근 14일 경쟁사 기사…" → "최근 14일 추적 기업·이슈 기사(최대 40건)의 논조를…". 기능 로직 무변경.

---

## 4. 회귀 가드 / 비기능 요건

- LLM 키 없음 → classifySentiment null → analyzed 0(graceful). cron은 상단 LLM 키 가드로 전체 skip.
- 추적 엔티티 0 → reason 반환, 0건.
- 어드민 수동 버튼 동작·반환 형태 무변경(범위만 확대).
- cron 마지막 단계 추가 — 앞 단계(인사이트·후보·타임라인·브리핑) 무영향, 예산 초과 시 backfill 내부 deadline break.
- 하드코딩 hex 0·신규 의존성 0·SQL 0.

## 5. 검증 (Sonnet 자체)

1. `npx tsc --noEmit` 0 (route에서 classifySentiment 미사용 import 제거 확인)
2. `npx eslint` 0
3. 하드코딩 hex grep 0
4. `.or('is_competitor.eq.true,mention_count.gte.3')` PostgREST 문법 점검(콤마 구분, 공백 없음)

## 6. 후속 (범위 밖)

- 이슈 직접 링크(issue_contents) 기반 후보 보강 — matched_keywords에 안 잡히는 이슈 기사까지(v2, 쿼리 복잡).
- 논조 재판정(기존 sentiment 갱신) · 신뢰도 낮은 분류 재시도.
- D2 자동 발행 게이팅과 묶어 품질 신호로 활용.

---

## 7. 라이브 검증 체크리스트 추가분 (`docs/라이브-검증-체크리스트.md` §16 신설)

- [ ] `/admin/insights` "논조 분석" 패널 라벨이 경쟁사→추적 기업·이슈 범위로 갱신
- [ ] 버튼 실행 → analyzed>0 (경쟁사 외 추적 엔티티 기사도 분석됨)
- [ ] 이슈 상세 논조 시계열·엔티티 사건 타임라인 논조가 더 촘촘해짐
- [ ] 야간 cron(`/api/cron/ai-refresh`) 응답에 `sentiments` 카운트, 다음 날 sentiment 채워진 기사 증가
- [ ] LLM 키/엔티티 없을 때 graceful(0, 화면 무파손)
