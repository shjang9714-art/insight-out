# Sonnet 지시서 16 — 아카이빙 메일 전송 설계 + 구현 (#27/#28)

> 작성: Opus(Cowork) · 대상: Claude Code(Sonnet) · 유형: Route Handler 신규 + UI 연동
> 목적: 아카이브의 콘텐츠를 **Resend API** 로 이메일 발송 — 지시서 15 에서 `disabled` 처리한 "이메일로 받기" 버튼 활성화
> ⚠️ 작업 전 `AGENTS.md` + `src/app/dashboard/mypage/page.tsx`(지시서15 완료 후) + `src/lib/types.ts` + `src/app/api/` 구조 를 먼저 읽을 것.

---

## 0. 아키텍처 결정 (#27 — 설계)

### 이메일 서비스: Resend

- AGENTS.md §14 "발송 채널: Resend 또는 SendGrid" → **Resend** 채택 (간단한 HTTP API, Next.js 친화)
- 패키지: `resend` (npm). **설치 전 사용자 확인 필요** — PR 설명에 이유 명시할 것.
- Resend 무료 플랜: 100건/일. Phase 2-B 내부 테스트에 충분.

### 플로우

```
마이페이지 "이메일로 받기" 클릭
  → POST /api/email/send-archive
  → Route Handler: 인증 확인 → 아카이브 + 항목 조회 → HTML 빌드 → Resend 발송
  → 응답: { success: true } 또는 에러
  → 클라이언트: 완료 토스트 / 에러 메시지
```

### 수신 이메일 결정 규칙

1. `newsletter_subscriptions.newsletter_email` 이 있으면 그걸 사용
2. 없으면 `auth.users.email` 사용
3. 발신자: 환경변수 `RESEND_FROM_EMAIL` (예: `noreply@insight-out-app.vercel.app`)

### 첨부파일 처리

- PDF 리포트(`contents.file_path` 있음): Supabase Storage **서명 URL(유효 1시간)** 을 이메일 본문에 링크로 삽입 — 실제 파일 첨부 X (Resend 무료 플랜 첨부 제한 + 서명 URL 보안)
- 뉴스/일반 콘텐츠: `original_url` 링크 삽입

### 환경 변수 (신규 2개)

| 변수명 | 설명 | 예시 |
|---|---|---|
| `RESEND_API_KEY` | Resend API Key (서버 전용, NEXT_PUBLIC_ 절대 금지) | `re_xxxx` |
| `RESEND_FROM_EMAIL` | 발신자 이메일 | `noreply@insight-out-app.vercel.app` |

> ⚠️ Resend 무료 플랜은 **resend.dev** 도메인만 발신 가능 (운영 전 도메인 설정 필요). 개발 테스트 시 `onboarding@resend.dev` 사용 가능.

---

## 1. 할 일

### 1.1 패키지 설치
```bash
npm install resend
```

### 1.2 `.env.example` 갱신 (신규 항목 추가)
```
# 이메일 발송 (Resend)
RESEND_API_KEY=
RESEND_FROM_EMAIL=
```

### 1.3 `src/lib/email/archive-template.ts` (신규)

아카이브 HTML 이메일 템플릿 빌더. **React Email 불필요** — 간단한 문자열 템플릿으로 구현.

```typescript
import type { ContentCategory } from '@/lib/types'

export interface ArchiveEmailItem {
  title: string
  category: ContentCategory | string
  sourceName: string | null
  publishedAt: string | null
  summaryKo: string | null
  originalUrl: string | null
  reportSignedUrl: string | null // PDF 서명 URL
}

export interface ArchiveEmailData {
  archiveName: string
  recipientName: string
  items: ArchiveEmailItem[]
}

export function buildArchiveEmailHtml(data: ArchiveEmailData): string {
  const itemsHtml = data.items.map((item, i) => {
    const linkUrl = item.reportSignedUrl ?? item.originalUrl
    const linkLabel = item.reportSignedUrl ? 'PDF 보기' : '원문 보기'

    return `
      <tr>
        <td style="padding: 16px; border-bottom: 1px solid #f3f4f6;">
          <p style="margin: 0 0 4px; font-size: 13px; color: #6b7280;">${i + 1}. ${item.category}${item.sourceName ? ` · ${item.sourceName}` : ''}${item.publishedAt ? ` · ${new Date(item.publishedAt).toLocaleDateString('ko-KR')}` : ''}</p>
          <p style="margin: 0 0 6px; font-size: 16px; font-weight: 600; color: #111827; line-height: 1.4;">${item.title}</p>
          ${item.summaryKo ? `<p style="margin: 0 0 8px; font-size: 14px; color: #374151; line-height: 1.6;">${item.summaryKo}</p>` : ''}
          ${linkUrl ? `<a href="${linkUrl}" style="font-size: 13px; color: #E6007E; text-decoration: none;">${linkLabel} →</a>` : ''}
        </td>
      </tr>
    `
  }).join('')

  return `
<!DOCTYPE html>
<html lang="ko">
<head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"></head>
<body style="margin: 0; padding: 0; background-color: #f9fafb; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;">
  <table width="100%" cellpadding="0" cellspacing="0" style="max-width: 600px; margin: 32px auto; background: #ffffff; border-radius: 12px; overflow: hidden; border: 1px solid #e5e7eb;">
    <!-- 헤더 -->
    <tr>
      <td style="padding: 24px 24px 16px; background-color: #E6007E;">
        <p style="margin: 0; font-size: 12px; color: rgba(255,255,255,0.7); letter-spacing: 0.05em;">INSIGHT OUT</p>
        <h1 style="margin: 4px 0 0; font-size: 20px; color: #ffffff;">${data.archiveName}</h1>
      </td>
    </tr>
    <!-- 안내 문구 -->
    <tr>
      <td style="padding: 16px 24px; border-bottom: 1px solid #f3f4f6; background: #fdf2f8;">
        <p style="margin: 0; font-size: 13px; color: #6b7280;">
          ${data.recipientName} 님이 담아둔 인사이트 ${data.items.length}건을 전달드립니다.
        </p>
      </td>
    </tr>
    <!-- 콘텐츠 목록 -->
    <table width="100%" cellpadding="0" cellspacing="0">
      ${itemsHtml}
    </table>
    <!-- 푸터 -->
    <tr>
      <td style="padding: 20px 24px; border-top: 1px solid #f3f4f6; text-align: center;">
        <p style="margin: 0; font-size: 12px; color: #9ca3af;">
          이 메일은 Insight Out에서 발송되었습니다.<br>
          <a href="https://insight-out-app.vercel.app/dashboard/mypage" style="color: #9ca3af;">수신 설정 변경</a>
        </p>
      </td>
    </tr>
  </table>
</body>
</html>
  `.trim()
}
```

### 1.4 `src/app/api/email/send-archive/route.ts` (신규)

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { cookies } from 'next/headers'
import { createServerClient } from '@supabase/ssr'
import { Resend } from 'resend'
import { buildArchiveEmailHtml, type ArchiveEmailItem } from '@/lib/email/archive-template'
import { getReportSignedUrl } from '@/lib/contents/report-url'

const resend = new Resend(process.env.RESEND_API_KEY)

export async function POST(req: NextRequest) {
  try {
    // ── 1. 인증 ──────────────────────────────────────────────────────────
    const cookieStore = await cookies()
    const supabase = createServerClient(
      process.env.NEXT_PUBLIC_SUPABASE_URL!,
      process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
      {
        cookies: {
          getAll() { return cookieStore.getAll() },
          setAll(toSet) { toSet.forEach(({ name, value, options }) => cookieStore.set(name, value, options)) },
        },
      }
    )

    const { data: { user } } = await supabase.auth.getUser()
    if (!user) {
      return NextResponse.json({ error: '로그인이 필요합니다.' }, { status: 401 })
    }

    // ── 2. 요청 파싱 ──────────────────────────────────────────────────────
    const { archiveId } = await req.json() as { archiveId: string }
    if (!archiveId) {
      return NextResponse.json({ error: 'archiveId 가 필요합니다.' }, { status: 400 })
    }

    // ── 3. 아카이브 + 항목 조회 ───────────────────────────────────────────
    const { data: archive, error: archiveErr } = await supabase
      .from('archives')
      .select(`
        id, name,
        archive_items(
          content_id, youtube_video_id,
          contents(id, title, category, summary_ko, original_url, file_path, published_at,
            sources(name)
          )
        )
      `)
      .eq('id', archiveId)
      .eq('user_id', user.id)   // 본인 아카이브 확인
      .single()

    if (archiveErr || !archive) {
      return NextResponse.json({ error: '아카이브를 찾을 수 없습니다.' }, { status: 404 })
    }

    // ── 4. 수신 이메일 결정 ───────────────────────────────────────────────
    const { data: userProfile } = await supabase
      .from('users')
      .select('name')
      .eq('id', user.id)
      .single()

    const { data: newsletterSub } = await supabase
      .from('newsletter_subscriptions')
      .select('newsletter_email')
      .eq('user_id', user.id)
      .single()

    const toEmail = newsletterSub?.newsletter_email ?? user.email
    if (!toEmail) {
      return NextResponse.json({ error: '수신 이메일을 찾을 수 없습니다.' }, { status: 400 })
    }

    // ── 5. 이메일 항목 빌드 ───────────────────────────────────────────────
    const items: ArchiveEmailItem[] = []

    for (const archiveItem of archive.archive_items) {
      const content = archiveItem.contents as any
      if (!content) continue

      let reportSignedUrl: string | null = null
      if (content.file_path) {
        reportSignedUrl = await getReportSignedUrl(content.file_path)
      }

      items.push({
        title: content.title,
        category: content.category,
        sourceName: content.sources?.name ?? null,
        publishedAt: content.published_at ?? null,
        summaryKo: content.summary_ko ?? null,
        originalUrl: content.original_url ?? null,
        reportSignedUrl,
      })
    }

    if (items.length === 0) {
      return NextResponse.json({ error: '발송할 콘텐츠가 없습니다.' }, { status: 400 })
    }

    // ── 6. Resend 발송 ─────────────────────────────────────────────────────
    const html = buildArchiveEmailHtml({
      archiveName: archive.name,
      recipientName: userProfile?.name ?? '사용자',
      items,
    })

    const { error: sendError } = await resend.emails.send({
      from: process.env.RESEND_FROM_EMAIL ?? 'onboarding@resend.dev',
      to: toEmail,
      subject: `[Insight Out] ${archive.name} — ${items.length}건의 인사이트`,
      html,
    })

    if (sendError) {
      console.error('[send-archive] Resend error:', sendError)
      return NextResponse.json({ error: '이메일 발송에 실패했습니다.' }, { status: 500 })
    }

    return NextResponse.json({ success: true, to: toEmail, count: items.length })

  } catch (err) {
    console.error('[send-archive] unexpected error:', err)
    return NextResponse.json({ error: '서버 오류가 발생했습니다.' }, { status: 500 })
  }
}
```

### 1.5 마이페이지 "이메일로 받기" 버튼 활성화 (지시서 15 후속)

`src/app/dashboard/mypage/page.tsx` 에서 `disabled` 버튼을 활성화.

**State 추가**
```typescript
const [sendingArchiveId, setSendingArchiveId] = useState<string | null>(null)
const [sendResult, setSendResult] = useState<{ archiveId: string; to: string } | null>(null)
```

**핸들러 추가**
```typescript
async function handleSendEmail(archiveId: string) {
  setSendingArchiveId(archiveId)
  setArchiveError(null)
  setSendResult(null)
  try {
    const res = await fetch('/api/email/send-archive', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ archiveId }),
    })
    const data = await res.json()
    if (!res.ok) throw new Error(data.error ?? '발송에 실패했습니다.')
    setSendResult({ archiveId, to: data.to })
  } catch (err) {
    setArchiveError(err instanceof Error ? err.message : '이메일 발송에 실패했습니다.')
  } finally {
    setSendingArchiveId(null)
  }
}
```

**버튼 수정** (지시서 15 의 disabled 버튼 대체)
```tsx
<Button
  type="button"
  variant="outline"
  size="sm"
  onClick={() => handleSendEmail(archive.id)}
  disabled={sendingArchiveId === archive.id}
  className="h-7 text-xs"
>
  <Mail className="mr-1 h-3.5 w-3.5" />
  {sendingArchiveId === archive.id ? '발송 중...' : '이메일로 받기'}
</Button>

{/* 발송 완료 메시지 */}
{sendResult?.archiveId === archive.id && (
  <p className="mt-1 text-xs text-green-600">
    {sendResult.to} 으로 발송되었습니다.
  </p>
)}
```

## 2. 완료 조건
- [ ] `resend` 패키지 설치 + `package.json` 반영
- [ ] `.env.example` 에 `RESEND_API_KEY`, `RESEND_FROM_EMAIL` 추가
- [ ] `src/lib/email/archive-template.ts` — HTML 빌더 함수
- [ ] `src/app/api/email/send-archive/route.ts` — POST 핸들러
- [ ] 마이페이지 "이메일로 받기" 버튼 활성화 + 발송 중/완료 상태
- [ ] 인증 없는 요청 401, 본인 아카이브 외 404, 빈 아카이브 400 처리
- [ ] `RESEND_API_KEY` / `RESEND_FROM_EMAIL` 코드에 하드코딩 금지 (env만 참조)
- [ ] `npx tsc --noEmit` · `npm run build` · `npm run lint` 통과

## 3. 검증 명령
```bash
npx tsc --noEmit && npm run build && npm run lint
```

**로컬 테스트**:
1. `.env.local` 에 `RESEND_API_KEY=re_xxxx`, `RESEND_FROM_EMAIL=onboarding@resend.dev` 설정
2. 마이페이지 → 아카이브 → "이메일로 받기" 클릭
3. 수신 이메일에서 콘텐츠 목록 + 링크 확인
4. Resend 대시보드(resend.com)에서 발송 로그 확인

> ⚠️ Vercel 배포 후에는 수희(owner)가 Vercel 환경변수에 `RESEND_API_KEY`, `RESEND_FROM_EMAIL` 을 직접 추가해야 함. 추가 전까지 운영에서 500 에러.

## 4. 완료 보고 양식
```
## 완료 보고 — 지시서 16 메일 전송
- 변경 파일: <경로 목록>
- 한 일: <3~5줄>
- 완료 조건 충족: [x] 항목별
- 검증 결과: tsc · build · lint
- 미해결·판단보류: <없으면 "없음">
```
