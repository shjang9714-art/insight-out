# 지시서 383 — 어드민 콘텐츠 탭 셸 표준 (S2)

> 작성: 플래너(Opus) · 2026-07-17 · 어드민 개편 슬라이스 S2
> 근거: `docs/어드민-개편-최종안-2026-07-17.md` §2.2·§16 · `docs/어드민-개편-갭분석-2026-07-17.md` §5 S2
> 협업 루프: 로컬(커밋X). 디렉터 위임 → 구현 → 검증 → "커밋". **SQL 없음.**
> 선행/동반: S1(지시서 382, 배포됨). 이후 S3·S4·S7이 이 셸을 재사용.
> 번호: 383 (순차 확인 완료. 착수 전 `ls docs/sonnet-지시서/ | sort -n | tail`로 재확인)

---

## 0. 한 줄
대상 화면 공통 프레임 **`AdminTabShell`**(브레드크럼 · 페이지 헤더 · 컨텍스트 바 슬롯 · URL 기반 탭)을 만들고, `/admin/upload`에 파일럿 이식해 표준을 확립한다. **기존 `AdminTabs`·`AdminPageHeader`를 재사용**하며 새로 만들지 않는다.

---

## 1. 현행 진단 (확인된 코드 사실)

- **`src/components/admin/ui/AdminTabs.tsx`**: 이미 존재하는 레벨2 인페이지 탭 공통 컴포넌트. **순수 표시(상태는 부모 관리)**. API: `items: AdminTabItem[]`(각 `{value,label,count?,disabled?}`, `export interface AdminTabItem`), `value`, `onChange`, `aria-label`. 세그먼트 박스 스타일(활성 `bg-brand-600`). → **이 컴포넌트를 재사용.**
- **`src/components/admin/ui/AdminPageHeader.tsx`**: 제목·설명·아이콘(`findAdminNavItem(pathname)`에서 조회, null 안전 `?? '어드민'`)·도움말(`InfoHelp`)·`actions` 슬롯. `titleOverride`/`descriptionOverride` prop 지원. `mb-7`. **32개 페이지가 사용** → **API를 바꾸지 말고 셸이 내부에서 재사용.**
- 탭 상태는 현재 **로컬 `useState`**(예: `ContentAddTabs.tsx` `useState<Tab>('file')`). URL 연동 없음. → S2에서 **URL `?tab=`으로 표준화.**
- **브레드크럼 컴포넌트 없음**(`Breadcrumb` grep 0). → 신규(경량).
- **`AdminContentManager.tsx`가 이미 `useSearchParams`를 사용**하며 정상 빌드됨 → URL 쿼리 훅은 검증된 선례가 있다. 동일 패턴을 따른다.
- 파일럿 대상 `src/app/admin/upload/page.tsx`(서버 컴포넌트, `metadata` export):
  ```tsx
  export default function AdminUploadPage() {
    return (<><AdminPageHeader /><ContentAddTabs /></>)
  }
  ```
  `ContentAddTabs.tsx`(클라이언트)는 `AdminTabs`+`useState`로 file/paste/url 3탭. → 파일럿으로 셸에 이식.
- nav: `/admin/upload` → 그룹 `콘텐츠`, 라벨 `콘텐츠 추가`, 아이콘 `FilePlus`(지시서 382 반영). `findAdminNavItem`은 item만 반환(그룹 없음) → 브레드크럼용 **그룹 조회 헬퍼 추가 필요.**
- CSS 토큰 존재: `admin-caption`(12px), `admin-btn-text`, `admin-page-title/desc`. 새 globals 불필요(Tailwind 유틸로 처리).

---

## 2. DB / SQL
**없음.**

---

## 3. 구현

### 3.1 신규 `src/lib/admin/useTabParam.ts` — URL 탭 상태 훅
```ts
'use client'

import { useCallback } from 'react'
import { usePathname, useRouter, useSearchParams } from 'next/navigation'

/** ?tab= 로 탭 상태를 관리한다. 기본값이면 파라미터를 지워 URL을 깨끗이 유지. history 오염 방지 위해 replace. */
export function useTabParam<T extends string>(
  validValues: readonly T[],
  defaultValue: T,
  paramKey = 'tab',
): [T, (value: T) => void] {
  const searchParams = useSearchParams()
  const router = useRouter()
  const pathname = usePathname()

  const raw = searchParams.get(paramKey)
  const active = (raw && (validValues as readonly string[]).includes(raw) ? raw : defaultValue) as T

  const setActive = useCallback(
    (value: T) => {
      const params = new URLSearchParams(searchParams.toString())
      if (value === defaultValue) params.delete(paramKey)
      else params.set(paramKey, value)
      const qs = params.toString()
      router.replace(qs ? `${pathname}?${qs}` : pathname, { scroll: false })
    },
    [searchParams, router, pathname, defaultValue, paramKey],
  )

  return [active, setActive]
}
```

### 3.2 `src/lib/admin/nav.ts` — 그룹 조회 헬퍼 **추가**(기존 export·로직 변경 금지)
파일 맨 아래 `findAdminNavItem` 아래에 **덧붙인다**:
```ts
/** pathname → { 그룹명, 항목 }. 브레드크럼용. 정확 일치 우선, 없으면 최장 startsWith. */
export function findAdminNavLocation(pathname: string): { group: string; item: AdminNavItem } | null {
  for (const g of ADMIN_NAV_GROUPS) {
    const exact = g.items.find(i => i.href === pathname)
    if (exact) return { group: g.group, item: exact }
  }
  for (const g of ADMIN_NAV_GROUPS) {
    const pref = g.items
      .filter(i => i.href !== '/admin' && pathname.startsWith(i.href))
      .sort((a, b) => b.href.length - a.href.length)[0]
    if (pref) return { group: g.group, item: pref }
  }
  return null
}
```

### 3.3 신규 `src/components/admin/ui/AdminContextBar.tsx` — 상태 컨텍스트 바(라벨:값 칩)
```tsx
import type { ReactNode } from 'react'
import { cn } from '@/lib/utils'

export interface AdminContextItem {
  label: string
  value: ReactNode
}

/** 화면 상단 상태 요약 바. AI 콘텐츠 화면에서 유형·환경·상태·프롬프트 버전·주 모델 등을 표시(§16). 항목 없으면 렌더 안 함. */
export default function AdminContextBar({ items, className }: { items: AdminContextItem[]; className?: string }) {
  if (items.length === 0) return null
  return (
    <div className={cn('mb-5 flex flex-wrap items-center gap-x-5 gap-y-2 rounded-lg border border-border bg-muted/40 px-4 py-2.5', className)}>
      {items.map((it, i) => (
        <div key={i} className="flex items-center gap-1.5">
          <span className="admin-caption text-muted-foreground">{it.label}</span>
          <span className="admin-caption font-medium text-foreground">{it.value}</span>
        </div>
      ))}
    </div>
  )
}
```

### 3.4 신규 `src/components/admin/ui/AdminTabShell.tsx` — 공통 셸(클라이언트)
```tsx
'use client'

import type { ReactNode } from 'react'
import { usePathname } from 'next/navigation'
import { findAdminNavLocation } from '@/lib/admin/nav'
import AdminPageHeader from '@/components/admin/ui/AdminPageHeader'
import AdminTabs, { type AdminTabItem } from '@/components/admin/ui/AdminTabs'
import { useTabParam } from '@/lib/admin/useTabParam'

interface Props {
  tabs: AdminTabItem[]
  defaultTab: string
  renderContent: (activeTab: string) => ReactNode
  actions?: ReactNode
  contextBar?: ReactNode
  titleOverride?: string
  descriptionOverride?: string
  'aria-label'?: string
}

/** 어드민 대상 화면 표준 프레임: 브레드크럼 → 헤더 → 컨텍스트 바 → 탭(URL ?tab=) → 본문. */
export default function AdminTabShell({
  tabs, defaultTab, renderContent, actions, contextBar,
  titleOverride, descriptionOverride, 'aria-label': ariaLabel,
}: Props) {
  const pathname = usePathname()
  const loc = findAdminNavLocation(pathname)
  const values = tabs.map(t => t.value)
  const [active, setActive] = useTabParam(values, defaultTab)

  return (
    <div>
      {loc && (
        <nav aria-label="breadcrumb" className="admin-caption mb-2 flex items-center gap-1.5 text-muted-foreground">
          <span>{loc.group}</span>
          <span aria-hidden>/</span>
          <span className="text-foreground">{titleOverride ?? loc.item.label}</span>
        </nav>
      )}

      <AdminPageHeader actions={actions} titleOverride={titleOverride} descriptionOverride={descriptionOverride} />

      {contextBar}

      {tabs.length > 0 && (
        <div className="mb-6">
          <AdminTabs items={tabs} value={active} onChange={setActive} aria-label={ariaLabel} />
        </div>
      )}

      {renderContent(active)}
    </div>
  )
}
```

### 3.5 파일럿 이식 — `/admin/upload`

**`src/components/admin/ContentAddTabs.tsx` 전체 교체:**
```tsx
'use client'

import AdminTabShell from '@/components/admin/ui/AdminTabShell'
import ReportUploadForm from '@/components/admin/ReportUploadForm'
import TextPasteForm from '@/components/admin/TextPasteForm'
import UrlImportForm from '@/components/admin/UrlImportForm'

const TABS = [
  { value: 'file',  label: '파일 업로드' },
  { value: 'paste', label: '텍스트 붙여넣기' },
  { value: 'url',   label: 'URL 가져오기' },
]

export default function ContentAddTabs() {
  return (
    <AdminTabShell
      tabs={TABS}
      defaultTab="file"
      aria-label="콘텐츠 추가 방식"
      renderContent={(tab) =>
        tab === 'file' ? <ReportUploadForm /> : tab === 'paste' ? <TextPasteForm /> : <UrlImportForm />
      }
    />
  )
}
```

**`src/app/admin/upload/page.tsx` 교체** — 헤더는 셸이 렌더하므로 제거, `useSearchParams` 정적생성 대비 `Suspense`로 감싼다:
```tsx
import type { Metadata } from 'next'
import { Suspense } from 'react'
import ContentAddTabs from '@/components/admin/ContentAddTabs'

export const metadata: Metadata = {
  title: '콘텐츠 추가 | 어드민 | Insight Out',
  description: '파일 업로드 또는 텍스트 붙여넣기로 콘텐츠를 수동 등록합니다.',
}

export default function AdminUploadPage() {
  return (
    <Suspense fallback={null}>
      <ContentAddTabs />
    </Suspense>
  )
}
```

---

## 4. 회귀 가드 (건드리면 안 되는 것 / 경계)

1. **`AdminTabs.tsx`·`AdminPageHeader.tsx`의 public API를 바꾸지 말 것.** 다른 5개 매니저(IssueManager·EntityManager·RequestsBoard·AdminContentManager·ContentAddTabs)와 32개 페이지가 의존한다. 셸은 이들을 **재사용만** 한다.
2. **이번 슬라이스는 `/admin/upload` 한 곳만 이식한다.** 나머지 31개 페이지·매니저는 **그대로**(후속 슬라이스에서 이식).
3. **`nav.ts`는 `findAdminNavLocation` 추가만.** 기존 `ADMIN_NAV_GROUPS`·`findAdminNavItem`·타입 변경 금지(지시서 382 배포본 유지).
4. **`useSearchParams` + 정적생성**: 파일럿 페이지에서 `<Suspense>`로 감쌌다(§3.5). 빌드가 `useSearchParams() should be wrapped in a suspense boundary` 로 실패하면 이 Suspense가 있는지 확인. 선례 `AdminContentManager.tsx`와 동일 계열 패턴이다.
5. **탭 전환은 `router.replace`**(push 아님) — 뒤로가기 히스토리를 탭 클릭으로 오염시키지 않는다. `{ scroll: false }` 유지.
6. **기본 탭일 때 `?tab=` 파라미터를 URL에서 제거**한다(깨끗한 URL). 잘못된 tab 값은 기본값으로 폴백.
7. 시각: 브레드크럼은 헤더 **위**, 컨텍스트 바는 헤더 **아래**·탭 **위**. `AdminPageHeader`의 `mb-7` 유지되므로 간격 중복 확인(파일럿에서 육안 점검).
8. 파일럿에서 `AdminPageHeader`를 페이지에서 제거하고 셸이 렌더하므로 **헤더가 두 번 나오지 않아야** 한다.

---

## 5. 검증 (구현자 자체 체크)

```bash
npx tsc --noEmit
npm run lint
npm run build          # ← useSearchParams/Suspense 경계 여기서 확인

# 신규 파일 4개 존재
ls src/lib/admin/useTabParam.ts \
   src/components/admin/ui/AdminContextBar.tsx \
   src/components/admin/ui/AdminTabShell.tsx
grep -n "findAdminNavLocation" src/lib/admin/nav.ts        # 추가 확인
# AdminTabs/AdminPageHeader public API 무변경
git diff --stat src/components/admin/ui/AdminTabs.tsx src/components/admin/ui/AdminPageHeader.tsx  # 비어야 함
```

- 완료 조건: tsc/lint/build 통과. 변경 파일 = `useTabParam.ts`(신규)·`AdminContextBar.tsx`(신규)·`AdminTabShell.tsx`(신규)·`nav.ts`(헬퍼 추가)·`ContentAddTabs.tsx`·`upload/page.tsx`. **그 외 없음.**
- 로컬 육안(`/admin/upload`):
  - 상단에 `콘텐츠 / 콘텐츠 추가` 브레드크럼, 그 아래 헤더(제목·설명·FilePlus 아이콘) 1회만.
  - 탭 클릭 시 URL이 `?tab=paste`/`?tab=url`로 바뀌고, `file`이면 `?tab=` 없음.
  - `/admin/upload?tab=url` 직접 접근 시 URL 가져오기 탭이 활성으로 열림(딥링크).
  - 뒤로가기가 탭마다 쌓이지 않음(replace).
  - 다른 페이지(예: `/admin/contents`, `/admin/newsletter`) 헤더·탭 정상(무영향).

---

## 6. 후속 (이번 범위 밖)
- S3: `인사이트>핵심인사이트` 통합 시 이 셸 채택(이슈·일일핵심·카드 탭). 컨텍스트 바에 상태·프롬프트 버전 표시.
- S4: `콘텐츠`(뉴스/웹인사이트/유튜브) 분화 시 셸 + `?type=`&`?tab=` 조합.
- S7: `AI 리포트` 생산단위 탭에 컨텍스트 바(유형·환경·프롬프트 버전·주 모델·최근 실행) 채움.
- 나머지 30여 페이지 점진 이식. 탭 없는 화면용 셸 변형(탭 생략)도 이때 정리.
- 선택: 브레드크럼 그룹을 클릭 가능하게(그룹 랜딩/첫 항목 이동) — 현재는 텍스트.

---

## 7. 라이브 체크리스트 (배포 후 육안)
- [ ] `/admin/upload`에 브레드크럼(`콘텐츠 / 콘텐츠 추가`)이 뜬다.
- [ ] 헤더가 1회만 렌더된다(중복 없음).
- [ ] 탭 전환이 `?tab=`에 반영되고 딥링크가 동작한다.
- [ ] 기본 탭에서 URL이 깨끗하다(`?tab=` 없음).
- [ ] 파일 업로드·붙여넣기·URL 3탭 기능이 이전과 동일하게 작동한다.
- [ ] 다른 어드민 페이지가 영향받지 않는다.
