# 지시서 171 — 관심기업 카드 버킷색 정합

> 작성: Opus(Cowork) · 2026-06-29 · 레인: AI 인사이트 IA 개선 (UI 후속)
> 근거: 167(카테고리 버킷색) 후속 — 당시 관심기업 탭은 bucketByTopic 미전달로 중립 폴백. David 비-LLM UI 후속 묶음.
> 협업 루프: 로컬(커밋X). David 위임 → 구현 → Opus 검증 → "커밋". **SQL 없음. LLM 없음.**
> 선행: 167.

---

## 0. 한 줄

기업동향 관심기업 탭 카드의 카테고리 칩이 **중립색으로만 나오는 것을 버킷색(167)으로** 맞춘다. 브리핑 탭과 동일하게 topic→버킷 매핑을 계산해 `bucketByTopic` 을 넘긴다.

## 1. 현행 진단 (검증된 코드 사실)

- `src/app/dashboard/entities/page.tsx` 관심기업 뷰(243-256): `<InsightCardsSectionClient groups={companyInsightGroups} contentMap={companyContentMap} />` — **`bucketByTopic` prop 미전달** → 167 폴백('일반' 중립).
- 브리핑(`AiInsightsView`)은 `patternTagMap`(keyword_groups name/include_patterns → tag_type) + `tagTypeToBucket` 로 `bucketByTopic` 구성해 전달(167). 동일 패턴 재사용 가능.
- `InsightCardsSectionClient` 는 이미 `bucketByTopic?: Record<string, TagBucket>` 옵셔널 지원(167).

## 2. DB / SQL

**없음.** 기존 `keyword_groups` 조회만.

---

## 3. 구현 (`entities/page.tsx`)

- 관심기업 뷰(view==='watchlist') 데이터 패칭부에 **keyword_groups 조회 추가**(AiInsightsView 와 동일):
  ```ts
  supabase.from('keyword_groups').select('name, tag_type, include_patterns').eq('is_active', true).limit(200)
  ```
- `patternTagMap`(name/include_patterns lower → tag_type, industry 우선순위 낮춤) 구성 → `companyCards` 의 각 `topic` 을 `tagTypeToBucket` 로 매핑해 `bucketByTopic` 생성.
- `<InsightCardsSectionClient ... bucketByTopic={bucketByTopic} />` 전달.
- 조회 실패/미매칭 → 기존처럼 '일반' 폴백(graceful).

## 4. 회귀 가드 / 비기능 요건

- 관심기업 카드가 브리핑 카드와 동일한 버킷색 규칙.
- 미매칭 토픽은 중립('일반') — 깨짐 0.
- 다른 뷰(경쟁사·관계지도) 무변경. SQL/hex 0. 미사용 코드 0.

## 5. 검증 (Sonnet 자체)

1. `npx tsc --noEmit` 0 / `npx eslint` 0
2. 관심기업 탭 카드 카테고리 칩이 버킷색(기술=블루·시장정책=앰버·업체=슬레이트)으로 표시
3. 브리핑 탭과 색 규칙 일치
4. 미매칭 토픽 중립 폴백, 신규 hex 0

## 6. 후속 (범위 밖)

- keyword_groups 조회를 공통 헬퍼로 추출(AiInsightsView·entities 중복 제거).

## 7. 라이브 검증 체크리스트 추가분

- [ ] 관심기업 탭 카드 카테고리 칩이 버킷색으로 구분(중립 일변도 해소)
- [ ] 브리핑 탭 카드와 동일한 색 규칙
