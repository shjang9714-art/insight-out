# Insight Out 인수인계서 (2026-07-07)

> 작성: Opus(Cowork). 다음 세션에서 이 문서 + 자동 메모리(roadmap)로 곧바로 이어가기 위한 인계.
> 진행 원장(단일 소스): 자동 메모리 `insight-out-뉴스수집-개선-로드맵.md`(항상 자동 로드됨). 이 문서는 그 스냅샷·요약.

---

## 1. 지금 상태 (2026-07-07)

**직전 대묶음: 어드민 UIUX 통일 + 어드민 2차 기능. 지시서 201~212 전부 커밋 완료.**

배포·확인 완료(커밋):
- 201·202 시스템 설정 화면(어드민 외관 폰트·색·테마 localStorage / 공개 홈 위젯 구성 DB) — **202 SQL 적용됨**(homepage_sections 라이브).
- 203 저대비 컨트롤 글자색 + 기본 폰트 한 단계 축소.
- 204 콘텐츠 테이블 관리 열 오른쪽 고정(sticky) + 강렬한 빨강 톤다운(`--destructive` 어드민 재정의).
- 205 콘텐츠 툴바 재편(카테고리 탭 상단·확대, 풀본문 기간 팝업, 선택바 sticky, 행 순서).
- 206 "비우기" 2종 → 시스템 설정.
- 207 콘텐츠 데이터 관리 페이지 신설(레벨1) — 풀본문·신호·URL·삭제 이관 + 선택바 벌크 삭제.
- 208 대시보드 전환 로딩 스켈레톤(`/admin/loading.tsx`).
- 209 레벨2 인페이지 탭 세그먼트 박스 통일(공유 `AdminTabs`).
- 210 콘텐츠 추가 URL 가져오기(임포트) — 추출→편집→저장.
- 211 썸네일 — 기본 표지(표시 시점 브랜드 커버) + 편집 모달 업로드/교체·제거.

방금 커밋(배포/후속 대기):
- **212 리치 에디터(마크다운)** — 커밋 완료(코드 22428e6 · SQL핸드오프 26015e4).
  - **① 배포 확인 대기**: David가 production `/api/version`으로 22428e6 확인.
  - **② 수희 SQL 대기**: `docs/sql-handoff/212-body-markdown.sql`(`contents.body_markdown` 컬럼). 적용 전에도 42703 graceful(저장·상세 안 깨짐), 적용 후 마크다운 렌더 점등.

→ **어드민 2차 3기능(210 URL 임포트 · 211 썸네일 · 212 리치 에디터) 완료.**
→ **212 마무리(2026-07-08): 수희 `212-body-markdown.sql` 적용 완료 → 마크다운 렌더 라이브 점등.** (코드 게이트 없음 — `body_markdown` non-null이면 상세에서 `ReportMarkdown` 렌더. paste 저장 시 컬럼에 기록.)

### 진행 업데이트 (2026-07-08) — 어드민 개선 슬라이스 213~215

- **213 어드민 테이블 한 줄 정리** — 커밋 `1ea8819`, **production 배포 완료(David 확인).**
  - globals.css `.admin-scope tbody td` 기본 `nowrap` + `.admin-cell-wrap` 예외 클래스. 멀티라인 의도 셀(콘텐츠 제목·이슈 제목+요약·크롤 오류·소스 품질·RequestsBoard 요청/작업 3셀)에 예외 부여. UserManager 이메일·이름, SourceManager 이름 셀 말줄임(+`title`) 보강. 액션 열 불변.
- **214 관리 박스 폰트·위계·패딩** — 커밋 `c5c3c75`, **production 배포 완료(`/api/version` 확인).** (Opus 재현검증 통과 `tsc`·`eslint` 0)
  - globals.css `admin-metric-dashboard` 28→24px(반응형 ≤1439 26→22, ≤1023 22→20), 유닛 16→14, `admin-card-title` 14→15. KPI·오늘할일·운영신호등 박스 `p-5`+`h-full` 통일. AdminOpsSignals·AdminContentHealth 설명 `admin-caption` 정돈. `.admin-metric`(38px) 불변.
- **215 업로드 폼 커버 미리보기·설정** — 커밋 `12f0d75`, **production 배포 완료(`/api/version` 확인).** (Opus 재현검증 통과 `tsc`·`eslint` 0)
  - 211 재사용: `BrandedCover`를 `src/components/dashboard/BrandedCover.tsx`로 분리(ContentCard 시각 불변), `src/lib/contents/upload-cover.ts` 공유 헬퍼(편집모달·PDF표지·신규폼 통합), `CoverImageField`(16:9·objectURL revoke·웹/모바일 미리보기). ReportUploadForm·TextPasteForm 통합, insert 이후 업로드(사용자>PDF자동>기본표지), graceful. URL 탭은 import-url API `thumbnailUrl(og:image)` → TextPasteForm 초기 미리보기. 신규 SQL/컬럼 없음(`contents.thumbnail_url`·`report-covers` 재사용).
- **216 커버 후속 정리** — 커밋 `5e145da`, **production 배포 완료.** (Opus 재현검증 통과 `tsc`·`eslint` 0) 215 나눔점 2건 해소: ① `uploadCoverFile`/`uploadCover` 분리로 편집모달 커버를 저장 시점 기록으로 통일(업로드/제거 대칭), ② 신규 `/api/admin/cover-from-url`로 URL og:image를 `report-covers`에 서버 복사(핫링크 제거, 실패 시 외부 URL 폴백). 하드닝 여지: cover-from-url SSRF(사설 IP 차단) — admin 전용이라 후순위.

- **217 편집모달 마크다운 편집**(어드민 2차 후속 #1) — 커밋 `0f5c6a3`, **production 배포 완료.** (Opus 재현검증 통과 `tsc`·`eslint` 0) 편집모달 본문 평문 textarea → 212 `MarkdownEditor`로 교체. `openEdit`에 `body_markdown` 조회, 시드는 `body_markdown ?? cleanBodyText(htmlToPlainText(body_original))`, 저장 시 `body_markdown`=마크다운·`body_original`=`stripMarkdown` 평문 동기. AdminContentManager.tsx만 수정, SQL 없음.
- **218 생 red-* → 시맨틱 토큰 + AdminErrorBox**(어드민 2차 후속 #2, A안) — 커밋 `6dc2a83`, **production 배포 완료.** (Opus 재현검증 통과: 어드민 red-* grep 잔여 0, `tsc`·`eslint` 0 errors) 어드민 ~90여곳/19파일 생 red-* 전수 교체(오류=negative, 위험·파괴=destructive), 반복 오류박스는 공유 `AdminErrorBox`(= 정식 `border-negative/20 bg-negative-soft text-negative`)로 통일. 실패배지는 `StatusBadge tone="negative"` 재사용. grep 잔여 0 확인. SQL 없음. (코드베이스 기존 관례: negative=오류 메시징, destructive=위험 액션 — §2의 "destructive"를 정확화.)
- **219 크롤 썸네일 og:image 재시도**(어드민 2차 후속 #3) — 커밋 `6dc2a83`, **코드 배포 완료.** ⚠️ **수희 `219-thumbnail-fetched-at.sql` 적용 대기 → 적용 전까지 "219 SQL 적용 필요" degrade(42703 graceful), 적용 후 벌크 op 점등.** (Opus 재현검증 통과 `tsc`·`eslint` 0) 풀본문 백필 패턴 미러 + 216 이미지 복사 로직 공유(`cover-from-image.ts`). content-data에 "썸네일 재시도" 벌크 op 추가. `thumbnail_fetched_at`로 1회 시도 후 제외(무한재시도 방지), 42703 graceful.
> 어드민 2차 후속(#1 편집모달 마크다운 · #2 red 토큰 · #3 og:image 재시도) 지시서 전부 준비 완료.

### 진행 업데이트 (2026-07-08 오후) — 수집/표시 개선 배치 220~222 + 이미지풀 설계

David 결정: near-dup=(b) 본문/의미유사도+대표선정+백필 · 본문최소 250자 완전미수집(어드민 설정) · 생성이미지풀=프롬프트만(David가 생성·삽입) · 유튜브=ContentCard 통일.

- **220 near-dup 성숙화** — 지시서 + SQL(`220-cluster-checked-at.sql`) 작성, **위임 대기.** 삽입 hot-path 유지, 별도 재클러스터링 백필(본문 어휘 유사도 `bodySimilarity` + 대표 신호선정 `pickRepresentativeId` + `cluster_checked_at` 마커). content-data 벌크 op. 의미 임베딩(pgvector)은 Phase C 보류.
- **221 본문 최소길이 + 어드민 설정** — 지시서 + SQL(`221-crawl-settings.sql`, `crawl_settings.min_body_length` 기본 250) 작성, **위임 대기.** 크롤 게이트에 본문 250자 미만 reject 추가, `/admin/settings`에서 조정. 42P01 graceful.
- **222 유튜브 카드 ContentCard 통일** — 지시서 작성, **위임 대기.** ContentCard에 `externalHref`·유튜브 재생 오버레이·video_id 썸네일 폴백 추가, contents(category 유튜브)로 그리드·홈위젯 통일. SQL 없음.
- **④ 생성 이미지 폴백 풀 프롬프트 설계** — 문서 `docs/생성이미지-폴백-프롬프트설계.md`(David가 생성·업로드). 폴백 체인 실제>og:image(219)>생성풀>BrandedCover, 키=엔티티>키워드그룹>카테고리, `topic-covers` 버킷 규약. **셀렉터 배선은 이미지 준비 후 후속 지시서(223 예정).**
- ③ 크롤 시점 og:image 자동수집은 이미지 배치 후속으로 함께 검토.

### 진행 업데이트 (2026-07-08 저녁) — 기업동향 개선(PPT p12~14 분석)

David 첨부 PPT p12~14 = 기업동향 페이지(`/dashboard/entities`, `EntityTabs` watchlist/competitor/briefing) 개선. 결정: 탭을 **관심기업 / 경쟁사(뉴스) / 경쟁사(동향)** 3개로. 브리핑 → AI인사이트 메뉴로 이동. 헤드라인 분석(AI인사이트) → 경쟁사(동향)로 이동 + 경쟁사를 통신사/글로벌빅테크/other 그룹 분리. 관심기업은 마이페이지 자유입력 문제(오타도 등록) → 국내 기업 마스터 검색+수동입력 폴백, 관심기업 기준 콘텐츠 필터 교정, 탭 내 설정(+팝업)·리프레쉬 버튼(마이페이지 미러링).

진단 요지: 인사이트 카드 제목은 클릭불가 `<p>`, "자세히보기"는 빈 토픽으로 감(라우팅 버그). `entities.is_competitor` boolean만 있고 통신사/빅테크 그룹 필드 없음. `user_watchlist.company`는 자유 text(검증 없음)→오타 저장→필터(insight_cards.topic 부분일치)에서 조용히 0건. `entities`+`entity_aliases` 마스터는 있으나 국내기업 시드 미보유. 헤드라인분석=AiInsightBoard `view=headline`(industry scope) InsightCardsSectionClient.

- **223 인사이트 카드 라우팅 수정** — 커밋 `f5e19f7`, **배포 완료.** 공유 헬퍼 `getCardDetailHref`(card-meta.ts) 재사용, 제목/자세히보기 → 근거 콘텐츠 상세. SQL 없음.
- **226 폭 체계 정렬** — 커밋 `d76f6a3`, **배포 완료.** 전역 1152(max-w-6xl), `PageContainer` list(1152)/reading(768) 단일화 + 8개 페이지 버킷 재배치(토픽·브리핑·이슈/엔티티 상세=list, 콘텐츠/리포트 상세·마이페이지·작성=reading). SQL 없음. (Opus 재현검증 통과)
- **227 내비 refined(저대비)** — 커밋 `88219cc`, **배포 완료.**

### 진행 업데이트 (2026-07-09) — 수집품질 배포 + UI 정비 진행

- **228 메뉴 L2 통일·중복헤더·폰트** — **배포 완료**(`1a82ebf`).
- **220 near-dup 재클러스터링 백필** — **코드 배포 완료 + 수희 SQL 적용 완료(2026-07-09).** (Opus 재현검증 통과)
- **221 본문 최소길이 미수집 + 어드민 설정** — **코드 배포 완료 + 수희 SQL 적용 완료(2026-07-09).** (Opus 재현검증 통과)
- **229 ContentListCard 커버 폴백** — **배포 완료.** 리서치 등 썸네일 없는 카드에 BrandedCover 표지.
- **수희 SQL 완료(2026-07-09)**: `219`·`220`·`221`·`237`·`238`·`241` · `246`(경쟁사그룹, 224 대체·컬럼 자체생성) · `225`(watchlist entity_id FK) · `251`(briefing·key_insight 라우팅 안정화) · `239`+`240`(도메인Hook·자동승인·admin역할, 적용 확인) — **전부 적용 완료.**
- **수희 SQL 적용 대기**: 없음. **`253-curated-companies-그룹-프롬프트.sql` 적용 완료(2026-07-09)** — curated_groups(12)·curated_companies(40, 경쟁사16)·llm_prompts(company_insight 한글). 검증: cp_telecom3·cp_aidc4·cp_cloud4·cp_sidx4·cp_security4. → 주요기업(254/255)·경쟁사 동향 리팩터 토대 완성.
- **⏳ 마지막 한 스텝**: 어드민 `/admin/insights` → **"위기·기회 분석 실행"** 백필 1회 → 경쟁사 최근 뉴스에 위기/기회/관망 칩 채워짐. (그룹박스는 224 SQL로 이미 점등)
- **위임 대기**: 230 내비(센터정렬·긴줄제거·타이포 17/15) · 231 성능 즉효(loading.tsx·쿼리병렬·body_original 제거).
- **미작성/결정 대기**: 224 기업동향 탭 재구성 · 225 관심기업 마스터 · 232 성능 후속(콘텐츠 SSR·미들웨어) · ④ 생성이미지 셀렉터 · ③ 크롤 og:image 자동수집.
- **환경변수 확인(코드 아님)**: LLM 키 Vercel Production·Preview 등록 완료 — GEMINI/GROQ/CEREBRAS(2키)/OPENROUTER/**SAMBANOVA/MISTRAL**_API_KEYS. TTS(GCP_TTS_SA_KEY_BASE64) 미확인(다음).
- **239 로그인 회사이메일 OTP + 자동승인 + 프리미엄 재디자인** — **[백로그·상의 필요]** 지시서·SQL·목업 작성 완료, 보류(상의 후 진행). David 결정: 회사 이메일이 Google 아님(별도 사내 계정) → **@lguplus.co.kr 6자리 OTP** 로그인(Google·비번 제거), Before User Created Hook으로 도메인 제한, **도메인 검증 시 자동 승인**(기존 approval_status 수동승인 폐지). (1) `239-로그인-회사이메일-OTP-프리미엄.md`(login/page.tsx 2단계 OTP + 애니메이션 프리미엄 UI, 실로고 유지), (2) `239-로그인-도메인hook-자동승인.sql`(Hook 함수+예외 allowlist[yjhead@gmail.com 등 admin]+handle_new_user 자동승인). ⚠ David 대시보드 마무리: Auth→Hooks에 Before-user-created 함수 등록 + 이메일 템플릿 `{{ .Token }}` 추가(6자리 코드 표시). 목업: `docs/mockups/login-premium-mockup.html`(이메일-퍼스트 2단계). 기존계정·admin 락아웃 방지(Hook은 신규생성에만 발동).
  - **⚠⚠ OTP 실발송 = SMTP 필수(239에서 누락 발견, 2026-07-09)**: OTP 메일은 **Supabase Auth가 발송**(앱 코드 아님). 기본 내장메일은 시간당 소수 제한(프로덕션 불가) → **Auth→SMTP Settings에 Brevo SMTP 릴레이(`smtp-relay.brevo.com`) 연결 필수**(앱은 이미 Brevo 사용 `lib/email/brevo.ts`, 단 Auth SMTP는 별개 키). + 발신 도메인 SPF/DKIM. 이거 없으면 로그인 UI 완성돼도 코드 안 날아감.
  - **248 로그인 통합 — [코드 배포 `77d6645`]** — BulbScene(v3 애니메이션)·LoginCard(OTP 2단계·관리자 Google)·login/page·globals.css 구현·검증·커밋(로그인 세션 실브라우저 검증 포함). ⚠ **실발송은 대시보드 설정 남음**(Brevo SMTP·템플릿·도메인 Hook — `로그인-Supabase대시보드-설정가이드.md`). 지시서 `248-로그인-통합-화면-OTP-발송.md`. 239(OTP·Hook·자동승인)+240(관리자Google)+247(전구히어로)+발송설정을 **하나로 통합**한 자립형 구현 지시서. 코드=login 3파일(page/BulbScene/LoginCard), DB=239·240 SQL, 대시보드=`로그인-Supabase대시보드-설정가이드.md`(Brevo SMTP+OTP템플릿`이메일템플릿-OTP-magiclink.html`+Hook+rate limit+SPF/DKIM). 히어로 SVG는 v3 목업 포팅.
  - **247 로그인 전구 히어로(목업 v3 애니메이션)** — 로그인 세션 WIP 마무리·검증 지시서(248에 통합됨).
  - **256 로그인 헤드라인 상단고정 + 전구·카드 세로중앙 — [배포 완료]** → **262로 큰 화면 보완.**
  - **262 로그인 큰 화면 무스크롤 — [재현검증 통과, 커밋 대기]** 씬 폭 `w-[min(107%,87vh)]`(좁은화면 %상한·넓은화면 vh상한 중 작은쪽)로 aspect 세로넘침 원천차단. 5뷰포트(1280×800~2560×1440) 무스크롤 실측. 헤드라인 좌상단 유지. header를 그리드 밖 상단 pin, flex-1 items-center로 전구+카드 중앙, 씬 폭 반응형(1280~ 107%/2xl 88%). 3뷰포트 무스크롤 실측. `256-로그인-헤드라인상단-전구카드중앙.md`(250 대체).
  - **250 로그인 무스크롤 상단정렬 — [배포 `966e473`, 256으로 조정됨]** — main/그리드 items-start·패딩축소·전구씬 축소상향(w153→122%, -mt28→44%)·카드fallback 560. 3뷰포트(1280×800·1440×900·1536×864) 세로스크롤 0 실측. `250-로그인-무스크롤-상단정렬.md`. `247-로그인-전구히어로-목업v3.md`(원래 "246"으로 작성됐으나 246=경쟁사그룹정정SQL 선점 → **247 교정**). BulbScene(SMIL orb/comet/twinkle) + 필드 확장 컴포지션, LoginCard(239·240) 유지. 로그인 세션 워킹트리 기준.
- **240 관리자 Google 로그인(보조 진입) + admin 역할 자동부여** — **[백로그·상의 필요]** 지시서·SQL 작성 완료, 239와 함께 보류(상의 후 진행). David 결정 ①안: 관리자 gmail을 그대로 관리자 계정으로 사용(연결 없음). 로그인 화면에 **저강조 "관리자 Google 로그인"** 보조 진입(OTP가 메인). 관리자 gmail은 239 allowlist에 있어 Hook 통과 → Google 직접 로그인, 일반 직원 gmail은 Hook 차단(관리자 외 Google 가입 자동 불가). (1) `240-관리자-google-로그인-보조진입.md`(login/page.tsx에 보조 진입, 239 기반), (2) `240-관리자-google-admin역할.sql`(allowlist `is_admin` 플래그 + `handle_new_user` 최종판=자동승인+admin 역할, **239 SQL 뒤에 적용**). Manual Linking 불필요. 세션: 쿠키 기반 지속(퇴근/출근 재로그인 없음, 로그아웃/새기기/만료 시에만 재인증).
- **238 LLM provider 확장(SambaNova·Mistral) + fast-failover/쿨다운** — **[커밋 완료 `e7a2214`, 배포 확인 중]** Opus 재현검증 통과. 배포 완료 + **238 SQL 적용 완료(2026-07-09)** — 라우팅에 sambanova/mistral 편입 + Cerebras 30M. **LLM 완료** — 분류 부하가 여러 provider로 분산·복구.
- **237 contents 성능 인덱스 SQL** — **[커밋 완료 `ac228b9`]** docs 핸드오프(CONCURRENTLY 제거판). 수희 적용 완료(2026-07-09). 진단(실측 /api/admin/llm 2026-07-09): 분류 주력 **Cerebras 자체 월캡 1M 100% 소진**(1,003,103) + Groq 무키였음(→키 등록) → 부하가 Gemini(무료 RPM 빡빡)로 몰려 간헐 실패. 처방: (1) `openai-compat`에 sambanova/mistral provider 2종 추가 + **429/401 fast-failover·provider 3분 쿨다운**(외부 소진 시 재시도 낭비 제거, David 결정 포함)(`238-...md`), (2) `238-llm-provider-라우팅-한도.sql` — 라우팅 전체 재구성(분류: cerebras→sambanova→groq→gemini→openrouter→mistral 등) + **Cerebras 월한도 1M→30M 상향** + 신규 provider 설정/모델 카탈로그. env는 David 등록 완료. ⚠ 순서: 코드 배포(Sonnet) → 238 SQL(수희) → `/admin/llm` 라이브 체크.
- UI 톤/폭/성능 상세: `docs/작업계획-UI톤-레이아웃-2026-07-08.md`. `--brand-muted` 토큰(#A83E63/#C76A8A), L1 좌측정렬+무트 언더라인, "오늘 업데이트" 톤다운, 공유 `InsightViewTabs`(지속형 L2)로 AiInsightBoard pill 교체. 유틸바·로고 유지. SQL 없음. (Opus 재현검증 통과) ※ UI 톤 원칙·폭체계는 `docs/작업계획-UI톤-레이아웃-2026-07-08.md` 참조.
- **241/242/243 경쟁사(뉴스) 그룹핑 + 위기/기회(LG U+ 관점)** — **[전부 배포·재현검증 통과: 241 `c35077a`, 242 `d04e2ae`, 243 `077a4c1`. 224·241 SQL 완료.]** 남은 것=어드민 위기·기회 백필 1회. — David 피드백(경쟁사동향=전 경쟁사·등록무관[이미 그러함, 224 SQL로 점등] / 경쟁사뉴스도 그룹핑 / 긍정·부정→위기·기회 라벨[v2 LLM]). **241**(백엔드+SQL): `contents.lgu_impact`(위기/기회/관망) 컬럼 + `classifyLguImpact`(sentiment.ts 미러, LG U+ 관점 LLM 분류) + 백필 + 어드민 트리거. `241-...md` + `241-lgu-impact-위기기회.sql`. **242**(프론트): 경쟁사(뉴스)를 competitor_group 그룹박스+회사카드 그리드(레이아웃 A, 목업 `docs/mockups/경쟁사뉴스-그룹레이아웃-목업.html`) + sentiment→lgu_impact 칩. `242-...md`. 순서: 224SQL+241(SQL·백엔드·백필) → 242 프론트. **241 코드 재현검증 통과(2026-07-09, Opus 4파일 확인)** — 커밋·배포 후 241 SQL(수희) + 어드민 "위기·기회 분석 실행" 백필. 트리거 위치=`admin/insights/page.tsx`. 둘 다 42703 graceful(코드 선배포 가능). **242 추가 반영**: 탭 이름 경쟁사(뉴스)→**경쟁사 최근 뉴스**, 경쟁사(동향)→**경쟁사 동향분석**; 최근 뉴스에 기간(최근 14일·기사 날짜) 표시; 회사 카드 **가로 배치**(그룹 내 가로 스크롤). **243**(`243-...md`): 경쟁사 동향분석도 최근 뉴스와 **동일 그룹박스 양식**(InsightCardsSectionClient `boxed` 옵트인, 기존 사용처 무영향). **경쟁사 분류(그룹 지정)는 224 EntityManager competitor_group 필드로 이미 어드민에서 가능.**
- **224 기업동향 3탭 재구성 + 경쟁사(동향)** — **[커밋 완료 `147322d`, 배포 확인 중]** 재현검증 통과. 배포 확인 후 → **224 SQL(수희)** → 라이브. ⚠라이브 QA: 짧은 topic 부분매칭 오탐 관찰. 지시서·SQL 작성 완료. 결정 반영: 탭=관심기업/경쟁사(뉴스)/경쟁사(동향), 경쟁사(동향)=헤드라인분석 insight_cards를 `competitor_group`(통신/클라우드·플랫폼/빅테크)으로 재그룹핑(새 AI 생성 없음), 브리핑 탭 제거. (1) `224-기업동향-3탭재구성-경쟁사동향.md`(EntityTabs 3탭 + InsightCardsSectionClient에 `label` 그룹 지원 + entities/page trend 뷰 + EntityManager competitor_group 편집), (2) `224-경쟁사-competitor-group.sql`(컬럼+백필+is_competitor 보정, 어드민 편집 가능). 그룹값은 어드민에서 추후 변경 가능.
- **224B 브리핑→AI인사이트 "키워드 분석" 탭** — **[커밋 완료 `147322d`, 배포 확인 중]** AiInsightsView(entity_signal 패치)·AiInsightBoard(맨끝 탭·렌더)·issues/page(view=keyword 검증) OK. SQL 없음. AI인사이트 페이지=`/dashboard/issues`. `224B-브리핑-AI인사이트-키워드분석탭.md`. 224에서 제거한 entity_signal 브리핑을 AI인사이트 L2 **맨 끝 "키워드 분석"** 탭으로 이동(AiInsightsView 패치 + AiInsightBoard 탭/뷰 추가 + 페이지 view 검증에 'keyword'). SQL 없음, 프론트 온리. David 지정: 이름 "키워드 분석", 위치 마지막.
- **225 관심기업 마스터검색+수동입력+인탭설정+필터교정**(예정) — 기업 검색 API·자동완성, user_watchlist.entity_id FK, 콘텐츠 필터 엔티티기준 교정, 공유 설정 모달+리프레쉬. *결정 대기: 국내기업 마스터 소스/범위.*

> 213~215는 §2 "어드민 2차 후속"의 실행분. 커버(215)는 §2의 "붙여넣기·URL 임포트 생성 직후 썸네일 지정"을 포함·확장.
> ⚠️ Cowork 자동 메모리(`memory.md`)는 이 세션에서 읽기전용이라 갱신 불가 — 진행 원장은 당분간 이 git 문서 기준.

### 246 경쟁사 그룹 정정 백필 SQL — [적용 완료 2026-07-09]
- 결과: 통신5·클라우드·플랫폼4·빅테크6 = 15 경쟁사 그룹핑, LG유플러스/AX/AI전환 경쟁사 제외. ⚠224 SQL이 실제 미적용이라 competitor_group 컬럼이 없었고 246 첫 실행 42703 실패 → 246에 컬럼 생성(0단계) 자체포함해 재실행 성공. (경쟁사 최근뉴스/동향분석 그룹박스 점등)
- 라이브 실측(/admin/entities)으로 "기타 경쟁사" 원인 규명: 224 백필 일부 미반영 + 노이즈(AX·AI전환) 경쟁사 오플래그 + **LG유플러스(자사) 경쟁사 오플래그** + 빅테크 미편입. `246-경쟁사-그룹-정정백필.sql`: 통신(KT·SKT·세종텔레콤·SK브로드밴드·KT AI)/클라우드·플랫폼(네이버클라우드·카카오엔터프라이즈·NHN Cloud·KT클라우드)/빅테크(AWS·MS·Google Cloud·Oracle·NVIDIA·Azure) 명시 지정 + AX·AI전환·LG유플러스 is_competitor=false. 멱등. 판단콜: KT AI→통신, KT클라우드→클라우드·플랫폼, 빅테크 편입.

### 245 경쟁사 최근뉴스 3×2 그리드 + 전체보기 페이지 — [배포 `e4b497b`, 재현검증 통과]
- 공유 헬퍼 `lib/entities/competitor-news.ts` + `CompetitorNewsGroups` 컴포넌트 추출, 요약 3×2(cap6)+전체보기, 신규 `/dashboard/entities/competitor-news`. Opus 확인 OK.

### 245 경쟁사 최근뉴스 3×2 그리드 + 전체보기 페이지(구) — [위임 대기]
- David 라이브 피드백: 그룹 내부 가로스크롤→**3열2행(6개) 그리드**, **전체보기→전용 전체 경쟁사 뉴스 페이지**(`/dashboard/entities/competitor-news`). `245-...md`. 242 그룹박스/칩 재사용, 공유 컴포넌트 추출. SQL 없음. ⚠225와 같은 `entities/page.tsx` 수정 → 225 커밋 후 진행. **별개 데이터 이슈**: 경쟁사들이 "기타 경쟁사"로 몰림(224 백필 이름 불일치) → 어드민 EntityManager competitor_group 지정 필요.

### 225 관심기업 마스터검색 + 엔티티FK + 필터교정 — [배포 `defc64c`, 재현검증 통과]
- 검색API·WatchlistManager(로드·insert 42703 graceful)·모달·마이페이지/인탭·lens watchlistEntityIds 확인 OK. ⏳ **225 SQL(entity_id FK)** 수희 적용 대기 — 적용 전엔 문자열 저장, 후엔 신규부터 FK 자동연결+기존 마이그레이션. 범위밖(명시): 관심기업 insight_cards는 topic 문자열 유지.

### 225 관심기업 마스터검색 + 엔티티FK + 필터교정(구) — [위임 대기]
- David 결정 A/C: entities 마스터 + 수동 add + `user_watchlist.entity_id` FK(DART 임포트 미채택). `225-관심기업-마스터검색-엔티티FK-필터교정.md` + `225-watchlist-entity-fk.sql`(FK+name/alias 백필). 기업 검색 API + 공유 `WatchlistSettingsModal`(자동완성·수동입력) + 마이페이지·기업동향 인탭 통합 + 필터 엔티티기준 교정(entity_id 우선·문자열 폴백). 42703 graceful. 후속: 전 렌즈 완전 엔티티화.

### 263/264 어드민 도움말 · 유튜브 2차 — [위임 대기]
- **263 어드민 도움말**: 기능마다 한줄설명+정보아이콘(InfoHelp) 도움말(무엇·서비스연계·운영). 문안 §2 제공. `263-...md`.
- **264 유튜브 2차**: 유튜브칩 옆 해시태그(엔티티), 세로영상 우측 여백에 분석/요약 패널, 하단 추천기사·추천유튜브 연관. 자막 스크립트는 후속(자막 미수집). `264-...md`.
- 참고: **#3 해시태그→기사모아보기 이미 있음**(`/dashboard/topics/[topic]` 실존, 엔티티는 `/dashboard/entities/[id]`).

### 252 유튜브 인앱 상세 + 임베드 + 해시태그/엔티티 태깅 — [배포 `009383d`, 재현검증 통과]
- 임베드(youtube-nocookie+폴백)·라우팅(3카드 인앱)·유튜브 태깅 파이프라인(tagYoutubeContent 신규수집 자동 + `/admin/insights` "유튜브 태깅 백필" 버튼 기존영상)·CSP없음 확인. ⏳ **어드민에서 유튜브 태깅 백필 실행**해야 기존 영상에 해시태그·관련기사 채워짐.

### 252 유튜브 인앱 상세 + 임베드 플레이어(구) — [위임 대기]
- David: 유튜브 클릭 시 youtube.com 새탭 이탈 대신 우리 시스템 상세로 "한번 더 들어가서" 인앱 재생(A안). 유튜브=contents(category='유튜브', original_url=watch), 상세=`/dashboard/contents/[id]`, `extractVideoId`로 ID 확보. `252-...md`: 유튜브 카드 라우팅 외부→내부 상세(ContentCard/ContentListCard) + 상세에 youtube-nocookie iframe(16:9)+메타+임베드차단 폴백링크 + CSP frame-src 확인. **추가(David)**: 유튜브 해시태그(엔티티) 태깅 — 유튜브가 분류(matched_groups/content_entities)를 안 타면(크롤러 별도경로) 분류·엔티티 파이프라인 적용+백필(선결) → 그래야 해시태그·관련기사 채워짐. 관련 기사 캐러셀은 content 상세에 이미 존재(FeedCarousel)라 라우팅으로 자동 상속(단 분류 선결). SQL 없음(태깅은 코드+백필).

### 주요 기업 큐레이션 후속 (258·259·260) — 진행
- **258 즉효 — [배포 `17ef6b5`, 재현검증 통과]**: 회사생성 window 7→90(COMPANY_INSIGHT_WINDOW_DAYS)·articlesPerCompany 12·minArticles 1·`insightCompanyAutoPublish`(인용≥1·출처≥1, 회사 카드만)·설정모달 독립에러+너비. → David 재트리거로 다수 회사 published 예상.
- **259 회사별 타깃 수집 — [배포 `a459922`, 재현검증 통과]**: `crawlCompanySearch`(curated name+aliases seed→`crawlKeywordSearch`/processCrawlItem 재사용, 20s예산+날짜회전, 42703 graceful). 니치 회사 코퍼스 확충. 크롤 1회전 후 지니언스·메가존 등 수집 확인.
- **260 설정=큐레이션 선택 — [배포 완료, 재현검증 통과(코드)]**: 자유검색/직접추가 제거, 7그룹 회사 칩/체크 선택 전용. ⚠253 curated 테이블 RLS 미설정(현재 열림, 보안 후속).
- **257 경쟁사 동향 리팩터 — [폐기]** — David: per-company 카드는 중복 심하고 주요기업과 차별화 약함 → **261(주간 리포트)로 대체.**
- **261 주간 경쟁사 동향 리포트 — [위임 대기]** — 경쟁사 동향=per-company 카드→**주 1회 종합 리포트**(사업영역별 종합+위기/기회 칩+타임라인). LLM은 검색 안 함(우리가 기사검색·영역분류[matched_groups]·위기기회[241] 전처리, LLM은 영역별 종합만). `261-주간-경쟁사동향-리포트.md`+`261-competitor-weekly.sql`(테이블+프롬프트 2종). 프롬프트 어드민 편집(llm_prompts). 통신3사 중심·주간 볼륨 OK. 260 완료.

### 254/255 주요 기업(구 관심기업) — [배포 `49fc929`/`02ee603`, 재현검증 통과]
- 254 생성백엔드: curated_companies 소스(42P01→user_watchlist graceful)+llm_prompts DB프롬프트(한글, 폴백)+`summarize` 태스크(gemini/한국어)+deadline. 255 UI: 탭 "주요 기업"·7그룹 계층(대표3~5 사용자선택/이슈중요도top5)·풍부카드(해시태그·시사점3·근거3)·골드 반짝 테두리·`/dashboard/entities/major/[group]` 전체보기·WatchlistManager curated 선택. ⏳ **David: `/admin/insights`에서 생성 1회전 트리거** → 41개사 한글 카드 확인.
- 257 경쟁사 동향 리팩터(curated 5그룹·위기기회 오버레이·always/on_issue) 지시서 준비완료 — 255 배포됐으니 위임 가능.

### 249 관심기업 회사별 카드그리드 + 동향 상세페이지 — [배포 `7a2982b`, 재현검증 통과]
- 회사별 카드 그리드 + `/dashboard/insights/[id]` 상세(근거 인용문+출처기사) + getCardDetailHref 교정(id옵셔널·후방호환, AI인사이트/동향분석 파급). user_services 실존 확인. **소폴리시(비블로커)**: industry 카드 상세의 뒤로가기 fallback이 `/dashboard/ai-analysis`(AI분석 콘텐츠) — 직접진입 시만. 원하면 scope별 fallback(company→관심기업, 그외→`/dashboard/issues`)로 다듬기. — [설계]
- David 피드백: 관심기업 "자세히 보기"가 기사1건으로 점프=기업동향 아닌 최신뉴스. AI요약·근거(insight_cards scope=company)는 이미 있으나 **카드 상세페이지가 없어** 콘텐츠로 떨어짐. 결정: **회사별 카드 그리드**(기간그룹 폐지·회사당 최신1개·날짜라벨)·**자세히버튼 제거→카드전체 클릭**·**신규 `/dashboard/insights/[id]` 상세**(핵심·시사점 전문+근거 인용문/출처기사)·`getCardDetailHref` 교정(공유→AI인사이트/동향분석도 개선). **1단계 프론트만**(생성·SQL 무변경). 2단계: 다중전개 요약·온디맨드생성·종합뷰. `249-...md`, 설계 `설계-관심기업-동향상세-재구상.md`.

### 244 생성이미지 커버 셀렉터 — [위임 대기]
- `244-생성이미지-커버셀렉터-어드민.md` + `244-topic-cover-images.sql`. `topic_cover_images` 테이블 + `topic-covers` 버킷 + 어드민 "토픽 커버 이미지" 업로드/배정(엔티티/그룹/카테고리) + 셀렉터 헬퍼(`lib/contents/cover-pool.ts`, 엔티티→그룹→카테고리 해시회전) + 콘텐츠 목록(v1) 통합. 폴백체인: thumbnail_url→풀→BrandedCover. 42P01 graceful. David 이미지는 `docs/이미지-프롬프트-라이브러리-나노바나나.md`로 생성 중. 후속: 피드·엔티티상세·검색에 확대.

### TTS 진단 (2026-07-09, 라이브 확인)
- **TTS 미작동 = env 미설정 유력.** /admin/briefings 실측: 2026-07 TTS 사용량 **0/900,000자**, 6월부터 **전 브리핑 "오디오 없음"** → 한 번도 성공한 적 없음. 사용량 0이라 한도초과 아님. 코드(`synthesize-briefing.ts`) 정상 → `GCP_TTS_SA_KEY_BASE64` 미설정 시 즉시 throw. **처방**: GCP 서비스계정 JSON→base64→Vercel `GCP_TTS_SA_KEY_BASE64` + GCP Cloud Text-to-Speech API 활성화. **확정**: 브리핑 "오디오 생성" 클릭 시 메시지("설정 오류"=env미설정 / "합성 중 오류"=인증·API문제)로 판별.
- **[진단완료] 브리핑 생성 실패** — 7/8·7/9 실패(`llmCompleteDetailed('briefing')` null → status=failed, error_reason="openrouter: 응답 없음"). 원인: briefing 체인 gemini(1)→openrouter(2)뿐이라 둘 다 실패한 날 폴백 소진. **처방 `251-briefing-라우팅-안정화.sql`(수희)**: briefing·key_insight를 gemini→sambanova→cerebras→openrouter로 재구성(무료 openrouter 강등). 적용 후 실패 브리핑 **재생성**하면 성공 가능성 큼. (238이 sambanova 폴백 추가했으나 openrouter가 2순위라 매번 먼저 시도 → 251로 순서 교정.)

## 1.9 성능 백로그 (2026-07-09 실측 근거)

- **[대형·보류] Supabase 서울 이전(ap-northeast-2) + Vercel `icn1`** — 현재 Supabase=시드니(ap-southeast-2), 사용자=한국. Vercel 함수를 syd1로 co-location(236 배포됨)해 홈 스트리밍 2.8초→1.7초(−37%)까지 개선. 남은 1.7초의 바닥 = **한국↔시드니 거리(~130ms/왕복, 고정)**. 완전 해결은 Supabase를 서울로 이전해 사용자·함수·DB 3자 근접시키는 것뿐(→ 500ms 이하 예상). **단 프로젝트 신설+데이터/Storage/Auth 이전 + 다운타임 컷오버 = 리스크 큼(특히 Auth 로그인).** 요금제 업그레이드로는 리전 이전 불가(Read Replica는 REST 앱엔 효과 불확실). **급하지 않음(리전 지연은 데이터 증가와 무관·불변). 사용자/데이터 적을수록 이전 리스크 낮으니 "할 거면 이를수록 쉬움". 런북 준비 후 점검 창에 실행.**
- **[중간] 인덱스 점검** — 데이터 증가 시 느려지는 건 리전 아니라 인덱스 없는 hot 쿼리. (아래 진행)
- **[소] RSC 프리페치 축소·홈 섹션 정리** — 체감 소폭.
- **[소·후속 · 수희/나중]** 카드 좌측 컬러 스트라이프 제거 — `InsightBriefCard.tsx`(border-l-4 brand-600), `InsightCardNewsList.tsx`(border-l-[3px]) 등의 마젠타 border-l accent가 프리미엄감 깎음(단면 컬러바 안티패턴). 은은한 배경틴트/그림자/도트로 대체. 내비 톤 통일. **→ David: 나중에 수희가 처리.**
- **[완료·실측] 236 인증 /dashboard 측정**(David 세션 브라우저): 스트리밍 2.8초→1.7초(−37%), load 2.3초. x-vercel-id icn1::syd1:: 확인.
- **[완료] 내비 프리미엄(227~235)**: 좌측 L1·L2 그룹중앙·긴줄제거·잉크대비·활성 브랜드 도트·17/15·Pretendard. 라이브 시각 QA 통과.

## 2. 다음 세션 후보

수희/배포 마무리:
- 212 배포 확인 + 수희 212 SQL 적용 → 마크다운 렌더 라이브 검증(붙여넣기 서식→상세 렌더).

어드민 2차 후속(선택):
- 편집 모달에서 기존 콘텐츠 마크다운 서식 편집(212 MarkdownEditor 재사용).
- 붙여넣기·URL 임포트 생성 직후 썸네일 지정, 크롤 og:image 실패분 재시도.
- UrlImportForm 오류 박스 생 `red-*` → destructive 토큰 정리(211/203/204 정합).

파킹(오래 열려 있음):
- **뉴스 near-dup 클러스터링**: 같은 사건 다매체 기사 묶기(canonical 196 위). 구 197 파킹분.
- **밀린 라이브 검증**: 제외규칙 효과(hit_count)·자동 제외 후보·canonical 병합을 "지금 수집" 1회로 실제 점등 확인(194~196 배포됨, 데이터 검증만 남음).

## 3. ⭐ 작업 루틴 (협업 루프) — 꼭 지킬 것

역할:
- **David**: 방향·의사결정. Sonnet 위임/커밋 트리거. 배포 확인(Vercel 접근 없음 → `/api/version`). 수희에게 SQL 전달.
- **Opus(Cowork, = 이 에이전트)**: 설계·지시서 작성(로컬, **커밋 안 함**)·재현검증·메모리 갱신. 코드 직접 수정은 하지 않고 지시서로 위임.
- **Sonnet(Claude Code)**: 지시서대로 구현·`tsc`/`build`/`lint`·커밋·푸시.
- **수희**: Supabase에 SQL 핸드오프 적용(Opus/Sonnet는 DB 직접 못 건드림).

루프(1슬라이스):
1. Opus가 요구를 조사(코드 확인)하고 필요한 결정을 David에게 확인.
2. Opus가 지시서를 `docs/sonnet-지시서/NNN-*.md`에 작성(로컬, 커밋 X). SQL 있으면 `docs/sql-handoff/NNN-*.sql`도. **SQL 핸드오프는 먼저 푸시**(수희 적용).
3. David가 Sonnet에 위임(Opus가 **복사용 코드블록**으로 위임문 제공).
4. Sonnet 구현 + 자체 검증(tsc·eslint 0, build) → David에게 완료 보고.
5. **Opus가 재현검증**(실물 diff 확인 + `npx tsc --noEmit`·`npx eslint` 재실행 + 회귀·토큰·graceful 점검). 통과 시 David에게 **커밋용 코드블록** 제시.
6. David가 "커밋" 트리거 → Sonnet 커밋·푸시·`/api/version`.
7. David가 배포 확인 보고 → **Opus가 메모리 갱신**.

규약:
- 완료 보고 타이틀은 "지시서 NN — <지시내용>".
- Sonnet 위임/커밋 지침은 **복사용 코드블록**으로 전달.
- 지시서엔 항상: 현행 진단(검증된 코드 사실) / 구현 / 회귀 가드 / 검증 / 라이브 체크리스트.
- **SQL은 42703 graceful 패턴**(컬럼 미적용에도 안 깨지게) — SQL 선적용 없이도 배포 안전하게.
- 배포 순서상 파괴적/이관은 "기능이 중간에 사라지지 않게" 순서 배치(예: 206 먼저).

## 4. 핵심 컨텍스트·규약

- 대상 사용자: 한국 직장인. **UI·기능은 항상 비전문 사용자 기준**(전문용어 지양, 빈 상태 안내). UI 텍스트 한국어.
- 어드민 테마: `.admin-scope` + `data-admin-theme`로 전역과 독립. 색은 **CSS 변수/토큰만**(생 hex·`red-*` 지양). 다크/라이트 양쪽 대비 확인.
- 어드민 폰트 스케일: `--admin-font-scale`(201) — `.admin-*` 클래스가 `calc(px * var(--admin-font-scale,1))`.
- 레벨2 인페이지 탭: 공유 `src/components/admin/ui/AdminTabs.tsx`(세그먼트 박스) 사용. 카운트 필터는 `AdminFilterChip`(역할 구분).
- 마크다운 렌더: `src/components/reports/ReportMarkdown.tsx`(react-markdown, raw HTML 미렌더=안전). 신규 마크다운 UI는 이걸 재사용.
- 콘텐츠 데이터 일괄 작업(풀본문·신호분류·URL정규화·삭제·초기화)은 **시스템 > 콘텐츠 데이터 관리**(`/admin/content-data`)에 모여 있음. 콘텐츠 관리 화면엔 없음.
- 기록 방침: 단일 소스 = git + 자동 메모리. (Notion 중단.)
- 파일 위치: 지시서 `docs/sonnet-지시서/`, SQL `docs/sql-handoff/`.

## 5. 바로 쓰는 첫 메시지(다음 세션)

David가 다음 중 하나로 시작하면 됨:
- "212 배포 확인됐어, 수희도 SQL 적용함 → 마크다운 라이브 검증하자"
- "near-dup 클러스터링 설계하자"
- "밀린 라이브 검증(제외규칙·canonical) 하자"
- 또는 새 지시.

---
## 2026-07-10 추가
- **261 재현검증 통과(Opus)**: `lib/competitor-weekly/generate.ts` — 검색·경쟁사필터·사업영역 버킷팅은 우리가 판정, LLM은 종합만(`summarize`). 인용 picked id셋 검증, 데드라인 budget, 42703(lgu_impact)·42P01(테이블) graceful, week_start 멱등 upsert. `entities/page.tsx` trend 뷰 = CompetitorWeeklyReport+Timeline, 빈결과 graceful. tsc/eslint/build 0(Sonnet). → **코드 커밋 OK**.
- **261 SQL(`sql-handoff/261-competitor-weekly.sql`) → 수희 실행 필요**. 미실행 시 trend 탭 빈 상태로 graceful. 코드/SQL 독립 → 병렬 진행 가능.
- **265 지시서 작성**(유튜브 자막 수집+번역 스크립트). 265 SQL은 contents.transcript/transcript_ko/transcript_lang 컬럼(별도).
- 위임 대기(Sonnet): 262(로그인 vh-cap 확인/배포)·263·264·265.
