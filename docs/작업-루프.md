# 🔁 작업 루프 — Opus · Sonnet · David 역할분담

> 목적: 세 주체의 작업을 **명확히 분리**하고, **복붙 가능한 형태**로 주고받는 협업 루프 정의
> 운영: Cowork(Opus) 가 설계·지시·검증, Claude Code(Sonnet) 가 구현, David 가 위임·머지
> 작성: 2026-05-30

---

## 1. 역할 분리 (한 줄씩)

| 주체 | 한 줄 정의 | 산출물 |
|---|---|---|
| **Opus** (Cowork=나) | 판단·설계·**Sonnet 지시서 작성**·**결과 검증** | 스키마/아키텍처 설계, `docs/sonnet-지시서/*.md`, 검증 리포트 |
| **Sonnet** (Claude Code) | 지시서대로 **구현·PR** | 코드, 테스트, 완료 보고 |
| **David** (나=사용자) | Sonnet 에 **위임**, PR **리뷰·머지**, git **push** | 머지된 코드 |

원칙: **Opus 는 코드를 직접 많이 쓰지 않는다.** 설계와 지시서·검증에 집중하고 구현은 Sonnet 에 위임한다. (단, DB 스키마처럼 설계=산출물인 경우는 Opus 가 직접 작성)

---

## 2. 루프 (1 사이클)

```
① Opus  →  지시서 작성        docs/sonnet-지시서/NN-제목.md  (복붙형)
② David →  Sonnet 에 위임      Claude Code 에 한 줄 붙여넣기
③ Sonnet→  구현 + 완료 보고     "무엇을 했는지 + 결과 보고" (§4 양식)
④ David →  보고를 Opus 에 전달   Cowork 로 복붙
⑤ Opus  →  검증 + 판정         ✅통과 / 🔧수정요청(다음 지시서) 
⑥ 통과 시 David 머지 → 다음 사이클
```

핵심: **③ 완료 보고**와 **⑤ Opus 검증**이 매 사이클 빠지지 않는다. 보고 없이 머지 금지.

---

## 3. David 가 쓰는 복붙 명령

### 3.1 Sonnet 에 위임 (Claude Code 에 붙여넣기)
```
docs/sonnet-지시서/01-타입-동기화.md 의 지시서대로 작업해줘.
완료되면 그 안의 "완료 보고 양식"에 맞춰 보고해줘.
```

### 3.2 Sonnet 보고를 Opus 에 전달 (Cowork 에 붙여넣기)
```
Sonnet 작업 완료 보고다. 검증해줘:
[Sonnet 보고 내용 붙여넣기]
```

### 3.3 git (커밋·푸시는 David 가 본인 터미널에서)
```bash
cd ~/Documents/insight-out/insight-out
rm -f .git/index.lock          # 스테일 락 있으면
git add -A
git commit -m "feat: <한국어 요약>"
# 푸시는 나중에 한꺼번에:
git push -u origin docs/용주-작업계획-수립
```

---

## 4. Sonnet 완료 보고 양식 (지시서마다 동일)

```
## 완료 보고 — [지시서 번호·제목]
- 변경 파일: <경로 목록>
- 한 일: <3~5줄 요약>
- 완료 조건 충족: [x] 항목별 체크
- 검증 결과: tsc / lint / build 통과 여부 + 로그 핵심
- 미해결·판단보류: <있으면. 없으면 "없음">
```

## 5. Opus 검증 기준 (매 사이클)

1. **완료 조건** 항목 전부 충족했는가 (지시서의 체크리스트 대조)
2. **설계 의도**대로인가 (스키마/타입/네이밍 일치)
3. **실제 검증** — Opus 가 직접 `tsc`/파싱/diff 재확인 (보고를 믿지 않고 재현)
4. 판정: ✅ 통과 → 머지 OK / 🔧 수정 → 다음 지시서로 차이만 지시

---

## 6. 알려진 정합성 이슈 (백로그 — Opus 추적)

| # | 이슈 | 영향 | 처리 시점 |
|---|---|---|---|
| BL-1 | **서비스 분류 불일치**: `schema.sql` 시드는 STAGE/BizWork/GovLink/… (placeholder), UI mock(`taxonomy.ts`)은 Connectivity/보안클라우드/M2M/AICC/AIDC/모빌리티/기업솔루션 (실제 LGU+ B2B). 둘 중 무엇이 진짜인지 확정 후 `services` 시드 교체 필요 | 콘텐츠↔서비스 매핑 전부 영향 | Phase 2 진입 전, David 확인 |
| BL-2 | `contents` 카테고리 enum 값(`뉴스`,`가트너`…)과 UI 라벨(`뉴스 & 미디어`,`가트너 리포트`…) 매핑 테이블 필요 | 카드 표시 | 지시서 01 에서 라벨맵 추가 |

---

## 7. 진행 중인 Sonnet 지시서 큐

| 번호 | 제목 | 상태 | 선행 |
|---|---|---|---|
| 01 | TS 타입 동기화 (schema → types.ts) | 🟢 검증완료(머지 대기) | 스키마 ✅ |
| 02 | 카테고리 뱃지→"오늘 N건" 텍스트(#14) | ⚪ 대기(독립) | — |
| 03~ | crawl_logs SQL·타입 / #12 fuzzy중복 / #13 품질 / #23 현황 | 대기 | Opus #4 |

**Opus 트랙 병렬 진행**: #3 크롤링 파이프라인 설계 🟢 완료([설계](./크롤링-파이프라인-설계.md)·[시퀀스](./크롤링-시퀀스.mermaid)) → 다음 #4 구현(블로커: PRD 4장 소스 목록 Q1).

> 마스터 계획은 [`용주-작업계획.md`](./용주-작업계획.md) 의 Sonnet 지시서 묶음 표 참조.
