# Insight Out 작업계획서 — 2026-07-11

이 세션까지의 진행 현황과 다음 실행 계획.

## 0. 작업루틴 (인수인계 필수 — 새 세션 먼저 읽기)
전체 표준: **`docs/협업-작업루틴-템플릿.md`**. 인사이트 아웃 역할 매핑:

| 역할 | 담당 | 하는 일 |
|---|---|---|
| 디렉터 | **David** | 방향·우선순위 결정, 위임 전달, "커밋해" 트리거 |
| 플래너 | **Opus(이 세션)** | 지시서 작성(`docs/sonnet-지시서/NN-*.md`)·SQL 핸드오프(`docs/sql-handoff/NN-*.sql`) 작성, **재현검증**(코드 사실 대조 ✅/🔧), 다음 스텝 제안. **git 실행 안 함.** |
| 구현자 | **Sonnet(별 세션)** | 지시서대로 구현 + tsc/eslint/build + 완료 보고, **커밋·푸시 전담**. 검증 통과 전 커밋 X. |
| DB 작업자 | **수희** | Supabase SQL Editor에서 `docs/sql-handoff/*.sql` 실행 |

**1 슬라이스 = 1 루프:** ① David 지시 → ② Opus 지시서(커밋X, 포크/모호점 먼저 질문) → ③ Sonnet 구현·완료보고(커밋X) → ④ Opus 재현검증(실제 diff·grep으로 코드 사실 확인) → ⑤ 통과 시 David "커밋해" → Sonnet 커밋. SQL은 병렬로 수희.

핵심 원칙: 작게 쪼갠다 · 명세(지시서)와 구현 분리 · 구현과 검증 분리(자기 "됐다" 불신) · **검증 통과 전 커밋 금지** · 불확실하면 만들기 전에 질문 · 정직성(막히면 막혔다고). 지시서엔 항상 회귀 가드 + 검증 절차 + 완료 보고 항목을 명시. 신규 컬럼/테이블은 42703/42P01 graceful.

> 기록 루틴: `docs/기록-동기화-루틴.md` 참고. (Notion 기록은 현재 하지 않음 — David 결정.)

---

이후는 현황·계획. 협업 루프: David 지시 → Opus 지시서·재현검증 → Sonnet 구현·커밋 → 수희 SQL.

---

## 1. 완료·배포됨 (커밋 확인)
| 지시서 | 내용 | 커밋 |
|---|---|---|
| 261 | 주간 경쟁사 동향 리포트(코드) | 34aa716 |
| 263 | 어드민 도움말(정보아이콘) | 7f02edd |
| 264 | 유튜브 2차(해시태그·분석패널·연관) | 7f02edd |
| 266 | 유튜브 요약 수집시 생성 + 제목밑·영상크기 | 7b38466 |
| 267 | 본문 푸터 위젯 정제 강화 | 5a701e9 |
| 269 | 로그인 카드 언더바 제거 + 타이틀 한글 | 9dd865e |
| 272 | auth 콜백 실제 에러 노출(진단) | cb3efd2 |
| 274 | 전략보고서 백엔드(AI HTML 생성·발행 API) — **코드만** | 3243d4b |
| 278 | 어드민 IA v2 Phase 1(nav 8그룹·라벨·Danger Zone·사용자 KPI) | 0d98ce3 |
| 279 | 어드민 Phase2-A(insights/content-data 분할: ai-jobs·competitor-weekly·maintenance) | aa37512 |
| 280 | 어드민 Phase2-B(settings/sources 분할: homepage-sections·crawl-settings·source-quality) | 6cc102e |

- 로그인 UI는 262-2(202af90)로 확정·"현재 좋다" 종료.
- 참고(다른 레인): 121 W3, 270/271(그쪽 번호=AI인사이트 카테고리·실험실 네비), 281(토픽커버 폴백 — 재현검증 완료, 커밋 대기).

## 2. 커밋 대기 / 확인 (UPDATE 07-11 오후)
- (완료) 273·281 커밋됨(d85e073·3dcd7f4). HEAD=3dcd7f4.
- **282** 실사 썸네일 커버리지 강화 — 재현검증 통과, **uncommitted**(`enrich-body`·`full-body`·`cover-from-image`·`thumbnail-backfill`·`AdminContentProcessing`). → "282 커밋해".
- **⚠️ topic-covers 파일명 확인**: `public/topic-covers/*.jpg`가 `git status`에 **untracked**로 보임(281 커밋 후에도) → **NFC/NFD 파일명 이중화** 가능성. 커밋된 커버 파일명이 매니페스트 URL과 일치하는지(프로드 200) 확인 필요. 불일치 시 커버 전량 404 → 파일명 ASCII 슬러그화로 근본 해결.

## 3. 수희 SQL 대기 (미적용 시 기능 미노출)
- **261 SQL** `competitor_weekly_reports` 테이블+RLS+프롬프트 2 → **"주간 경쟁 리포트 생성" schema cache 오류 해소.** (가장 시급)
- **274 SQL** `ai_reports` 컬럼 6 + `strategy_report` 프롬프트 → 전략보고서(275/276) 전제.
- (완료) 270 curated 읽기 RLS → 주요 기업 설정 모달. (271은 진단용, 조치 불필요.)

## 4. 구현 대기 지시서 (작성됨 → Sonnet)
의존 순서 주의.
- **275** 전략보고서 서비스 UI(카드형 + HTML sanitize 상세) — 전제: 274 SQL.
- **276** 전략보고서 어드민 HITL(생성·재생성·표지·발행) — 전제: 274·275.
- **277** Azure TTS 공급자 추가 + 폴백 — 독립. 전제: David Azure 키(§5).
- **265** 유튜브 자막 수집 + 번역 스크립트 — 독립 후속. (컬럼 SQL 별도 필요.)

## 5. 외부·David 액션 (코드 밖)
- **sh.jang Google 로그인 auth_failed**: 계정 정상 확인됨(PKCE 유력). 272 배포됨 → 그녀가 쿠키삭제·일반창·동일 URL 재시도 → 화면 실제 메시지 공유. (redirect 계열이면 Supabase Auth URL 등록.)
- **OTP 미도착**: Brevo Transactional 로그 확인 → 시도 없으면 Supabase 커스텀 SMTP 설정, 발송됐는데 미도착이면 발신 도메인 SPF/DKIM.
- **281 배포 후**: 한글 파일명 커버(`통신 b2b.jpg`·`전략보고서 표지-1.jpg`) 프로드 200 확인 — 404면 매니페스트에서 파일명 ASCII 슬러그화.
- **277 전제**: Vercel에 `AZURE_SPEECH_KEY`·`AZURE_SPEECH_REGION`(예 koreacentral)·(선택)`AZURE_TTS_VOICE`·`TTS_PROVIDER=azure`.

## 6. 백로그
- 어드민 IA **Phase 3**(신규 객체 화면): 프롬프트 라이브러리(`llm_prompts`)·AI 작업 로그(`ai_jobs`)·전략보고서 admin(276과 통합)·경쟁사주간 admin·참여분석(`content_views`/`bookmarks`)·표지자산·가입허용목록·큐레이션기업. (`ADMIN_RESTRUCTURE_PLAN.md` §6)
- 리스크 정리(감사): `ai_report_sources.issue_id` 스키마 드리프트, `schema.sql` 중복 cluster_id/인덱스, 미들웨어 `src/proxy.ts` 규약, YouTube 정본 저장(`youtube_videos` vs `contents`), report 명칭 3분(소스/전략/경쟁사주간).
- 전략보고서 AI 표지 생성(나노바나나) 파이프라인, 스케줄 자동 초안.
- **MCP 서버(인사이트 아웃)** — Claude Desktop/ChatGPT에서 다수 관리자가 전략보고서·리포트 작업 후 MCP로 인사이트 아웃에 직접 기록. 읽기 도구(콘텐츠 검색·인사이트·경쟁사 리포트·엔티티 동향) + 쓰기 도구(전략보고서 초안, 274 백엔드 재사용). **원격 HTTPS + OAuth(계정 role 기반) + 감사 로그** 전제(팀 공유). GPT는 Developer Mode + 유료 플랜 필요. → 백로그.
- 커버 커버리지 **2단계(풀 다양화)**: 282(실사) 효과 확인 후 고volume 토픽 커버 확충 + 결 다양화 + 반복 억제 회전.
- 문서 git 추적 여부(지시서·설계·SQL 핸드오프 다수 untracked).

## 7. 권장 실행 순서
1. **273 커밋** + **281 커밋(이미지 포함)** — 검증 끝난 것 정리.
2. **수희에게 261 + 274 SQL 전달** — 눈에 보이는 오류(주간 경쟁 리포트) 해소 + 전략보고서 언블록.
3. 274 SQL 적용 후 **275 → 276**(전략보고서) Sonnet 진행 → Opus 재현검증.
4. **277(Azure)** 은 키 준비되면, **265(자막)** 은 후속.
5. 배포 후 검증: sh.jang 재시도·OTP/Brevo·281 한글커버 200.
6. 정리되면 **어드민 Phase 3** 착수 여부 결정.
