# Insight Out 작업계획서 — 2026-07-12 (마감 갱신)

협업 루프: David 지시 → Opus 지시서·재현검증 → Sonnet 구현·커밋·푸시 → 수희 SQL.
루틴: `docs/협업-작업루틴-템플릿.md` (⓪ 최신화 + **`git log --stat` 읽기** · git은 구현자 전담 · rebase 금지)

---

## 0. ⭐ 오늘의 결론 — **"만든 게 안 쓰이거나 안 보인다"**

어드민 화면을 처음 열어보고 계획이 뒤집혔다. **UI를 다듬고 있을 때가 아니라 데이터가 안 만들어지고 있었다.**

| | 있었던 것 | 없던 것 |
|---|---|---|
| 305 | `content_views` API 완성 | **호출부** |
| 307 | 콘텐츠형 상세 껍데기 | 본문만 **iframe** |
| 310 | 요약 크론 | **SQL·감시목록·어드민버튼** → **요약 0건** |
| 311 | 피드 검증 코드 | 크롤러와 **다른 User-Agent** |
| 312 | `source_quality_stats` RPC 코드 | **SQL 미적용** → 품질 지표 전부 `—` |
| 313 | `lgu_impact` 생성 + 근거 적재 | **화면 노출 0회** |
| B-1 | `/admin/source-quality` 진단 화면 | **아무도 안 봄** |
| 요약 | `toExcerpt` 본문 폴백 유틸 | **`null`을 넘겨 죽여둠** |

**새로 만들 게 적었다. 연결하고 보이게 하는 게 대부분이었다.**

### 반복된 실패 두 종류

**① 조용한 실패(silent failure)** — graceful이 silent면 안 된다.
`276`(근거 적재) · `310`(요약 0건, `job_runs`엔 '성공') · `312`(RPC 없는데 빈 객체) · `305`(POST 미관측) — **전부 에러 없이 아무 일도 안 일어났다.**

**② "사람이 지킬 규칙"은 반드시 깨진다**
`293`(생성기 목록 주석) · `292`(`EXPECTED_CRONS` 손 유지 → **하루에 두 번** 어긋남)
→ **292 후속에서 `vercel.json`을 단일 진실로 삼고 빌드 실패로 강제**했다. **그게 정답이다.**

---

## 1. ✅ 오늘 배포됨 (커밋 40+)

| 지시서 | 내용 |
|---|---|
| **293** | LLM 산출물 `<quote>`·UUID 전역 제거 (11개 생성기 전수) |
| **294** | 콘텐츠 검수 2패널 모달 + 연속 검수 + **키워드 편집**(검수 화면에 없던 것) |
| **305** | `content_views` 조회 계측 — ⚠️ **POST 미관측, 확인 필요** |
| **307** | 리포트 본문 — **인라인 PDF 뷰어**(pdf.js·텍스트 레이어·가상 스크롤) + 텍스트 탭 + 다운로드 |
| **310 + 후속** | ⭐ **요약 0건 복구** — 영구 박제 방지 · **LLM 한도소진 분리** · 폴백 3종 · 어드민 수동 버튼 |
| **311** | 피드 실검증(크롤러와 동일 fetch·파서) — 정부 소스 4개 **RSS 폐지 확인** |
| **312** | 수집 가시성 — 제외 건수·사유 기록 · **품질 지표 실패 노출** · 경고 3단계 분리 |
| **313 · 299** | 위기/기회 배지 + **역참조**("이 기사를 인용한 리포트·인사이트") · 경쟁사 카드 정리 |
| **309** | 기본 렌즈(`default_lens`) — 콘텐츠 목록에 **개인화가 아예 없었다** |
| **292 후속** | `EXPECTED_CRONS`를 `vercel.json`에서 파생 — **빌드로 강제** |

이전: 282 · 275/276 · 283/284 · 265 · 285/286/291 · 287 · 288 · 289/292 · 290

---

## 2. 🔴 확인 대기 — **이게 다음 작업이다**

**배포 ≠ 동작.** 8번 배포했는데 **실제로 도는 걸 본 게 거의 없다.**

### 2-1. 수희 SQL (8건 대기)

| 순위 | 파일 | 효과 |
|---|---|---|
| **1** | **`186-source-quality.sql`** | ⭐ **코드 배포 무관, 즉시 품질 지표 부활.** 가장 싼 수리 |
| **2** | `312-crawl_logs-rejected.sql` | **457건(67%)의 행방**이 드러남 |
| 3 | `293-LLM프롬프트-인라인태그금지.sql` | 프롬프트 정정 (+ **305 확인 쿼리** 포함) |
| 4 | `309-default_lens.sql` | DB 기본 렌즈 |
| 5 | `313-역참조-인덱스.sql` | ⚠️ **확인 쿼리 먼저** — 4종 다 0이면 근거 적재(276)가 실패 중 |
| 6 | `296-유튜브-발행일-진단.sql` | **측정만.** 결손률 0%면 296 취소 |
| 7 | `306-죽은컬럼-제거.sql` | **[STEP 1] 확인만.** 삭제는 David 확인 후 |

**모든 SQL: 에디터를 완전히 비우고(Cmd+A → Delete) 붙여넣을 것.**

### 2-2. David (화면 확인)

| 확인 | 왜 |
|---|---|
| ⭐ **`/admin/ai-jobs` → 본문 수집 → 뉴스 요약 백필** | **오늘 최대 사고를 고쳤는데 아무도 안 눌러봤다.** `filled` 숫자가 올라가야 끝난 것 |
| ⭐ **내일 오전 `/admin/job-runs`** | 크론 8개가 **진짜 안 도는지 그때 판정**된다(계측이 14:18 배포, 크론은 새벽 실행) |
| **콘텐츠 상세** | 위기/기회 배지 · **"이 기사를 인용한 리포트·인사이트"** 블록이 뜨는지. **안 뜨면 근거 적재 실패** |
| **`[내 업무]` 렌즈 2페이지 이상** | 임베디드 조인 + 페이지네이션이 맞물리는 지점 |
| **iOS Safari 리포트 상세** | 기존 iframe이 실제로 깨져 있었는지(307이 고장 수리였는지) |
| **`/admin/llm`** | LLM 실사용량 — **추측 말고 실측** |
| **`/admin/sources`** | 죽은 정부 소스 4개 **비활성화** + Google News RSS로 대체([피드 검증] 버튼 사용) |

> ⛔ **`body-backfill`이 안 돌면 요약이 계속 `not_ready`다.** 310이 무력화된다. **내일 1순위.**

---

## 3. 📋 지시서 준비됨 — 위임만 하면 됨

| # | 내용 | 크기 | SQL | 비고 |
|---|---|---|---|---|
| **296** | 유튜브 발행일 결손 — **수집일을 발행일로 표시하는 거짓말** 정정 | 소~중 | **진단 먼저** | 결손률 0%면 **취소** |
| **306** | 죽은 컬럼(`position`·`content_filter_mode`) 정리 | 소 | 2단계 | **308 선행. 오픈 후 권장**(되돌릴 수 없음) |
| **308** | 마이페이지 설정 허브 | 중 | 없음 | ⚠️ **§1 재검증 필요** — 309가 mypage를 이미 바꿨다 |

> ⚠️ **298(유튜브 키워드)은 지시서가 없습니다.** 이 표에 "준비됨"으로 적혀 있었으나 **파일을 쓴 적이 없습니다.** 하려면 새로 써야 합니다.
> ⚠️ **314는 지시서가 없습니다.** `docs/_보류/314-proxy.ts.보류` 는 **코드 파일**입니다.
> **297·316은 배포 완료**(`4724955`)라 이 표에서 뺐습니다.

### 3-1. 🔢 지시서 번호 대역 — 레인끼리 겹치지 말 것

| 대역 | 주인 |
|---|---|
| ~316 | **이 레인**(제품·성능). 마지막: 316 속도 개선 |
| **317~329** | **이 레인의 완충 대역. 다음은 317부터.** |
| 330~ | **다른 레인**(어드민 IA) — 330·331·332 사용 중 |

**지금까지 번호가 23번 겹쳤습니다.** 다른 레인이 316/317/318 → 330/331/332로 양보하며 대역을 나눴습니다.
⛔ **새 지시서 전에 `ls docs/sonnet-지시서/ | sort -n | tail -20`.**

---

## 4. 🔵 남은 과제

### 오픈 전
- **305 확인** — `content_views`에 데이터가 쌓이는지. **어드민 화면이 전무**하다
- **정부 소스 대체** — Google News RSS(정책·규제 쿼리). 정책 축이 통째로 비어 있다
- **경쟁사 주간 역참조** — 근거가 `sections jsonb`에 묻혀 있어 313에서 빠졌다

### 수집 프로젝트 (B) — **B-1은 이미 답이 나왔다**
`/admin/source-quality`가 진단 화면이었고, 오늘 그걸 열어서 311·312가 나왔다.

| 단계 | 근거 |
|---|---|
| **B-2** RSS 상태 체계 | 312가 3단계 분리를 시작했다. **소스 유형별 기준 차등**이 남음 |
| **B-3** 본문 상태 필드 | **310의 `not_ready`가 첫걸음** |
| B-4 본문 보강 폴백 체인 | `body-backfill`이 실제로 도는지 먼저 |
| B-5~B-8 | RSS 발견 · 과거 백필 · 중복 3단계 · 재분석 |

> ⚠️ **B-6(과거 백필)은 LLM 무료 한도를 태운다.** 310의 `rate_limited` 가드가 있어야 안전하다.

### 오픈 후
302(관계지도 RPC 확장) · 301(어드민 테이블 공통화) · 277(Azure TTS, 키 대기) · 303(관계 유형 LLM) · MCP 서버 · 참여분석 화면

---

## 5. ⛔ 폐기 결정 (사유를 남긴다 — 또 나온다)

### PDF → HTML 자동 변환
**변환은 퀄리티 상한이 원본이다.** 뷰어는 상한이 없다. 리포트 13건에 파이프라인을 지을 규모가 아니고, 사용자는 42페이지 리서치를 **웹에서 정독하지 않는다.** → **307(뷰어)로 대체.**

### LG U+ "시사점 서술문"
**개별 기사에 시사점은 뻔하다.** 진짜 시사점은 여러 기사를 묶어야 나오고, **그건 인사이트·리포트가 이미 하는 일**이다. LLM은 null을 안 내고 억지로 만든다. → **313(위기/기회 배지 + 역참조)로 대체. LLM 0.**

---

## 6. 실행 순서

```
🔴 지금 (코드 아님 — 확인)
 1. 수희 → 186 SQL           ← 즉효. 품질 지표 부활
 2. /admin/ai-jobs → 본문 수집 → 요약 백필   ← 오늘 최대 사고가 고쳐졌는지
 3. 수희 → 312 · 293 · 309 · 313(확인쿼리) SQL

🔵 내일 오전
 4. /admin/job-runs — 크론 8개 판정 (body-backfill이 핵심)

📋 위임 대기
 5. 297  필터 바 정리        ← 지금이 뺄 유일한 시점
 6. 296  유튜브 발행일       ← 진단 결과 보고
 7. 298  유튜브 키워드
 8. 308  마이페이지 (§1 재검증 후)
 9. 314  proxy.ts (프리뷰 검증)
10. 306  죽은 컬럼 (오픈 후)

🔵 오픈 후
   B-2~B-8 · 302 · 301 · 277 · 303
```

---

## 7. 루틴에 기록된 교훈 (`협업-작업루틴-템플릿.md`)

- **⓪ 최신화 때 `git log --oneline --stat origin/main -5`를 읽어라.** `merge`는 충돌만 막지 **의미 충돌**은 못 막는다. 오늘 **세 번** 당했다(`cc59a1d` 요약 분리, `9e10230` key_insights 폐기 ↔ MCP, PR #40이 309를 말없이 구현).
- **크론은 혼자 오지 않는다** — ①SQL ②감시 목록 ③어드민 수동 버튼이 세트.
- **§5.1 플래너가 가장 자주 틀리는 방식: 부분 grep으로 단정.** 오늘 4번 틀렸다(생성기 3→11 · `validateSourceRows`가 fetch 안 한다 → 하고 있었다 · 마이페이지가 네비에 섞여 있다 → 없었다 · 검수 모달에 키워드가 있다 → 0회).
- **§5.2 규칙은 코드로 강제한다.** 주석으로 부탁하면 깨진다. **빌드가 실패하게 만들어라.**

---

## 8. 잔여 정리

- **`.git/_locktrash` 91개** — 내(플래너) 샌드박스가 만든 것. David 맥에서 `rm -rf .git/_locktrash`
- **플래너는 이제 `git fetch`/`merge`를 쓰지 않는다** — 락이 남아 구현자의 병합을 막았다. 재현검증은 워킹트리를 직접 읽는다
- `gc.auto=0` · `maintenance.auto=false` 되돌리기 (David 맥)
- 빌드 경고 2개: workspace root 추론 · **middleware deprecation**(314가 해결)
