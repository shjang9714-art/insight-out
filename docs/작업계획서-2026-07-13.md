# 작업계획서 — 2026-07-13

> 루틴: `docs/협업-작업루틴-템플릿.md` · 역할: 디렉터=David / 플래너=Opus / 구현자=Sonnet / SQL=수희
> 신호등: 🔴 즉시 · 🟡 결정 필요 · 🟢 착수 가능 · 🔵 진행 중 · ⚪ 백로그
>
> **번호 대역**: 317~329는 **속도개선 레인(316)** 완충. 우리 레인은 **330~**. (295·313·316에서 세 번 충돌했다)

---

## 0. 오늘 나간 것

| SHA | 슬라이스 | 화면 확인 |
|---|---|---|
| `5a0f840` | **308** 마이페이지 설정 허브 + 죽은 설정 제거 | ❌ 미확인 |
| `37102e9` | **332** 콘텐츠 검수 본문 보강(행·선택) | ✅ **확인** |
| `c8809be` | 문서 16개 | — |
| `9c5b405` | **330** 어드민 IA v3 — nav 재편 + `ai-jobs` 2화면 분리 | ✅ **확인** |
| `f819fdf` | (다른 레인) 316 속도개선 §3-1 `staleTimes` | — |

**⭐ 오늘 처음으로 "배포한 게 실제로 도는 것"을 확인했다.**
인수인계 §2: *"8번 배포했는데 실제로 도는 걸 본 게 거의 없다."* → 330·332는 끊었다.
확인된 것: 사이드바 IA · `/admin/enrich` 카드 7 · `/admin/ai-jobs` 카드 5 · **`/admin/job-runs`에 기록 남음** · 본문 보강(↺) 동작

**수희 SQL 적용 완료**: 186 · 293 · 296 · 306(STEP1) · 309 · 312 · 313 · 315

---

## 1. 🔴 지금 진행 중 / 대기

| # | 내용 | 담당 | 상태 |
|---|---|---|---|
| **331** | 운영 대시보드 관제화면 정리 | Sonnet | 🔵 구현 중 |
| **334** | `users.role` 자기승격 차단 (트리거 1줄) | 수희 | 🔵 STEP 2 요청함 |
| **335** | `users` 쓰기 서버화 + `authenticated` 권한 회수 | — | 🟢 **지시서 준비됨** |

**334는 임시 방어다** — "몰래 바꾸면 조용히 되돌린다". 인수인계 §4-1의 graceful-silent를 우리가 일부러 만드는 것.
**답은 335다** — `authenticated`가 `users`를 **아예 못 쓰게** 한다. 그 뒤 SQL `336`(revoke)을 **코드 배포 확인 후에** 적용.

> ⛔ **335 → 화면 확인 → 336** 순서. 뒤집으면 온보딩·마이페이지 저장이 즉시 죽는다.
> 가장 위험한 지점은 **온보딩**이다. 깨지면 아무도 로그인 못 한다(미들웨어가 전 경로를 `/onboarding`으로 되돌린다).

---

## 2. 🟢 다음 슬라이스 — **337 백그라운드 잡 + 우하단 진행 인디케이터**

> **David 결정: A안 — "어드민 안에서만" (싸다)**

### 현재 (확인된 코드 사실)
| | |
|---|---|
| **크롤 실행** (`crawl-now`) | ✅ **이미 진짜 백그라운드** — `route.ts:92` Next `after()`. `SourceQualityManager`가 localStorage(`insight-out:admin-crawl-job`) + `setInterval` 폴링으로 추적. **별도 진행 테이블 없이 `crawl_logs` 행을 세어 역산한다** |
| **보강 잡 12개** | 🔴 **브라우저 `while(true)` 루프** (`AdminContentProcessing.tsx:110,151,192,233,274,315…`). `await fetch` → `sleep(300)` 반복. **화면을 떠나면 진행현황이 사라진다** |

### 설계 (A안)
- 루프를 **`src/app/admin/layout.tsx` 레벨 전역 Provider**로 올린다 → 어드민 내 페이지 이동해도 계속 돈다
- **우하단 floating 진행 인디케이터** — 실행 중 잡 목록 · 진행률 · 중단 버튼
- **`ENRICH_JOBS`(330에서 만든 메타)를 그대로 재사용** — 잡 목록을 또 하드코딩하지 말 것
- ⚠️ **탭을 닫거나 새로고침하면 루프는 끊긴다.** 이때 **조용히 멈추면 안 된다** — localStorage에 잡 상태를 남겨, 다시 열면 **"중단됨 · 남은 N건 · [이어서 실행]"** 을 보여준다
- 진행 추적은 **`job_runs`**를 활용 (매 호출이 이미 행을 쓴다)
- 레퍼런스는 `SourceQualityManager`의 폴링/복원 패턴. **새로 발명하지 말 것**

---

## 3. 🟡 결정 대기

| # | 결정 | 왜 필요한가 |
|---|---|---|
| **D1** | **외부 고객사 판매가 확정 계획인가?** | 인증 방향을 가른다. 사내 9명이면 **패스워드리스 유지**가 싸고 안전. 외부 조직이 붙으면 **초대 코드** 필요. 스펙의 비밀번호 플로우는 **현재 코드에 전제가 하나도 없다**(`signInWithPassword` 0회) |
| **D2** | 유튜브 발행일 **결손률** (SQL 333 ④) | **0%면 지시서 296을 통째로 취소** |
| **D3** | `users.position` 버려도 되나 (SQL 333 ②) | 306 [STEP 2] 실행 여부 |
| **D4** | 저활성 소스 **59/87 (68%)** 원인 | David 검토 중 — "애초부터 잘못된 URL·RSS" 가설 |

상세: `docs/인증개편-현황분석-2026-07-13.md` · `docs/어드민개편-현황대조-2026-07-13.md`

---

## 4. 🟢 코드 없이 화면에서 되는 것

### 4-1. 죽은 정부 소스 4개 — **11일째 HTTP 404**
```
개인정보보호위원회 · 국토교통부 · 산업통상자원부 · 정책브리핑 보도자료
🔴 연속실패 6 · 피드 HTTP 404 · 마지막 수집 2026-07-02
```
`/admin/sources`에서 **비활성화**. 인수인계 §2-2가 예고한 그대로 확인됐다.
> ⚠️ Google News RSS 대체는 **본문확보율부터 보고** 결정. 소스 품질에서 `Google News - 5G 특화망`의 **본문확보 0%**였다.

### 4-2. 아직 안 본 화면
| 화면 | 볼 것 | 슬라이스 |
|---|---|---|
| **콘텐츠 상세** | **"이 기사를 인용한 리포트·인사이트"** — **안 뜨면 근거 적재(276)가 조용히 실패 중** | 313 |
| `/dashboard/mypage` | 탭 3개 · [기업 설정]이 기업동향과 **같은 모달**인지 · 기본 보기 → 콘텐츠 즉시 반영 | 308·309 |

---

## 5. ⚪ 백로그 — UI 개선 (하나씩 깬다)

> **David: "UI 개선은 백로그로 두고 하나씩 깨자"**

**근본 원인**: 어드민에 `<table>`이 **16개인데 공통 컴포넌트가 0개**다. `src/components/admin/ui/`에 `AdminEmptyState`·`AdminTabs`·`StatusBadge`는 있는데 **테이블만 없어서 16개가 각자 그렸다.**

| # | 항목 | 비고 |
|---|---|---|
| **U1** | **공통 `AdminTable` 신설** | 파일럿 = 콘텐츠 검수. 컬럼 정의·리사이즈·빈상태·정렬을 한 곳에 |
| **U2** | 콘텐츠 검수 **2줄형 전환** | 제목/본문 넓게 · 메타는 칩 · 관리는 아이콘. **가로 스크롤 자체를 없앤다** |
| **U3** | **관리 컬럼 톤앤매너** | 지금 혼자 `sticky right-0` + `boxShadow inset` → **테이블에서 분절돼 박스처럼 보인다**(`AdminContentManager.tsx:1389,1463`). sticky 쓰는 어드민 파일은 **이것 하나뿐** |
| **U4** | **텍스트 잘림 · 한눈에 안 들어옴** | `truncate` 남발. 16개 테이블 전수 점검 |
| **U5** | 나머지 15개 테이블 순차 적용 | U1 이후 |

**U1 → U2·U3 → U4 → U5** 순. 한 번에 갈아엎지 않는다.

---

## 6. ⚪ 백로그 — 기능

| # | 항목 | 왜 큰가 |
|---|---|---|
| **F1** | **소스관리 + 소스품질 통합** | 797 + 824 = **1,621줄.** 탭 분해 전제. 280에서 *의도적으로* 분리한 것을 되돌리는 것 |
| **F2** | **키워드 규칙 통합** | `keywords` + `keyword-groups` + `exclusion-rules` + `crawl-settings` **4화면 → 탭** |
| **F3** | **크롤 로그 상단 일별 차트** | recharts 0회. 요약 카드 6개만. ⚠️ **실패 사유 필터는 못 만든다** — `error_message`가 자유 텍스트라 분류 enum이 없다. 기존 `RejectedBy` 5종으로는 가능 |
| **F4** | **참여 분석 신설** | **DAU/MAU**(grep 0회, `last_seen_at` 원재료 있음) + **북마크한 사람**(bookmarks×users 조인이 어드민에 **0곳**) |
| **F5** | **`/admin/tts` 신설** | TTS는 **이미 구현돼 있다**(Google Cloud TTS, `/admin/briefings`에 생성·재생성·미리듣기·사용량). 진짜 빈 곳은 **실패 로그** — TTS 라우트만 `runJob` 래핑이 없어 `job_runs`에 안 남는다 |
| **F6** | **라벨 8종 재정의** | 현재 분류가 셋으로 쪼개짐(`signal_hint` + `sentiment` + `lgu_impact`). 합치면 **LLM 프롬프트·백필·DB·기존 데이터 마이그레이션** 전부 따라옴 |
| **F7** | **콘텐츠 건강 6지표 재정의** | 현재 4지표. 스펙의 6지표(정상비율·썸네일누락·요약누락·중복의심)와 다름 |
| **F8** | **인증 개편** | **D1 결정 후.** 현행 유지+구멍 막기면 작다(335가 그 절반). 비밀번호 도입이면 크다 |

---

## 7. ⚪ 정리 안 된 것

- **미커밋 문서** — `인수인계-2026-07-12.md` (⚠️ **이 세션이 시작된 그 문서가 git에 없다**) · `작업계획서-2026-07-12.md` · `협업-작업루틴-템플릿.md`(52줄 추가) · `293 SQL`(수정) · 오늘 만든 문서 5개
- **번호 중복 가드** — 295·313·316 **세 번** 났다. 빌드로 강제. **지금 `313`이 두 개**라 붙이면 즉시 실패 — 그게 정상(하나는 폐기본)
- `public/topic-covers/*.jpg` 3개 untracked — 의도한 건지 확인 필요
- **`signup_email_allowlist`가 `schema.sql`에 없다** — 핸드오프 SQL로만 존재. 새 환경 구축 시 누락된다
- **프로필 캐시 15분** — admin 강등·승인 취소가 최대 15분 반영 안 됨 (`profile-cache-cookie.ts:5`)
- **HMAC 키가 `SUPABASE_SERVICE_ROLE_KEY` 재사용** (`profile-cache-cookie.ts:16`)
- **도메인 게이팅 fail-open** — 유일한 차단선이 Supabase 대시보드 Hook. 앱·DB 어디에도 도메인 검사 없음

---

## 8. ⭐ 우선순위

```
🔴 1. 334 트리거 적용                수희 (요청함)
🔵 2. 331 재현검증 → 커밋            Sonnet 보고 대기
🟢 3. 335 users 서버화 → 336 revoke  ← 보안 근본 수정. 온보딩이 위험지점
🟢 4. 337 백그라운드 잡 + 인디케이터  ← A안 확정. ENRICH_JOBS 재사용
🟢 5. 죽은 정부 소스 4개 비활성화     David · 화면에서 5분
🟡 6. D1 결정 → 인증 개편 방향
⚪ 7. U1 공통 AdminTable → U2 2줄형   ← UI 백로그 시작
⚪ 8. F1 소스 통합 · F2 키워드 통합
```

### 왜 이 순서인가

- **보안(3)이 UI(7)보다 먼저다.** 어드민을 예쁘게 만들어도 **권한이 뚫려 있으면 의미가 없다.**
- **337(4)이 U1(7)보다 먼저다.** 백그라운드 잡은 **운영자가 매일 쓰는 기능**이고, 지금은 화면을 떠나면 진행현황이 사라진다. 테이블 톤앤매너는 불편하지만 **일이 되긴 된다.**
- **U1을 F1·F2보다 먼저** — 소스관리·키워드를 통합하면 **어차피 테이블을 다시 그린다.** 공통 컴포넌트가 먼저 있어야 두 번 안 한다.
